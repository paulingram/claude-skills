# Tasks — informative-run-notifications (v3.34.0)

- [x] 1. Extend `scripts/notify/notify.py` — `EVENT_TYPES` 6 → 10 (`run_start` / `waiting_on_agents` / `agents_complete` / `run_complete`), the universal `--details` / `--progress` / `--next-step` options + `_context_trailer`, the `--agents` roster, the repeatable `--plan-file` embed (`_read_plan_file` / `render_plan_section`, 20k-char/file + 8-file caps, missing-file note, never raises), the four new event bodies; preserve every existing render substring + the best-effort exit-0 contract. (REQ-001, REQ-002)
- [x] 2. Update engine tests — `tests/test_notify.py` (the 6→10 pin renamed `test_exactly_ten_event_types` + 16 new cases incl. an end-to-end SMTP-mocked `run_start --plan-file` CLI case) + `tests/test_heartbeat.py` (the exactly-ten pin). (REQ-008)
- [x] 3. Rewrite `common-pipeline-conventions` `## Notifications wiring convention` — the ten-event table, the "Informative, not just status" contract + engagement-email rule, the run-level bookends, the dispatch-wait pair; fix the stale "6th event type" heartbeat reference. (REQ-003)
- [x] 4. Wire `architect-team-pipeline` — section rewrite + run_start bash at the Phase 1 gate exit (`--plan-file` proposal/design/tasks) + waiting_on_agents/agents_complete bash at Phase 2/3 (+ named anchors at −1B/3b/6) + run_complete subsection at Phase 8. (REQ-004)
- [x] 5. Wire `bug-fix-pipeline` — section rewrite + run_start bash at the B3 gate exit (fix proposal) + the pair anchored at B1 (+ named at B5/B6/B6b) + run_complete at B8. (REQ-005)
- [x] 6. Wire `mini-architect-team-pipeline` — section rewrite + run_start bash at the M3 convergence (the 5-artifact bundle) + the pair at M4/M5 + run_complete at M7 after the auto-merge (M8 escalation defers to the full pipeline). (REQ-006)
- [x] 7. Wire `ux-test-builder` — the NEW full `## Notifications` section (U0–U9 boundaries; the documented `deploy` exclusion) + run_start at U4 (`distilled-flows.json` + `literal-flow.json`) + the pair at U3/U6 + issue_discovered at U8 + git_commit + run_complete at U9; add the `commands/ux-test.md` note; update the `commands/architect-team.md` note five → ten. (REQ-007)
- [x] 8. Update wiring tests — `tests/test_notify_wiring.py` rewritten (ten-event vocabulary pin, FOUR-pipeline parametrization via the per-pipeline required-event map, bookend + plan-embed + dispatch-wait + informative-contract + engagement-email + CPC-vocabulary + ux-test routing + command-note pins) + `tests/test_bug_fix_pipeline_notifications.py` (TEN_EVENTS listing + B3/B8/B1 pins) + the `tests/test_dispatch_banner.py` version pin → 3.34.0. (REQ-008)
- [x] 9. Document + release — README (notifications section, NEW IN v3.34.0, badges, timeline), CHANGELOG `## [3.34.0]`, CLAUDE.md (intro + Stack + Recent releases), `docs/CODEBASE_MAP.md` + `docs/INTEGRATION_MAP.md` (notify entries + note ledgers + counts), the `project-email-notifications` living-spec delta, `.architect-team-notify.example.json`; bump `.claude-plugin/plugin.json` + `marketplace.json` to 3.34.0. (REQ-009)
- [x] 10. Full suite green under both Windows cp1252 and `PYTHONUTF8=1`.
