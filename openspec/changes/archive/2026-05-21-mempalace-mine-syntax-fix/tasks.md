# Tasks — mempalace-mine-syntax-fix

## REQ-1 — documented `mine` commands match the installed CLI

- [ ] **T-1** — Audit + remove `--room`. Run `grep -rn -- "--room"` across `skills/`, `agents/`, `commands/`. Remove the `--room <room>` argument from every `mempalace … mine` command in: `skills/architect-team-pipeline/SKILL.md`, `skills/mempalace-integration/SKILL.md`, `skills/editability-completeness/SKILL.md`, `agents/route-mapper.md`, `agents/diagnostic-researcher.md` (and any other file the grep surfaces). Each command keeps `--palace`, `mine <path>`, and `--wing <wing>`. Do not invent replacement flags — `mine` has none for rooms.
- [ ] **T-2** — Reconcile the room model in `skills/mempalace-integration/SKILL.md`. Rooms are detected by `mempalace init` from the directory structure of the mined corpus — they are NOT a `mine` argument. Reframe the room-taxonomy section: keep the conceptual artifact categories (codebase-maps, route-maps, coverage-maps, solution-requirements, diagnostic-plans, final-reports, etc.) as documentation of how the `.architect-team/` + `openspec/` layout maps to MemPalace's auto-detected rooms, but make explicit that mining specifies only `--wing`. Add a note to the skill's operating rules.
- [ ] **T-3** — `tests/test_mempalace_integration.py`: update or remove any test that asserts a `mine … --room` form is used; ADD a regression test that asserts NO skill / agent / command file contains `mine` followed by `--room` (so the defect cannot silently return). Run the full suite green.
- [ ] **T-4** — Docs + release: `CHANGELOG.md` `## [0.9.14]` entry (leave the historical v0.9.4 `--room` mentions intact); check `README.md` and `docs/CODEBASE_MAP.md` for prose that describes `mine --room` and correct it if present; `.claude-plugin/plugin.json` + `marketplace.json` → `0.9.14`; `README.md` banner `v 0 . 9 . 14` + tests badge count. Re-verify the README freshness tests pass.
