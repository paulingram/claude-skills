# tasks — opus-5-model-upgrade

Two implementation teams with **disjoint** file scope, dispatched in parallel. Team A owns executable policy + the living spec + the code-side test pins; Team B owns instruction-surface prose (trailers + current-state docs). Neither team touches `agents/*.md` or `tests/test_agents.py` — those are the run's negative control. The version-bump triple and the release entry are the orchestrator's / `doc-updater`'s Phase 8 scope, not a team's.

## A. Model-policy team — executable surfaces + living spec (REQ-001, REQ-003, REQ-006 partial)

Files owned: `services/common/service_config.py`, `scripts/setup/install_gateway.py`, `scripts/setup/set_default_model.py`, `openspec/specs/fable-default-setup-fixes/spec.md`, `tests/test_services_common.py`, `tests/test_install_gateway.py`, `tests/test_evals_offline.py`, `tests/test_set_default_model.py`

- [ ] A.1 `services/common/service_config.py` — **two** edits that must land together: line 21 `FALLBACK_MODEL = "claude-opus-4-8"` → `"claude-opus-5"` with the inline comment refreshed, and line 31 (inside `resolve_model`'s docstring, function spans 25-53) `else ``fallback`` (Opus 4.8 by default)` → Opus 5. Add a comment recording the owner's decision + the surfaced trade-off (Opus 5 is newer than Fable 5, so this fallback is less likely to resolve on a harness predating the `fable` alias; shipped as directed). Do NOT change `DEFAULT_MODEL`. Do NOT substitute a laddered or unchanged value.
- [ ] A.2 `openspec/specs/fable-default-setup-fixes/spec.md` — amend the `Requirement: Fable 5 service-tier default with injected fallback` SHALL (line 88) and the `Scenario: rejecting checker falls back to opus` THEN (line 98) to `claude-opus-5`; add the constant-and-docstring-agree scenario per the change's delta spec. Do NOT touch the neighbouring `--model opus` remediation scenario (~line 83) — it concerns the uniform lever, not the fallback id. **Lockstep with A.1**: `hooks/pipeline-completion-audit.py:367` runs `openspec validate --all --strict` as a Stop-hook gate.
- [ ] A.3 `scripts/setup/install_gateway.py` — add `claude-opus-5` to `ANTHROPIC_EXPLICIT_MODELS` (305-313) and refresh the ordering comment (302-304) which currently calls `claude-opus-4-8` "the implemented fallback". **Retain every legacy id** — the tuple is a routable-id allow-list, not a currency statement; the `*` catch-all was observed non-functional on a real install.
- [ ] A.4 `scripts/setup/set_default_model.py` — exactly **two** lines: 6 (module docstring "the IMPLEMENTED Opus-4.8 fallback") and 199 (the delivery-split header comment "run the DELIVERY + ADVERSARIAL agents on Opus 4.8"). Leave `OPUS_MODEL = "opus"` (207), `VALID_MODELS` (76), and every versionless Opus mention — including argparse help line 603 — byte-identical.
- [ ] A.5 Test pins: `tests/test_services_common.py:284,294,304,331` → `claude-opus-5`; `tests/test_install_gateway.py:441` → assert BOTH `claude-opus-5` and the retained `claude-opus-4-8`; `tests/test_evals_offline.py:273` sample model string; `tests/test_set_default_model.py:6` docstring (hyphenated `Opus-4.8` form — no assertion depends on it).
- [ ] A.6 Verify: `python -m pytest -q tests/test_services_common.py tests/test_install_gateway.py tests/test_evals_offline.py tests/test_set_default_model.py tests/test_agents.py` — the first four reflect the new ids, `test_agents.py` passes **unmodified**. Then `npx openspec validate --all --strict`.
- [ ] A.7 Review evidence (schema v7) + independent `task-reviewer` pass.

## B. Instruction-surface team — trailers + current-state docs (REQ-004, REQ-005)

Files owned: `commands/architect-team.md`, `commands/classify-test-prod-safety.md`, `commands/visual-qa.md`, `commands/visual-to-api.md`, `skills/architect-team-pipeline/SKILL.md`, `skills/bug-fix-pipeline/SKILL.md`, `skills/mini-architect-team-pipeline/SKILL.md`, `tests/test_mini_run_trailer.py`, `CLAUDE.md`, `docs/CODEBASE_MAP.md`, `docs/INTEGRATION_MAP.md`, `README.md`

- [ ] B.1 Commit trailers — 7 live surfaces (`commands/architect-team.md:191`, `commands/classify-test-prod-safety.md:73`, `commands/visual-qa.md:103`, `commands/visual-to-api.md:122`, `skills/architect-team-pipeline/SKILL.md:657`, `skills/bug-fix-pipeline/SKILL.md:547`, `skills/mini-architect-team-pipeline/SKILL.md:303`): `Claude Opus 4.7 (1M context)` → `Claude Opus 5 (1M context)`. **Preserve the literal prefix `Co-Authored-By: Claude Opus `** — `tests/test_dispatch_banner.py:372,392,405` locates the trailer block by that substring. Leave `commands/optimize-structure.md:78` (`Claude Fable 5`) byte-identical.
- [ ] B.2 `tests/test_mini_run_trailer.py:17` — fixture trailer text, cosmetic consistency only; no assertion depends on the version.
- [ ] B.3 Current-state docs — exactly **6** lines: `CLAUDE.md:9` (v3.44.0 "Current shape" — "on Opus 4.8"), `CLAUDE.md:33` (`service_config` `FALLBACK_MODEL` digest), `docs/CODEBASE_MAP.md:293` (the Model note's "(Opus 4.8 — …)" parenthetical), `docs/CODEBASE_MAP.md:360` (the lever's present-tense "the IMPLEMENTED Opus 4.8 fallback" clause — moves in lockstep with A.4), `docs/INTEGRATION_MAP.md:20` (doubly stale: still says uniform `model: fable` "As of v3.32.0" and predates the v3.43.0 split), `README.md:1729` (the "Built with Claude Code · Opus 4.8" badge).
- [ ] B.4 **Freeze check** — confirm these 6 lines are untouched: `CLAUDE.md:60` (v3.43.0 digest), `docs/CODEBASE_MAP.md:36` (per-release ledger), `README.md:132` (v3.43.0 section), `README.md:251`, `README.md:256`, `README.md:1698` (v3.32.0 section + version cascade). Past releases genuinely shipped on Opus 4.8; rewriting a per-release section falsifies the record. Verify with `git diff -U0` — no hunk may overlap those lines.
- [ ] B.5 Verify: `python -m pytest -q tests/test_dispatch_banner.py tests/test_mini_run_trailer.py tests/test_visual_to_api_command.py tests/test_instruction_compliance.py` green; `python scripts/compliance/instruction_compliance.py` zero findings.
- [ ] B.6 Review evidence (schema v7) + independent `task-reviewer` pass.

## C. Coverage verification (orchestrator + adversarial review, after A and B land)

- [ ] C.1 Post-change sweep `grep -rniE "opus[- ]?4[.-][78]" --include=*.py --include=*.md --include=*.json .` returns **exactly 11** lines, matching `coverage-map.json` `surface_contract.post_change_expected_lines` entry-for-entry: the 6 frozen narrative lines + `scripts/setup/install_gateway.py:311,313` (retained legacy routes) + `tests/test_install_gateway.py:441,445` (the comment + the retained assertion) + `openspec/specs/fable-default-setup-fixes/spec.md:103` (forbidden strings as the quoted subject of a negative assertion). Exclusions: `CHANGELOG.md`, `openspec/changes/archive/**`, `docs/superpowers/**`, `__pycache__`, `.architect-team/`, **and `openspec/changes/opus-5-model-upgrade/**` — the run's own live bundle, which quotes the superseded ids ~34 times by construction (it is the document describing the change) and is self-referential exactly as the archive is.** Omitting that last exclusion makes the sweep return 45, not 11. Expected count corrected 8 → 11 per `surface_contract.contract_amendment`; line numbers shifted 307/309 → 311/313 because A's ordering comment grew by 4 lines.
- [ ] C.2 Negative control: `git diff --stat` shows **zero** files under `agents/`, and `tests/test_agents.py` is unmodified.
- [ ] C.3 Adversarial pass — an independent agent re-derives the 30/6 classification from the files themselves and attacks the current-state-vs-frozen split, using a search modality the implementers did not run.
- [ ] C.4 Full suite: `PYTHONUTF8=1 python -m pytest -q` — no new failures, no new skips vs the 5948/2/5 baseline; the 2 pre-existing Windows byte-count failures reported by name, never "fixed" by editing recorded counts.

## D. Release (orchestrator + `doc-updater`, Phase 8)

- [ ] D.1 Version bump FIRST: `.claude-plugin/plugin.json` + `.claude-plugin/marketplace.json` → `3.45.0` (the version source-of-truth; `doc-updater` reads but never writes these).
- [ ] D.2 `CHANGELOG.md` — new top entry `[3.45.0]` satisfying `docs/CHANGELOG_RUBRIC.md` (version match + a suite-total line). Every pre-existing entry byte-identical.
- [ ] D.3 `doc-updater` — `CLAUDE.md` `## Recent releases` gains the v3.45.0 digest and drops the oldest of the three to hold the 3-entry bound; the map note ledgers + `last_mapped` brought current.
- [ ] D.4 `python scripts/docs_tooling/changelog_check.py` and `python scripts/docs_tooling/capability_index.py --check` pass; independent `system-architect` Documentation Currency Audit returns `overall: pass`.
