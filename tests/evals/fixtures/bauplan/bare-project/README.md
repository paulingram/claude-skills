# Bare fixture

A perfectly ordinary project with NO Bauplan marker. Used to assert the
non-arming case: neither a `bauplan_project.yml` nor stated Bauplan intent, so a
run against it must dispatch zero `bauplan-*` skills.
