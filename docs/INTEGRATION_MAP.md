---
last_synthesized: 2026-06-16T00:00:00Z
codebases: ["architect-team-plugin"]
source_drafts: []
note: "Doc refresh 2026-06-16 for v3.21.0 — Logit / Helpdesk (CT6-6 component 5, HD-1…HD-3): **no new EXTERNAL integration of this plugin** — the new engine `scripts/helpdesk/logit.py` is stdlib-only (`typing` / `argparse` / `json` / `pathlib`); no HTTP / DB / queue / 3rd-party API. The capability PRODUCES a privacy-applied triage-submission payload locally; the actual SEND is the **server-tier**: the triage server (the **SEC** handshake + the **EVAL** server) is designed separately and is NOT part of this in-repo plugin — the transmission step (when a triage endpoint is configured) mirrors the EXISTING best-effort `scripts/notify/notify.py` pattern, by reference not duplicated. The plugin makes no triage-server call itself. Python stays stdlib-only. Prior: Doc refresh 2026-06-16 for v3.20.0 — MCP design agents (CT6-6 component 4, MCP-1…MCP-3): **no new EXTERNAL integration of this plugin** — the new engine `scripts/mcp_design/output_contract.py` is stdlib-only (`typing` / `argparse` / `json` / `pathlib`); no HTTP / DB / queue / 3rd-party API. The capability is provider-agnostic OUTPUT-CONTRACT DESIGN for an LLM agent embedded in the USER's application — the contracts it produces (closed JSON Schema + a forced structured-output tool) are consumed by THAT app, not wired into this plugin; it is the outward-facing counterpart to the internal `verified-agent-output`. Python stays stdlib-only. Prior: Doc refresh 2026-06-16 for v3.19.0 — Claude.md efficiency (CT6-6 component 3, CMD-1…CMD-4): **no new EXTERNAL integration of this plugin** — the new engine `scripts/claude_md/claude_md_efficiency.py` is stdlib-only (`typing` / `argparse` / `json` / `pathlib`); no HTTP / DB / queue / 3rd-party API. The only touchpoint is the OPTIONAL reuse of the EXISTING internal `mempalace-integration` skill (the CMD-1 availability check + the mine/wake-up flow `CLAUDE.md` points INTO) — already-integrated, not a new dependency; pointer-style applies only when MemPalace is installed. Python stays stdlib-only. Prior: Doc refresh 2026-06-16 for v3.18.0 — the closeout capability (CT6-6 component 2, CO-1…CO-3): **no new EXTERNAL integration of this plugin** — the closeout is entirely local: a new `PreCompact` hook (`hooks/precompact-closeout.py`) + a stdlib-only staleness engine (`hooks/closeout_check.py`) that reads the git working tree; no HTTP / DB / queue / 3rd-party API; Python stays stdlib-only. NEW INTERNAL contract surface: the `PreCompact` hook event is now wired in `hooks/hooks.json` (the FIRST `PreCompact` wiring), emitting a non-blocking, fail-open closeout doc-currency reminder before context is compacted; the engine's currency-doc inventory mirrors `documentation-currency` (pinned by a test against drift). Counts 43 skills / 38 agents / 21 commands; suite 4539 → 4574 passing + 5 skipped. Prior: Doc refresh 2026-06-16 for v3.17.0 — the data-dictionary skill (CT6-6 component 1, DD-1…DD-18): **no new EXTERNAL integration of this plugin** — the new engine `scripts/data_dictionary/data_dictionary.py` is stdlib-only (`sqlite3` / `json` / `re` / `argparse`). The only outward touchpoints are OPTIONAL and best-effort, neither a new dependency of this repo: (1) live-DB inspection (DD-9/10) that the skill performs ON A TARGET codebase's database WHEN one is reachable — SQLite via stdlib `sqlite3`; other engines documented to use the agent's own driver + credentials; the no-DB `build_from_inputs(...)` path never fabricates `live-data`; and (2) MemPalace persistence of the finished dictionary (DD-15) via the EXISTING internal `mempalace-integration` skill (already-integrated optional v0.9.4 memory layer, silent no-op when absent). Python stays stdlib-only. Prior: Doc refresh 2026-06-16 for v3.16.0 — responsive + parallel `/architect-team:inject` (MINOR; **no new EXTERNAL integration** — the in-flight inbox + concurrent lanes are local: the new `parallel-problem` classification + lane spawn reuse the existing `hooks/locks.py` file-scope lock + the `hooks/inflight_inbox.py` JSONL inbox; no HTTP / DB / queue / 3rd-party API; Python stays stdlib-only). Prior: Doc refresh 2026-06-16 for v3.15.1 — skill-gate narrowing fix (PATCH; the gate now blocks only build/dispatch tools, never the command wrapper's pre-Skill Bash setup; **no new EXTERNAL integration** — internal hook scope change only; Python stays stdlib-only). Prior: Doc refresh 2026-06-16 for v3.15.0 — skill-invocation hard-gate (MINOR; a new PreToolUse[*] hook `hooks/pretool_skill_gate.py` converting the Layer-6 skill-invocation audit from after-the-fact DETECTION into real-time PREVENTION). **No new EXTERNAL integration in v3.15.0** — the gate reads the local session transcript only (path supplied in the hook payload by the harness); no HTTP / DB / queue / 3rd-party API; Python stays stdlib-only; it REUSES the existing Layer-6 `hooks/skill_invocation_audit.py` detection (`find_skill_requests` + `COMMAND_TO_SKILLS`) and adds no new dependency. The hook is universal — keyed off the plugin's own discovered command set + the Skill-tool ledger, no codebase-specific reference. Prior: Doc refresh 2026-06-11 for v3.13.0 — code-wiki phenotype (MINOR; a fourth seeded phenotype). **No new EXTERNAL runtime integration in v3.13.0.** deepwiki-open (https://github.com/AsyncFuncAI/deepwiki-open.git, MIT) is the ABSORBED PROVENANCE SOURCE only — cloned READ-ONLY into .architect-team/reference/ for the P1-P2 absorption analysis, recorded in phenotypes/code-wiki/phenotype.json provenance.absorbed_from; it is NOT a dependency, has no runtime wiring into the plugin, and the plugin makes no HTTP/DB/queue/API call to it. The phenotype's EMITTED applications are EXTERNAL PRODUCTS the user builds and runs themselves (a standalone Next.js wiki: next/react/react-markdown/remark-gfm/mermaid, served via docker-compose locally or AWS-ECS/GCP-Cloud-Run via the config-management phenotype's service layer); they are not part of the plugin's own integration surface. The cross-seed (components.deploy.via = 'config-management phenotype') is an INTERNAL phenotype-to-phenotype reference (the ai-management precedent), not an external integration. Python stays stdlib-only; the phenotype engine (scripts/phenotypes/phenotypes.py) is unchanged. Prior: Doc refresh 2026-06-11 for v3.12.0 — structure-optimization performance + review remediation (MINOR; in-place). **No new EXTERNAL integration in v3.12.0** — every change is internal skill/agent/command/test correctness + cost optimization + doc; no new HTTP / DB / queue / 3rd-party API, Python stays stdlib-only. **New INTERNAL contract surfaces:** (1) the per-round from-scratch partition recompute is now PUBLISHED by the orchestrator at .architect-team/structure-optimization/<slug>/adversarial/round-<R>/partition-check.json and CONSUMED by the three structure-adversaries (replacing three redundant LLM re-derivations with one deterministic run; the from-scratch-every-round guarantee is preserved); (2) the structure-adversary per-round brief gains warm-start fields — the round delta (changed movement_ids) + the carried modalities_run union + carried clean per-movement evidence — so round N+1 re-runs modalities only on the delta + runs new modalities everywhere + re-confirms (not re-derives) unchanged evidence; the two-consecutive-clean exit + streak-reset-on-revision are unchanged. The movements.json schema v1.0 + architect-verdict.json contract surfaces from v3.11.0 are unchanged. Counts 41 skills / 37 agents / 20 commands UNCHANGED; suite 4356 → 4385 passing + 5 skipped. Prior: Doc refresh 2026-06-10 for v3.11.0 — Structure Optimization Pipeline (MINOR). **No new EXTERNAL integration in v3.11.0** — no new HTTP / DB / queue / 3rd-party API; Python stays stdlib-only. The new `structure-optimization` skill REUSES the already-integrated external plugins per the Uniform-plugin-usage contract: cartographer (via the cartographer-team wrapper, freshness-checked), ralph-loop (three completion-promise loops: STRUCTURE PROPOSAL CONVERGED / RESTRUCTURE PLAN VERIFIED / OPENSPEC AUTHORING COMPLETE), openspec (openspec-propose authoring + the uniform validate --all --strict --json gate), and superpowers (brainstorming / writing-plans / verification-before-completion). **New INTERNAL contract surfaces:** (1) movements.json schema v1.0 at .architect-team/structure-optimization/<slug>/ (movement table + stays + partition_check + batches + adversarial_rounds) — produced by the skill, consumed by the system-architect Restructure Plan Audit mode and by architect-team-pipeline when --execute hands off the produced openspec change; (2) the ninth system-architect audit-mode verdict file architect-verdict.json (partition_check_confirmed / reference_closure_spot_check / migration_order_sound); (3) the 20th slash command optimize-structure joins CANONICAL_COMMANDS via the directory-derived constant (frozen fallback extended to 20). Prior: Single-codebase work; 3-explorer convergence skipped (no peer codebases to integrate with). External-integration profile documented below as the canonical synthesis. Doc refresh 2026-06-10 for v3.10.0 — Second-tier review improvements (R1–R7; MINOR). **No new EXTERNAL integration in v3.10.0** — every change is internal machinery + doc; no new HTTP / DB / queue / 3rd-party API, Python stays stdlib-only. **New INTERNAL contract surfaces:** (1) the `hooks/vao_tools.py` Layer 3 surface is now a FACADE over the NEW `hooks/vao/` package (R2) — `vao_tools.py` re-exports the full 125-name public+test-referenced surface (20 verify_* + 42 UPPER constants + 62 underscore helpers + 1 alias; each `vao_tools.<name> is hooks.vao.<module>.<name>`) and preserves the CLI byte-for-byte (same 20 `verify-*` subcommands, exit codes, verdict-file writes), so EVERY existing consumer (hooks.json command strings / `tests/test_vao_glue_execution.py` / the `## Layer 3 gate invocation table (v3.10.0)` body references) is contract-unchanged; the facade↔package relationship is the only structural change and it is behavior-identical. (2) NEW `heartbeat` event in the `scripts/notify/notify.py` notifier (6th `EVENT_TYPE`, NOT phase-wired) — payload from `hooks/run_metrics.py::heartbeat_snapshot(workspace, run_id)` = `{run-id, phase, elapsed-since-start, qa-cycle-count, agents-dispatched}`; same opt-in / best-effort contract as the other 5 events (offline / no `.architect-team-notify.json` = silent no-op exit 0); fires during any >30-min phase + at every post-first-hour phase boundary; never gates / never caps. (3) NEW SR origin kinds in the canonical catalog (`skills/team-spawning-and-review-gates/SKILL.md`): `security-finding` (produced by the `security-hunter` adversarial shape — routed as an SR, NOT a Layer 3 verdict severity; spawns the owning team directly) + `a11y-gap` (produced by the interaction-completeness accessibility axis; spawns the owning frontend team directly); the catalog is now the OPEN canonical catalog (schema enum uses `integration-test-failure` / `visual-fidelity-drift`; the legacy spellings survive only in the py-core `hooks/shared_rule_constants.py::TEST_FAILURE_ORIGINS` constant + its 2 doc consumers — `SR-sr-catalog-spelling-reconcile-*` routes that reconciliation atomically). NEW `hooks/shared_util.py` (`load_json` / `_utc_now_iso` / unified JSONL reader) is consumed dual-form by `hooks/vao/*` + the inflight/registry/skill-audit/completion-audit hooks (internal helper consolidation; no contract change). Counts: 40 skills / 34 agents / 19 commands UNCHANGED; suite 4097 → 4268 passing + 5 skipped (172 test files). Prior: Doc refresh 2026-06-09 for v3.9.3 — Review-remediation (30 verified-defect fixes; PATCH). The one cross-file CONTRACT change: hooks/hooks.json's 8 hook command strings converted from the `python3 X || python X` polyglot to the v2.16.0 detect-once `$(command -v python3 || command -v python) \"…\"` form (A1) — the harness reads the same hooks.json shape, but the command string is now detect-once so a hook that BLOCKs (exit 2) runs exactly once instead of double-executing, and a `python3`-only host no longer drops the block (exit 127). Two new CLI entry points (A5 teams_mode.py `--banner`, A6 worktree_lifecycle.py `cleanup-merged`) make the existing command-banner / worktree-cleanup invocations actually run. The review-gate evidence schema is now correctly DOCUMENTED as v7 (17 required fields) across all taught locations + the Contracts catalog row below — the CODE was already v7 (`hooks/review_evidence_schema.py::SCHEMA_VERSION = 7`); v3.9.3 fixes the docs that mis-taught it as v6. **No new EXTERNAL integration in v3.9.3** — every change is internal glue correctness + doc reconciliation + regression tests; no new HTTP / DB / queue / 3rd-party API, Python stays stdlib-only. Prior: Doc refresh 2026-06-10 for v3.9.0->v3.9.2 — superpowers is now a HARD dependency: setup.py HARD-blocks (exit 1) on a missing required plugin (REQUIRED_PLUGINS = superpowers / cartographer / ralph-loop) and ensure_openspec_propose_skill() HARD-gates the vendored openspec-propose authoring skill as a 4th prerequisite. Every pipeline body opens with a ## Plugin prerequisites (v3.9.0) superpowers pre-flight abort gate + concrete superpowers:* invocations (brainstorming / test-driven-development / systematic-debugging / verification-before-completion); canonical home common-pipeline-conventions ## Uniform plugin usage (v3.9.0). OpenSpec gate parity: mini + bug-fix + full all run openspec validate --all --strict + openspec archive. v3.9.2 adds a deterministic Stop-hook openspec gate (hooks/pipeline-completion-audit.py::_audit_openspec_validation re-runs openspec validate --all --strict once a master-review verdict exists, blocking the commit on any invalid change). No new EXTERNAL integrations. Prior: Doc refresh 2026-06-09T00:00 for v3.8.0 — Unbounded solving + Code & Data Lineage Graph (CDLG) foundation. **No new EXTERNAL integration in v3.8.0** — the CDLG is an in-process, on-disk graph artifact; the runtime-execution witness it grounds against (`code-path-witness.json`) is ALREADY a plugin artifact (v0.9.31/0.9.32), reused not re-implemented; no new HTTP / DB / queue / 3rd-party API. New INTERNAL integration surfaces (documented in the new `## Code & Data Lineage Graph (CDLG) integration (v3.8.0)` section): (1) CDLG ↔ runtime-witness reconciliation — `hooks/lineage_graph.py::reconcile_with_witness` cross-checks every asserted edge against `code-path-witness.json` for edge recall + hallucination rate + the `witness_gate` trust verdict; (2) CDLG ↔ pipeline diagnosis — `diagnostic-research-team` (P2) consumes the witness-verified CDLG instead of re-tracing, and `bug-fix-pipeline` P0 front-loads the cheap `endpoint-trace-mapping` call-map before deep diagnosis; (3) `hooks/locks.py::cdlg_overlap` (P4) extends the cross-session lock layer from file-scope to call-graph-closure intersection (flags a shared hot callee); (4) `mempalace-integration` (P5) adds function-level lineage records keyed by the rename-stable `func://` ID; (5) `hooks/run_metrics.py` (P0.5) writes per-run §6 metrics + mirrors to MemPalace run-history. Honest boundary: P1 live polyglot call-graph extraction is the agent's runtime job, kill-gated / NOT claimed proven; the deterministic schema/ID/witness/freshness/cost pieces are real + unit-tested. Doc refresh 2026-06-04T16:00 for v2.19.0 — In-flight clarification injection mechanism. New `hooks/inflight_inbox.py` module + 17th Layer 3 tool `verify_inflight_clarifications_processed` + 18th slash command `/architect-team:inject <message>` + canonical section in `common-pipeline-conventions/SKILL.md` + `## Phase-boundary inbox check (v2.19.0)` wiring in all 3 pipeline bodies + canonical fixture + 51 new tests; 3075 → 3127 passing. **No new EXTERNAL integration** — the inbox lives entirely on the per-workspace filesystem (`.architect-team/inbox/<run-id>.jsonl`); no HTTP, no queue, no 3rd-party API; cross-session writes are append-only on local disk. Doc refresh 2026-06-04T13:00 for v2.18.0 — Codebase discipline registry + Phase 0.1 auto-update. New `hooks/discipline_registry.py` module + 16th Layer 3 tool `verify_discipline_registry_current` + 17th slash command `/architect-team:discipline-status` + canonical section + Phase 0.1 wiring in all 3 pipeline bodies + canonical fixture + 47 new tests; 3025 → 3075 passing. **No new EXTERNAL integration** — the registry is a per-codebase JSON file at `.architect-team/discipline-registry.json`; freshness checks are file-system scans of the workspace; auto-update routine invokes existing in-plugin commands (initially the v2.17.0 classifier in mass-classify mode). Doc refresh 2026-06-04T10:00 for v2.17.0 — Prod-safe test classification discipline. NEW 15th Layer 3 tool `verify_test_prod_safety_classification` + NEW skill `test-prod-safety-classifier` + NEW 16th slash command `/architect-team:classify-test-prod-safety` + 4 agent body extensions + canonical fixture + 71 new tests; 2952 → 3024 passing. **No new EXTERNAL integration in v2.17.0** — but introduces the `_PROD_URL_EXCLUSIONS` constant set in `hooks/vao_tools.py` (15 dev/staging substring patterns: localhost / 127.0.0.1 / 0.0.0.0 / file:// / .local / ::1 / host.docker.internal / dev. / staging. / etc.) that is consulted whenever an external HTTP target URL appears in a verification artifact — the v2.4.0 external-system probes (SendGrid Activity API / Stripe Charge API / SES delivery callback / OAuth token resource server / FCM device receipt / Blob HEAD) continue to be the EXTERNAL surface, joined by the new convention that any verification run targeting a non-excluded URL is treated as a production target and filtered to `@prod-safe` tests only. Doc refresh 2026-06-01T18:00 for v2.8.0 — No standing-red discipline (additive extension closing the verbatim heirship B23 case: agent diagnosed cross-layer bug, committed `live-intake-persist.spec.ts` as standing-red with `// will go green when it's fixed`, never routed `cross-layer-backend-required` SR). 10th Layer-3 tool `verify_no_standing_red` in `hooks/vao_tools.py` with 2 named severities (`standing-red-committed` / `cross-layer-fix-not-routed`) + `_STANDING_RED_MARKERS` constant (16 patterns including `// standing red` / `// will go green when fixed` / `test.fixme(` / `it.fixme(` / `test.fail(` / `@pytest.mark.xfail`) + `_CROSS_LAYER_SR_ORIGIN_KINDS` constant (the 2 canonical SR kinds). 4 agent body extensions (`bug-replicator.md` / `qa-replayer.md` / `frontend.md` / `backend.md`) with `## No standing-red discipline (v2.8.0)` sections; `bug-replicator` gains new `needs-cross-layer-fix` verdict; `qa-replayer` gains new `standing_red_finding` field on `bug-still-present` verdict. 1 canonical fixture `tests/fixtures/vao/standing-red-cross-layer-bug.json` reproducing the verbatim B23 case. 52 new tests across `tests/test_vao_no_standing_red.py` (26) + `tests/test_no_standing_red_discipline.py` (26); 2583 → 2635 passing; zero regressions. Schema v7 UNCHANGED. **No new EXTERNAL integration in v2.8.0** — the v2.3.0 / v2.4.0 / v2.5.0 / v2.6.0 / v2.7.0 external-integration profiles carry forward verbatim (no new HTTP / DB / queue / 3rd-party API surface; no Python runtime change; the change is internal verification machinery + 1 tool + 4 agent extensions + 1 fixture). Doc refresh 2026-06-01T16:00 for v2.7.0 — Pattern propagation mandate (extension to v2.6.0 closing the verbatim WtData walkthrough case: agent fixed Workspace.tsx, left IntakeSteps.tsx + ReviewPanel.tsx unmodified, offered the sweep as a follow-up). 6th severity `shared-mock-source-not-swept` added to `verify_live_data_wiring` + `_CROSS_LAYER_SR_ORIGIN_KINDS` discovery; `## Pattern propagation mandate (v2.7.0)` section in `agents/frontend.md` + `### Pattern propagation sweep (v2.7.0)` sub-section in `agents/interaction-reviewer.md`. 1 fixture `shared-mock-source-not-swept.json`. 26 new tests. **No new EXTERNAL integration in v2.7.0**. Doc refresh 2026-06-01T14:00 for v2.6.0 — Live-data wiring discipline (catches mock state surviving when the requirement said remove it). NEW 9th Layer-3 tool `verify_live_data_wiring` with 5 severities (mock-state-residue / live-response-not-rendered / mock-fallback-uncovered / network-not-intercepted / async-status-not-surfaced) + `_MOCK_STATE_SIGNATURES` constant (32 patterns covering MSW / Mirage / faker / fixture / mock-flag / fallback). 2-pass workflow (Playwright + tamper test, then code-side audit). `## Live-data wiring axis (v2.6.0)` extension to `interaction-completeness` skill body + `## Live-data wiring audit (v2.6.0)` extension to `interaction-reviewer` agent body. 1 fixture `live-data-mock-residue.json`. 45 new tests. **No new EXTERNAL integration in v2.6.0**. Doc refresh 2026-06-01T12:00 for v2.5.0 — In-flight clarification discipline (when a pipeline is mid-execution and the user injects a message without `/architect-team`, the orchestrator MUST fold the message into the in-flight pipeline's brief, NOT spawn a sibling workflow). 3 detection signals; 4 forbidden anti-patterns; symmetric counterpart to v2.0.0 Layer 6. **No new EXTERNAL integration in v2.5.0**. Doc refresh 2026-06-01T10:00 for v2.4.0 — External-state assertion + Evidence-artifact citation discipline (orthogonal extension to v2.2.0 closing 2 heirship-app-v3 cases: assertions against internal proxy when feature touches external system; fabricated verification result without on-disk artifact). 7th + 8th severities in `verify_live_verification_claim`. 6 canonical external-system kinds (email / payment / push / webhook-outbound / oauth / blob-storage). 2 fixtures. **External integrations the v2.4.0 discipline NAMES** (does not add — the discipline observes them as the truth source): SendGrid Activity API (events queryable independent of the 202 ack), Stripe Charge.paid field, FCM device onMessage capture, OAuth resource-server GET /me probe, blob-storage HEAD object check. These are documentation surfaces, not new plugin dependencies. Doc refresh 2026-05-30T18:00 for v2.2.0 — Verified-Live Discipline. **One new internal contract surface this run**: the bug-fix-pipeline Phase B6 `qa-replayer` → `verify-live-verification-claim` flow. The qa-replayer's `verification_artifact` block (Playwright trace metadata + test source + screenshot + claimed deployed URL + test state) and `bug_description` block (summary + gesture_pattern + requires_blank_state) become the input to the 7th Layer 3 tool. The tool's verdict (valid:bool + gaps[severity, evidence, remediation]) is the authoritative gate. Phase B6 routing matrix: qa-replayer:bug-resolved + tool:valid=true → accept (proceed to B6b/B7); qa-replayer:bug-resolved-verification-suspect + tool:valid=false → Phase B2 re-replication per suspect mode (gesture-substitution → fix gesture target; self-verification-loop → use independent test; prefill-masking → drive to bug-exposing state); qa-replayer:bug-resolved + tool:valid=false → escalate (qa-replayer's self-audit missed what the tool caught); qa-replayer:bug-resolved-verification-suspect + tool:valid=true → default to qa-replayer's more conservative verdict (re-replicate). Schema v7 OPTIONAL `live_verification_review` field cites the verdict path via dict-shape `{verdict, verdict_path}`. **No new EXTERNAL integration in v2.2.0** — the change is internal verification machinery + 1 tool + 1 qa-replayer extension + 1 optional schema field + Phase B6 wiring. The Playwright dependency for verification claims is ALREADY a plugin dep (used by playwright-user-flows + bug-replicator + qa-replayer + flow-executor + fix-sensibility-checker + v2.1.0's interaction-observer). 76 new tests in `tests/test_vao_live_verification_claim.py` (49) + `tests/test_verified_live_discipline.py` (27); 3 new canonical fixtures (gesture-substitution-corner-click / self-authored-unit-test-loop / prefill-masking-demo-matter) reproducing the heirship-app-v2 transcript verbatim. Tests: 2318 → **2394 passing** (+76 net). The v2.1.0 external-integration profile carries forward verbatim. Doc refresh 2026-05-30T12:00 for v2.1.0 — Interactive-Mockup Discovery. **One new internal contract surface this run**: the `oracle-deriver` → `interaction-observer` dispatch when the named oracle artifact triggers `spec_shape: interactive-mockup` (HTML file/dir carrying `<script>` tags, inline `onclick=`, `addEventListener`, or `[data-action]` attributes). The observer's interactions[] output is folded into the frozen oracle spec; from there `interaction-intuiter`'s new INTENT-INFERENCE mode walks the interactions[] and emits `interaction_intent_gap` entries for any semantic-vs-observed mismatch (canonical 10-pattern matrix lives in the intuiter agent body). Gaps flow through the EXISTING Phase −1D bulk-verify gate alongside per-codebase intuition entries — the user sees ONE unified numbered list and resolves all intent-source ambiguities together; the per-gap `resolved_intent` is written BACK to the corresponding interactions[] entry on the frozen oracle spec; the `_human_review_required` flag flips false. The Layer 3 `verify_interactions_honored` tool in `hooks/vao_tools.py` walks the interactions[] at Phase 3 review-evidence write time, prefers `resolved_intent` over the observed action when present, asserts the built code's handler matches with three named severities (`missing-handler` / `intent-violated` / `action-kind-mismatch`), and writes a deterministic verdict JSON to `<cwd>/.architect-team/vao-verdicts/<task-id>-interactions-honored.json`. Schema v7's new OPTIONAL `interactions_honored_review` field is REQUIRED only when the run's oracle spec carries non-empty interactions[]; n/a otherwise. The field cites the verdict JSON via the dict-shape `{verdict, verdict_path}` (same contract as the other v7 fields). v2.0.0 evidence files (which lack the field entirely) continue to validate. **No new EXTERNAL integration in v2.1.0** — the change is internal observation+verification machinery + 2 agent extensions + 1 skill + 1 tool + 1 optional schema field. The Playwright headless Chrome dependency for the observer's live execution path is ALREADY a plugin dependency (used by `playwright-user-flows` for Phase 5 + by `bug-replicator` / `qa-replayer` / `fix-sensibility-checker` / `flow-executor`); v2.1.0 ships the observer's runtime as DEFERRED (the observer reads pre-captured DOM-interaction snapshots for the v2.1.0 stdlib-only test suite; the runtime sub-script that launches Playwright against an arbitrary user-supplied mockup is v2.1.x). 63 new tests in `tests/test_vao_interactions_honored.py` (38) + `tests/test_interactive_mockup_discovery.py` (23); 1 new fixture at `tests/fixtures/vao/interactive-mockup-logout-misroute.json`. Tests: 2255 → **2318 passing** (+ 1 skipped) across 86 test files. The v2.0.0 external-integration profile carries forward verbatim: the Layer-6 `hooks/skill_invocation_audit.py` Stop-hook auditor remains the framework's foundation gate (parses session transcript + tool-call ledger for explicit Skill-invocation requests; exit 2 on any unmatched request; the 13 user-invocable commands + 6 prose verbs are the surface forms the regex covers). Doc refresh 2026-05-29T03:00 for v1.8.0 — Agent-Resume Discipline. A reliability gap distinct from v2.0.0's verified-agent-output framework: a real-world background `dv-attorney` agent ran 68 tool-calls of real work; the final report message was lost to a harness-level stream timeout (rate-limit cutoff); the orchestrator saw an empty result; the work was on disk the whole time; the user had to manually `redispatch and continue`. v1.8.0 ships the orchestrator-side discipline + helper that automates recovery. **No new external integration in v1.8.0** — the change is an internal helper (`scripts/setup/agent_resume.py`, stdlib-only) + discipline documentation (2 new canonical sections in `common-pipeline-conventions/SKILL.md`, one-paragraph reference in each of the 3 pipeline bodies, uniform `## Checkpoint discipline` section in all 27 `agents/*.md`) + structural-test coverage (42 new tests in `tests/test_agent_resume_discipline.py`). The Agent Teams primitive integration is unchanged from v1.0.0; the helper layers ON TOP of the existing dispatch flow. **New internal contract surface**: the orchestrator MUST route every background Agent dispatch result through `wrap_agent_result()` BEFORE treating the work as complete; the helper takes a dependency-injected `send_message` so it remains harness-independent and testable (the orchestrator binds the harness's real `SendMessage` tool at call time). The checkpoint dir (`.architect-team/agent-checkpoints/`) lives under the v1.1.0 `shared_state_dir()` (the main worktree's `.architect-team/`) so checkpoints are visible across worktrees during the same architect-team run. Tests: 2056 → **2098 passing** (+ 1 skipped) across 80 test files (+1 new file). Backwards-compatible — purely additive; runs that don't hit harness stream timeouts see no behavior change. **Orthogonal to v2.0.0** — the VAO framework on `architect-team/v2.0.0-verified-agent-output` is unaffected; if v2.0.0 is later approved, the v1.8.0 resume helper layers cleanly underneath VAO's Layer 3 tool invocations. All v1.7.0 external integration profile carries forward verbatim (no Gmail SMTP / SendGrid v3 change; MemPalace optional v0.9.4 integration unchanged; Agent Teams primitive requiring `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` + Claude Code ≥ 2.1.32 unchanged). Doc refresh 2026-05-28T08:00 for v1.7.0 — Frontend Missing-API Discipline. A discipline gap orthogonal to v1.6.0's: when a frontend agent encounters a UI element that needs a backend API which does NOT yet exist, the previous discipline didn't tell the agent what to do, and the predictable failure modes were the four downstream defects each existing gate catches AFTER the round trip is wasted (fake the data → caught by `dynamic-value-discovery`; mock the endpoint → caught by `playwright-user-flows`; hardcode the response → caught by `dynamic-value-discovery`; silently stub the UI → caught by `interaction-completeness`). v1.7.0 ships the explicit alternative at four enforcement points: the new `## Frontend missing-API discipline` section in `common-pipeline-conventions/SKILL.md` (canonical home; names the 4 anti-patterns + the right pattern: write SR with `origin.kind: \"missing-api-for-frontend-element\"`, pause that element's work, continue on the rest of the slice, return to wire when the orchestrator re-dispatches with the SR resolved); `## Missing-API discipline` section in `agents/frontend.md` + `## Missing-API SR intake` section in `agents/backend.md` (per-agent statement of the rule on both sides; the frontend section has a complete worked example for a `<UserAvatar>` component needing `GET /api/users/me`; the backend section documents the dispatch-report shape-surfacing responsibility); `agents/system-architect.md` Phase 2 architect brief gains a backend-vs-frontend ordering check for every `both`-layer requirement (decide between sequencing backend-first or authorizing the frontend to surface missing-API SRs — the default); the new `pending-backend` element classification in `skills/interaction-completeness/SKILL.md` (the 5th classification, extending the v0.9.x 4-class system); the new `missing-api-for-frontend-element` SR origin-kind in `skills/team-spawning-and-review-gates/SKILL.md` with documented routing (orchestrator dispatches BACKEND agent FIRST — NOT through `diagnostic-research-team`; on backend completion the orchestrator re-dispatches the FRONTEND to wire up). **No new external integration in v1.7.0** — the change is internal discipline + structural-test coverage; no Python runtime change, no new commands, no new agents, no new HTTP / DB / queue surface. The v1.6.0 external integration profile carries forward verbatim (the v0.9.18 Gmail SMTP + SendGrid v3 email notifications, MemPalace as the optional v0.9.4 memory layer, the v1.0.0 Agent Teams primitive requiring `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` env var + Claude Code ≥ 2.1.32). 26 new tests in `tests/test_frontend_missing_api_discipline.py`; tests 2030 → **2056 passing** (+ 1 skipped) across 79 test files. Backwards-compatible — well-behaved frontend runs already comply; no migration required. The discipline does establish a **new internal contract surface** the orchestrator must honor: an SR with `origin.kind: \"missing-api-for-frontend-element\"` routes to the BACKEND agent first (NOT through `diagnostic-research-team`); on backend completion the FRONTEND is re-dispatched with the SR marked `resolved`. The contract is documented in `skills/team-spawning-and-review-gates/SKILL.md` and audited by the new test file. Doc refresh 2026-05-28T07:00 for v1.6.0 — Teammate Git Discipline. A real-world failure surfaced by the user in a separate session exposed a plugin-level discipline gap: four teammates dispatched in parallel against the same working tree each ran `git stash` to verify their work against baseline; concurrent stash + pop interleaved catastrophically; the reflog showed 10+ consecutive `reset: moving to HEAD` entries; three of four teammates' work was lost. v1.6.0 ships the discipline at 4 enforcement points (matching v1.4.0 scope-discipline's layered pattern): the new `## Teammate git discipline` section in `common-pipeline-conventions/SKILL.md` (canonical home; names the 6 forbidden destructive operations, documents the heirship-app-v2 worked example with the reflog smoking gun, describes the baseline-SHA right pattern); 3 pipeline body anti-pattern entries; all 27 `agents/*.md` files gain a `## Forbidden git operations` section as a uniform 5-line block; the new `## Baseline SHA capture` sub-section in `team-spawning-and-review-gates/SKILL.md` documents orchestrator-side mechanics (SHA persisted to `intake-state.json` as `baseline_sha`, carried in every teammate's spawn brief extending the v0.9.13 manifest schema). No new external integration in v1.6.0 — the change is internal discipline + structural-test coverage; no Python runtime change, no new commands, no new agents. All v1.5.0 external integration profile carries forward verbatim. 265 new tests in `tests/test_teammate_git_discipline.py`; tests 1765 → **2030 passing** (+ 1 skipped) across 78 test files. Backwards-compatible — well-behaved teammates already comply with the discipline; no migration required. Doc refresh 2026-05-28T06:00 for v1.5.0 — Dispatch-Mode Observability. The user's direct question *'how do I know if a team is deployed via agent teams vs subagents, can we show an indicator'* exposed a real observability gap (v1.0.0 made the dispatch decision silent). v1.5.0 ships three observability pieces: (1) new `format_dispatch_banner()` helper in `scripts/setup/teams_mode.py` extending v1.0.0's module — stdlib-only, returns a multi-line box-drawn banner naming AGENT TEAMS or SUBAGENTS (fallback) with the diagnosed Reason: line; (2) all 3 pipeline-driving slash commands print the banner as their FIRST user-visible action (before v1.3.0 auto-cleanup, before argument parsing); (3) new `/architect-team:status` command (13th) reporting dispatch mode + active worktrees + open SRs + last completed run; (4) `Dispatch-Mode: <teams|subagents>` commit-trailer on Phase 8 / B8 / M7 commit-message templates. No new external integration; the change is internal observability + a new command surface. The Agent Teams primitive integration is unchanged from v1.0.0 — the banner just makes the already-recorded decision visible. 20 new tests in `tests/test_dispatch_banner.py`; backwards-compatible. Tests: 1744 → **1765 passing** (+ 1 skipped) across 77 test files. Doc refresh 2026-05-28T05:00 for v1.4.0 — Scope Discipline. A user-reported plugin-level discipline gap: agents using this package were silently NARROWING the user's prompt at intake (the real-world example: user said *'match the oracle'*, agent interpreted as *'enrichment + hardcoded data purge'* and deferred the visual rebuild). The v0.9.36 anti-deferral discipline forbade the MID-RUN version; v1.4.0 extends it to INTAKE. The new canonical `## Scope discipline` section in `common-pipeline-conventions/SKILL.md` is the canonical home of the rule (names the anti-pattern, lists the 6 parity-implying verbs — `match`, `rebuild`, `mirror`, `parity`, `make like`, `replicate` — each implying visual + structural + behavioral parity, classifies scope-narrowing as a DOMAIN gate, requires `AskUserQuestion` surfacing BEFORE starting work, enumerates four explicit forbidden patterns). The 3 pipeline bodies + the `prompt-refiner` agent (6th `scope-fidelity` grading axis), the `proposal-refiner` skill Phase R2 (updated weights / schema), the `bug-classifier` agent (new `## Action-verb interpretation (v1.4.0)` section), and the `system-architect` agent (Master Review Audit step 4 + Phase 2 architect brief `Scope check` field) all reference the canonical section. The change is documentation + structural-test only — no Python runtime change, no new external integration, no opt-in flag. New `tests/test_scope_discipline.py` (35 tests) audits the structural discipline. Tests: 1709 → **1744 passing** (+ 1 skipped) across 76 test files. Backwards-compatible; no migration. Carries forward all v1.3.0 prior context. Doc refresh 2026-05-28T04:00 for v1.3.0 — Auto-Cleanup of Merged Worktrees. The direct follow-up to v1.2.0. v1.2.0 made worktree CREATION automatic but explicitly left CLEANUP as a manual user step (pipeline emitted a recommendation at Phase 8 / B8 / M7 success; user decided when to run `git worktree remove`). v1.3.0 closes the loop with two auto-cleanup trigger points so the user no longer has to remember. **Trigger 1 — start of every `/architect-team` family invocation:** `/architect-team`, `/architect-team:bug-fix`, and `/architect-team:mini` each fire `cleanup_merged_worktrees()` as their FIRST action (before argument parsing, before refinement, before the v1.2.0 auto-worktree creation). A `git fetch origin main` runs first (best-effort), then the helper walks `git worktree list --porcelain`, identifies pairs whose branch starts with `architect-team/`, runs `git merge-base --is-ancestor <branch> origin/main` against each, and removes the merged ones via `git worktree remove` + `git branch -d`. The user sees a brief one-line note listing the paths cleaned. **Trigger 2 — end of mini Phase M7 after green merge:** the mini pipeline auto-cleans its own worktree via `cleanup_run_worktree(Path.cwd(), remove_branch=False)`. The `exclude_current=True` safeguard means the cwd's worktree is NEVER auto-removed by trigger 1 even if its branch is merged. Default behavior — no opt-out flag in v1.3.0 (the cleanup is desirable by design). **The two new helpers** in `scripts/setup/worktree_lifecycle.py`: `list_merged_architect_team_worktrees(against='origin/main', exclude_current=True) -> list[Path]` and `cleanup_merged_worktrees(against='origin/main', dry_run=False) -> list[Path]` — both stdlib only. Non-architect-team branches are NEVER touched regardless of merge state. **Squash-merged branches are NOT auto-detected** (`merge-base --is-ancestor` doesn't catch them — false-negative is safer than false-positive auto-delete). **The new `/architect-team:cleanup-worktrees` command** exposes the same helper for on-demand invocation with `--dry-run` and `--against <ref>` flags (12th command in the plugin). Best-effort discipline: cleanup failures NEVER block the new run. Carries forward all v1.2.0 prior context (auto-worktree creation default; `create_run_worktree` / `cleanup_run_worktree` / `current_worktree_is_run` / `current_run_slug` helpers; collision handling appends `-2`, `-3`, ...) and all v1.1.0 prior context (worktree-aware state resolution; the 3-layer model; the lock layer; MemPalace shared resolution) and v1.0.0 prior context (Agent Teams as default dispatch mode; teams-mode triggers in `hooks.json`) and v0.10.0 prior context (mini-architect-team-pipeline). v1.0.0 still requires `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` (env var OR `~/.claude/settings.json`) AND Claude Code ≥ 2.1.32 for teams mode. The v0.9.18 project-email-notifications integrations remain (Gmail SMTP + SendGrid v3, both stdlib-only, best-effort, opt-in via .architect-team-notify.json). MemPalace remains an optional external integration (v0.9.4)."
---

# Integration Map

> Auto-generated by master-synthesizer (Phase −1C, single-codebase degenerate case).
> Last synthesized: 2026-06-10T17:00 (doc refresh for v3.10.0 — Second-tier review improvements (R1–R7); no new EXTERNAL integration — the `hooks/vao_tools.py`→`hooks/vao/` package split is an internal layout change, `vao_tools.py` stays the byte-identical CLI + 125-name re-export facade, and the NEW `heartbeat` notify event is opt-in/best-effort; see the frontmatter ledger for the full v3.10.0 delta. Prior: v3.9.2 — deterministic openspec Stop-hook gate; v3.9.0 made superpowers a HARD dependency + added the `openspec-propose` 4th hard prerequisite; carries forward v3.8.0 — Unbounded solving + Code & Data Lineage Graph (CDLG) foundation; see the `## Code & Data Lineage Graph (CDLG) integration (v3.8.0)` section below + the frontmatter ledger for the v2.9.0→v3.8.0 deltas. Prior: v2.8.0 2026-06-01T18:00; original synthesis 2026-05-16). Every release v2.3.0 → v3.8.0 carries forward v2.4.0's EXTERNAL integration profile verbatim — no new HTTP / DB / queue / 3rd-party API surface; v3.8.0's additions are internal (the CDLG graph artifact + witness reconciliation against the already-existing `code-path-witness.json` + `cdlg_overlap` + run-metric instrumentation).

## Overview

The architect-team plugin is a single-codebase Claude Code plugin (no peer codebases in scope for this orchestration run). It does NOT have inter-codebase service-to-service calls, shared databases, queues, or contract files. Its "integrations" are entirely external — it composes other Claude plugins, an external CLI (openspec), Python testing tools, Playwright + chromium, and (v0.9.18, opt-in) two email-delivery services — Gmail SMTP and the SendGrid API — engaged by the per-project notifier only when a project supplies `.architect-team-notify.json`.

This INTEGRATION_MAP.md documents those external integrations so future orchestrator runs against the same workspace can short-circuit Phase −1C via the freshness check.

## Per-Pair Integration

The architect-team plugin does not integrate with any peer codebase. It DOES depend on the following external systems, all listed for transparency:

### architect-team ↔ superpowers plugin

- **Protocol:** in-process Skill tool invocation (architect-team doesn't directly invoke superpowers, but expects the user to have superpowers installed; many recommended subagent flows reference superpowers' methodology in their bodies).
- **Endpoint:** `/plugin install superpowers@claude-plugins-official`.
- **Required because (v3.9.0):** superpowers is a **HARD dependency that is actually exercised at runtime**. Every pipeline body (`architect-team-pipeline` / `bug-fix-pipeline` / `mini-architect-team-pipeline` / `ux-test-builder`) opens with a `## Plugin prerequisites (v3.9.0)` **superpowers pre-flight abort gate** and weaves concrete `superpowers:brainstorming` / `test-driven-development` / `systematic-debugging` / `verification-before-completion` invocations through its phases — replacing the prior decorative "Superpowers-driven teammates" framing. Canonical home: `common-pipeline-conventions` `## Uniform plugin usage (v3.9.0)`. Hard-blocking governs plugin PRESENCE only; user `CLAUDE.md` / `AGENTS.md` instructions still win.
- **Failure mode (v3.9.0):** superpowers is a HARD dependency — `setup.py` **HARD-blocks (exit 1)** on its absence (no longer a warning). The vendored `openspec-propose` authoring skill is verified as a **4th HARD-gated prerequisite** via `ensure_openspec_propose_skill()` (a missing vendored `.claude/skills/openspec-propose/SKILL.md` — or an opsx/openspec plugin id — is also exit 1).

### architect-team ↔ cartographer plugin

- **Protocol:** in-process Skill tool invocation (`architect-team:intake-and-mapping` invokes `cartographer:cartographer` as a sub-skill in Phase −1B).
- **Endpoint:** `/plugin install cartographer@cartographer-marketplace`.
- **Required because:** cartographer produces `<codebase>/docs/CODEBASE_MAP.md` which the orchestrator depends on for Phase −1B and many downstream phases (reuse-first design, system-architect agent).
- **Failure mode:** if cartographer absent, Phase −1B cannot produce CODEBASE_MAP.md → orchestrator must surface this as a blocker. `setup.py` reports missing.

### architect-team ↔ ralph-loop plugin

- **Protocol:** slash-command invocation (`architect-team:intake-and-mapping` describes Phase −1B/−1C wrapping in `/ralph-loop "<prompt>" --completion-promise "<exit string>"` — loops until the promise; no iteration cap per v3.8.0 unbounded solving).
- **Endpoint:** `/plugin install ralph-loop@claude-plugins-official`.
- **Required because:** the canonical Phase −1B/−1C iterate-until-converged loop is implemented via ralph-loop.
- **Failure mode:** if ralph-loop absent, the orchestrator must implement the iteration logic natively (current behavior in this dogfood run since `/ralph-loop` is a slash command not directly invokable from inside an orchestrator context — no functional break, just less ceremony).

### architect-team ↔ @fission-ai/openspec CLI

- **Protocol:** subprocess (Bash) invocation of `openspec` binary.
- **Endpoint:** `npm install -g @fission-ai/openspec@latest` (run by `setup.py`).
- **Required because:** Phases 0, 1, 7 all run `openspec` commands (`openspec init`, `openspec instructions <artifact>`, `openspec validate --all --strict --json`, `openspec status`, `openspec show <change>`, `openspec archive`). **v3.9.0 openspec gate parity:** `mini` + `bug-fix` + full all run `openspec validate --all --strict` + `openspec archive` uniformly (`mini` gained both in v3.9.0; `bug-fix` aligned `--strict` → `--all --strict`). **v3.9.2:** the `Stop` hook ALSO independently re-runs `openspec validate --all --strict` once a master-review verdict exists (`hooks/pipeline-completion-audit.py::_audit_openspec_validation`), blocking the commit on any invalid change.
- **OpenSpec authoring skill (v3.9.0 — 4th HARD prerequisite):** the vendored `openspec-propose` skill (the Phase 0 authoring playbook) is verified by `ensure_openspec_propose_skill()`; a missing vendored `.claude/skills/openspec-propose/SKILL.md` (or an opsx/openspec plugin id) is an exit-1 HARD block in `setup.py`.
- **Failure mode:** if openspec absent, no Phase 0 artifact generation can run. `setup.py` installs the CLI and HARD-gates the `openspec-propose` skill.

### architect-team ↔ pytest + pytest-asyncio + httpx

- **Protocol:** subprocess invocation; tests imported as Python modules.
- **Endpoint:** pip-installed via `setup.py` (uses `uv pip install --system` when no venv per the v0.1.1 fix; falls back to plain pip otherwise).
- **Required because:** plugin self-tests use pytest; backend teammates use httpx for dev-API integration tests.
- **Failure mode:** if absent, self-tests cannot run. `setup.py` installs.

### architect-team ↔ Playwright + chromium

- **Protocol:** Playwright Python package + chromium browser binary.
- **Endpoint:** `pip install playwright && playwright install chromium` (run by `setup.py`).
- **Required because:** the `integration` agent, the `frontend` teammate, and the `visual-capture` / `visual-analyzer` agents use Playwright for user-flow tests and live-app rendering against the dev environment.
- **Failure mode:** if Playwright or chromium absent, no Phase 5 frontend integration tests or visual-verification renders can run. `setup.py` checks both Python package import AND chromium cache directory presence (the v0.1.1 fix).

### architect-team ↔ MemPalace (optional, v0.9.4)

- **Protocol:** subprocess (Bash) invocation of the `mempalace` CLI; optionally an MCP server (`mempalace-mcp`) registered in Claude Code.
- **Endpoint:** `uv tool install mempalace` (or `pip install --user mempalace`), run by `/architect-team:mempalace-install` → `scripts/setup/install_mempalace.py`.
- **Required because:** the pipeline auto-mines every artifact (maps, RCAs, diagnostic plans, SRs, coverage maps, final reports) into a per-workspace MemPalace store at `<workspace>/.mempalace/palace`, and named agents search it for prior context before producing output (per `mempalace-integration`).
- **Failure mode:** MemPalace is an ergonomics layer, NOT a hard dependency. If it is not installed, every wake-up / mine / search is skipped with a one-line note and the pipeline proceeds without cross-run memory. Graceful degradation by design.

### architect-team ↔ Gmail SMTP (optional, opt-in notifier, v0.9.18)

- **Protocol:** SMTP over STARTTLS via the Python standard library — `smtplib.SMTP` + `email.message.EmailMessage` + `ssl` (a 15-second bounded socket timeout). No third-party dependency.
- **Endpoint:** `smtp.gmail.com:587`. Engaged only when a target project supplies `.architect-team-notify.json` with `provider: "gmail"`; the notifier `scripts/notify/notify.py` (the `GmailProvider`) authenticates with a Gmail **app password** read from the environment variable named in config (`gmail.app_password_env`).
- **Required because:** the pipeline emits opt-in per-project email notifications at five events (`phase_start`, `phase_complete`, `issue_discovered`, `git_commit`, `deploy`) when a project opts in (per `project-email-notifications` REQ-002/003/005).
- **Failure mode:** strictly best-effort. A missing config, a missing/empty `gmail.app_password_env` variable, an SMTP auth/connection/send error — each is caught, written to stderr as a single warning (never echoing the secret), and the notifier exits 0. A notification failure can never block, fail, or alter a pipeline run; with no `.architect-team-notify.json` the notifier is a silent no-op.

### architect-team ↔ SendGrid v3 mail-send API (optional, opt-in notifier, v0.9.18)

- **Protocol:** HTTPS POST via the Python standard library — `urllib.request` with a JSON body (`personalizations` / `from` / `subject` / `content`) and a `Bearer` Authorization header (a 15-second bounded socket timeout). No third-party dependency (the official `sendgrid` SDK was deliberately not adopted — stdlib-only, per design D3).
- **Endpoint:** `https://api.sendgrid.com/v3/mail/send`. Engaged only when a target project supplies `.architect-team-notify.json` with `provider: "sendgrid"`; the notifier `scripts/notify/notify.py` (the `SendGridProvider`) authenticates with the SendGrid API key read from the environment variable named in config (`sendgrid.api_key_env`).
- **Required because:** the alternate email provider for the opt-in per-project notification feature (per `project-email-notifications` REQ-002).
- **Failure mode:** strictly best-effort. A missing config, a missing/empty `sendgrid.api_key_env` variable, a non-2xx HTTP response, a network error — each is caught, written to stderr as a single warning (status code only, never the response body or the secret), and the notifier exits 0. A notification failure can never block, fail, or alter a pipeline run.

### architect-team ↔ Mailpit (test-scoped email capture, v0.9.34)

- **Protocol:** Docker container (`axllent/mailpit`) exposing SMTP on port 1025 and a REST API on port 8025. When Docker is unavailable, falls back to a direct binary download from `github.com/axllent/mailpit/releases/latest/download/` (Linux amd64, macOS amd64, Windows amd64). The REST API is accessed via `fetch()` inside Playwright test specs (no SDK, no third-party dependency).
- **Endpoint:** `http://localhost:8025/api/v1/search?query=...` for server-side email search (v0.9.35 refinement — replaces the prior `GET /api/v1/messages?limit=10` client-side filter), `GET /api/v1/message/{id}` for full message retrieval, `DELETE /api/v1/messages` for pre-test cleanup. SMTP trap listens on `localhost:1025`.
- **Required because:** the `email-testing` skill (v0.9.34) provides automatic Mailpit-based email flow verification across all QA agents (`bug-replicator`, `flow-executor`, `integration`). When any QA agent detects email-touching work in its slice, it provisions Mailpit, reroutes the dev environment's SMTP through it, triggers the email send via Playwright UI interaction, captures the email via the REST API, extracts and classifies every link by purpose, and follows every link in Playwright to complete the full user flow.
- **Failure mode:** test-scoped and fault-isolated. Mailpit provisioning failure exits as `env-failure` for the email portion of the test — it does NOT block non-email tests in the same slice. Teardown is mandatory (`try/finally`) — a dangling SMTP trap would intercept emails in subsequent test runs. The Docker provisioning includes a pre-flight `docker rm -f` to handle name collisions from prior failed teardowns.

## Pipeline-to-pipeline handoffs

### Mini → Full /architect-team escalation handoff (v0.10.0)

When `/architect-team:mini` hits Phase M8 cycle 4 (three red QA verdicts on the same proposal), it writes an escalation folder at `.architect-team/mini/<slug>/escalation/` containing the original prompt, the latest architect draft, the three `qa-verdict.json` files, the three M3 edit diffs, and an `escalation-context.md`. The mini pipeline then re-spawns `/architect-team` with that folder as REQ_DIR — using the existing pass-folder-as-REQ_DIR semantics, no new flag. The full pipeline resumes from Phase −1 on the same working branch (`mini/<slug>`); the mini pipeline exits.

This handoff IS the structural bridge between the mini and full pipelines. Update both pipelines in lockstep when the REQ_DIR conventions change.

### Cross-session lock layer (v1.0.0)

The user has been asking for cross-session parallelism — *"can I run multiple `/architect-team` invocations at once?"* — since v0.9.x. Given the Agent Teams docs' "one team at a time per Lead" + "no nested teams" constraints, in-session parallelism is impossible; parallelism IS via separate Claude Code sessions. v1.0.0 ships the coordination primitive that makes this safe: **`.architect-team/locks/<scope-hash>.json`**.

**Lock-file shape:**

```jsonc
{
  "holder": "run-<slug>",                   // human-readable run identifier
  "scope_glob": "src/auth/**",              // the file-scope claim
  "acquired_at": "2026-05-28T01:42:00Z",    // ISO 8601 UTC timestamp
  "ttl_seconds": 14400,                      // default 4h (14400s)
  "run_id": "<run-uuid>"                     // unique per /architect-team invocation
}
```

**The four primitives** (all in `hooks/locks.py`, stdlib-only):

| Function | Behavior |
|---|---|
| `acquire_lock(scope_glob, ttl_seconds, run_id)` | Hashes the scope_glob to a filename; if no non-stale lock at that path intersects, writes a new lock file and returns the lock id. If an intersecting non-stale lock exists, returns `blocked: <existing-lock-id>` (the caller's choice whether to queue or surface a conflict). Malformed / missing-field lock files are treated as stale and auto-released. |
| `release_lock(lock_id)` | Removes the lock file. Idempotent — releasing a non-existent lock is a no-op. |
| `detect_stale()` | Returns the lock ids whose `acquired_at + ttl_seconds` is in the past, or whose JSON is malformed / missing required fields. Used by `acquire_lock` internally; can be called standalone for cleanup. |
| `globs_intersect(a, b)` | Returns True when two path-globs could match a common path. Reuses the non-overlapping-file-scope discipline from `team-spawning-and-review-gates`. |

**Use case — two `/architect-team` invocations in separate Claude Code sessions:**

- **Session 1** (Lead A): calls `acquire_lock(scope_glob="src/auth/**", ttl_seconds=14400, run_id="run-A")` → writes the lock at `.architect-team/locks/<hash-of-auth>.json` with `holder: "run-A"`, returns the lock id. Lead A proceeds to dispatch teammates against `src/auth/**`.
- **Session 2** (Lead B, started 10 minutes later): calls `acquire_lock(scope_glob="src/auth/login/**", ...)` → `globs_intersect("src/auth/**", "src/auth/login/**")` returns True → returns `blocked: <Lead-A's-lock-id>`. Lead B's choice: queue (poll until the lock releases or goes stale) or surface a conflict to the user.
- **Disjoint case:** Session 2 calls `acquire_lock(scope_glob="src/billing/**", ...)` → `globs_intersect("src/auth/**", "src/billing/**")` returns False → acquire succeeds, both leads run truly parallel.
- **Crash case:** Lead A crashes; its lock sits at `.architect-team/locks/`. After 4 hours its `acquired_at + ttl_seconds` is in the past; the next intersecting `acquire_lock` invokes `detect_stale()`, removes the stale lock, and succeeds. No human cleanup required.

The lock layer is BEST-EFFORT cross-session coordination, NOT a hard-real-time mutex. If two leads in two sessions both call `acquire_lock` within the same wall-clock millisecond, the filesystem semantics of `os.replace` / atomic write decide the winner; the loser's call detects the winner's lock on its next read and returns `blocked`. The layer guarantees no data corruption and no silent overlap; it does NOT guarantee a strict FIFO acquisition order under perfect concurrency. Acceptable for the user's actual use case (one ad-hoc parallel run per developer per workspace).

This handoff IS the structural primitive that opens cross-session parallelism. Update `hooks/locks.py` in lockstep with the discipline documented in `skills/team-spawning-and-review-gates/SKILL.md`'s non-overlapping-file-scope section.

## Interactive-mockup-discovery flow (v2.1.0)

A new internal contract surface added in v2.1.0. The flow engages when the named oracle artifact at Phase 0.5 is an interactive HTML mockup; otherwise the existing 5 spec_shape categories (component-tree / design-map / api-contract / data-model / hybrid) handle the artifact and v2.1.0 is a no-op.

### Trigger detection (oracle-deriver, Phase 0.5)

`agents/oracle-deriver.md` classifies `spec_shape: interactive-mockup` when ALL of these hold:
- The artifact is an HTML file (or a directory containing `index.html` + assets).
- The artifact carries one or more runtime-interactivity signals: a `<script>` tag, an inline `onclick=` attribute, an `addEventListener` call, OR a `[data-action]` attribute.

When triggered, oracle-deriver dispatches the `interaction-observer` agent BEFORE writing its frozen spec.

### Pass 1 — Observation (interaction-observer)

`agents/interaction-observer.md` (opus, color green) runs in one of two paths:

| Path | When | Mechanism |
|---|---|---|
| Live | Playwright + headless Chrome available | Spin up Playwright, navigate to mockup (single-file HTML served via `python3 -m http.server` on a free port), wait for `document.readyState === 'complete'` + framework ready event, enumerate every interactive element, simulate each interaction with `page.click` / `page.fill` / `page.selectOption` / `page.hover`, capture before/after DOM snapshot, classify the diff into one of the 7 action_kind values |
| Snapshot | Live runtime unavailable (the v2.1.0 plugin self-test path) | Read a pre-captured DOM-interaction-snapshot JSON from the path the orchestrator names in the dispatch brief; iterate through `simulations[]` to produce interactions[] |

Output: a structured `interactions[]` array on the frozen oracle spec. Each entry has shape `{interaction_id, trigger_selector, semantic_label, action_kind, observed_effect, target_url_or_state, evidence_path}` where `action_kind` is the closed 7-value vocabulary `navigate / open-drawer / open-modal / submit / input-text / reveal / no-op`. Per-interaction evidence files (DOM before/after, screenshots, captured fetch URLs) live at `<workspace>/.architect-team/oracle-spec/<change-name>/interaction-evidence/int-NNN.json`.

The observer's Write tool is bounded to the oracle-spec path + the per-interaction evidence files + optionally an observation-log; touching feature source is forbidden.

### Pass 2 — Intent inference (interaction-intuiter, INTENT-INFERENCE mode)

`agents/interaction-intuiter.md` gains a new INTENT-INFERENCE mode that fires when the orchestrator finds the frozen oracle spec carries a non-empty `interactions[]`. The intuiter walks every entry and compares `semantic_label` against `observed_effect` + `target_url_or_state` using the canonical mismatch matrix (10 patterns — Logout / Sign In / Save Draft / Delete / Cancel / Next / Back / Search / Submit / Edit — each mapped to expected intent + mismatch examples). The matrix lives in the intuiter agent body so the rules are auditable from a single source.

For each mismatch, the intuiter emits an `interaction_intent_gap` entry with shape `{gap_id, interaction_id, trigger_selector, semantic_label, observed_action_kind, observed_target, expected_pattern, ambiguity_question, candidate_intents: [...], user_verdict: null, resolved_intent: null}`. Output file: `<workspace>/.architect-team/oracle-spec/<change-name>-intent-gaps.json`.

### Phase −1D bulk-verify integration

The orchestrator folds the intent gaps into the EXISTING Phase −1D bulk-verify gate alongside any per-codebase `INTERACTION_INTUITION_MAP.md` ambiguities. The user sees ONE unified numbered list and resolves all intent-source ambiguities together using the existing `all correct` / `<comma-separated numbers>` / `all incorrect` response format. The drill-down round uses `AskUserQuestion` with each gap's `candidate_intents[]` as options.

After resolution: `user_verdict ∈ {confirmed, corrected, confirmed-stub, deferred}`, `resolved_intent` is set to the canonical `action_kind:target` (e.g., `"navigate:/sign-in"`). The orchestrator writes `resolved_intent` BACK to the corresponding interactions[] entry on the frozen oracle spec at `<workspace>/.architect-team/oracle-spec/<change-name>.json`, then flips `_human_review_required: false`.

### Layer 3 verification (verify_interactions_honored)

At Phase 3 review-evidence write time (when an in-scope task touches a component referenced by an interactions[] entry), the orchestrator invokes `hooks/vao_tools.py::verify_interactions_honored(built_components, oracle_spec)` (or the CLI `python3 hooks/vao_tools.py verify-interactions-honored --components C --oracle O --out OUT`). The tool walks every interactions[] entry, prefers `resolved_intent` over the observed action when present, asserts the built code's handler matches with three named severities:

| Severity | When |
|---|---|
| `missing-handler` | Oracle says this trigger has an effect; built code has no handler at all |
| `intent-violated` | Resolved_intent says X; built code does Y with the same action_kind but a different target (the canonical mockup-lies case) |
| `action-kind-mismatch` | Action_kind itself differs (oracle says open-modal; built code navigates) |

Output verdict JSON at `<cwd>/.architect-team/vao-verdicts/<task-id>-interactions-honored.json` with shape `{tool, matched, gaps, honored_count, total_count, verdict_at}`. Sorted-keys + indent=2 (deterministic / bit-stable — same contract as the other 5 Layer-3 tools).

### Schema v7 citation contract

`hooks/review_evidence_schema.py` schema v7 includes an OPTIONAL `interactions_honored_review` field. Required ONLY when the run's oracle spec carries a non-empty `interactions[]` array; n/a in all other cases. Accepts:

- String shape: `"pass"` / `"n/a"` / `"fail"` (legacy review form).
- Dict shape: `{verdict: "pass", verdict_path: ".architect-team/vao-verdicts/<task-id>-interactions-honored.json"}` (canonical v7 citation form — the cited verdict file is the source of truth at hook level; an agent's prose `verdict: pass` that cites a verdict JSON with non-empty `gaps[]` is hook-blocked).

`REQUIRED_EVIDENCE_FIELDS` stays at 17. The new field lives in the new `OPTIONAL_VAO_FIELDS` tuple. `VALID_INTERACTIONS_HONORED_VALUES = {"pass", "n/a", "fail"}`. v2.0.0 evidence files (which lack the field entirely) continue to validate — the field's optional-ness is the v2.1.0 backwards-compat guarantee.

v3.14.0 adds the THIRD optional field on the same contract: `appearance_scope_review` — required only when the slice's diff touches frontend presentation surface (styling files, components, templates, routes, assets); same string-or-dict shapes; `VALID_APPEARANCE_SCOPE_VALUES = {"pass", "n/a", "fail"}`; the hook BLOCKS `fail` (an appearance-affecting delta with no mandate trace under the `strict` / `propose` appearance modes); `n/a` requires `appearance_scope_review_note`. Pre-v3.14.0 evidence files continue to validate. Canonical rules: `common-pipeline-conventions` `## Appearance-change policy discipline (v3.14.0)`.

### Failure-mode coverage

| Failure shape | Caught at | How |
|---|---|---|
| Source-walk oracle misses behaviors (button click effects, drawer/modal opens, type-able inputs) | Pass 1 observation | `interaction-observer` enumerates + simulates every interactive element |
| Mockup's `Logout` button routes to `/dashboard` | Pass 2 intent inference | Mismatch matrix flags; Phase −1D bulk-verify surfaces; user confirms `/sign-in`; resolved_intent set |
| Built code treats mockup's broken Logout as binding | Layer 3 `verify-interactions-honored` | Tool compares `resolved_intent` against built handler; severity `intent-violated` |
| Built code lacks an interactive element the mockup has | Layer 3 `verify-interactions-honored` + existing `verify-every-element` | Coverage gap → severity `missing-handler` |
| Built code's "drawer" button actually navigates instead | Layer 3 `verify-interactions-honored` | Severity `action-kind-mismatch` |

### Cross-layer impact

This flow does NOT change any external integration:
- Playwright + headless Chrome remain optional (live runtime is deferred to v2.1.x; the v2.1.0 plugin self-test path reads pre-captured snapshots).
- No new HTTP / DB / queue / file-system surface.
- No new env var, no new config file, no new opt-in.

The flow extends the v2.0.0 VAO framework's Layer 1 (oracle-deriver) and Layer 3 (vao_tools) additively — both the new `spec_shape: interactive-mockup` value and the new `verify_interactions_honored` tool are pure additions; the existing 5 spec_shape categories and 5 Layer-3 tools are unchanged.

## Runtime dependencies (v1.1.0)

### Teams-mode dependencies (v1.0.0)

v1.0.0 adds a NEW DEPENDENCY for teams-mode dispatch: **`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`** — set in the shell environment OR as `{"env": {"CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1"}}` in `~/.claude/settings.json` — AND **Claude Code ≥ 2.1.32**. Both are required for teams mode; without either the pipeline silently falls back to subagents mode (the v0.10.0 behavior, unchanged). The mode-detection helper `scripts/setup/teams_mode.py::is_teams_mode_available()` makes the decision once at pipeline startup; the decision is recorded in `intake-state.json` and the pipeline never re-checks. `/architect-team-setup` (`scripts/setup/setup.py`) checks both at install time and offers to add the env var to `~/.claude/settings.json` with explicit user consent. The `--no-teams` flag on any of the three pipeline commands (`/architect-team`, `/architect-team:bug-fix`, `/architect-team:mini`) forces subagents mode even when teams are available.

### Worktree-aware state resolution (v1.1.0)

v1.1.0 introduces a new INTERNAL runtime dependency (not user-facing — no env var, no config, no opt-in): **the lock layer and the MemPalace integration both resolve their root path via `scripts/setup/worktree_paths.py::shared_state_dir()`**, which routes through `git rev-parse --git-common-dir` to identify the MAIN worktree's `.architect-team/` directory. This closes the v1.0.0 gap where two `/architect-team` sessions in two git worktrees of the same repo each got their own isolated locks dir + their own isolated MemPalace, defeating the cross-session coordination intent.

**Resolution semantics** (canonical, applied by every consumer):

| Function | Returns | Used for |
|---|---|---|
| `shared_state_dir() -> Path` | parent(`git rev-parse --git-common-dir`) / `.architect-team` (or cwd / `.architect-team` if git probe fails) | `locks/` (via `hooks/locks.py` default), `.mempalace/palace/` (via `mempalace-integration` skill), `run-history/` |
| `run_state_dir() -> Path` | `Path.cwd() / '.architect-team'` (always per-worktree) | `reviews/`, `teammates/`, `handoffs/`, this-run's `openspec/changes/<slug>/`, this-run's findings + refined-prompts |
| `is_worktree() -> bool` | True iff `git rev-parse --git-dir` != `git rev-parse --git-common-dir` (absolute-path comparison) | downstream callers that want to know |

**The 3-layer model** the helper enables (canonical, applied to cross-session coordination overall):

| Layer | Concern | Mechanism | Resolution |
|---|---|---|---|
| Filesystem isolation | Two sessions can't clobber each other's working tree, index, or branch | Git worktrees (`git worktree add`) | Each session checks out its own worktree on its own branch — use `superpowers:using-git-worktrees` for lifecycle |
| Architectural coordination | Two sessions can't both decide to refactor the same scope-glob | `.architect-team/locks/` JSON locks (v1.0.0) | `shared_state_dir() / 'locks'` — both worktrees see the same lock dir |
| Context sharing | Session B sees what session A produced | MemPalace + CLAUDE.md + codebase maps | `shared_state_dir() / '.mempalace' / 'palace'` — both sessions share one palace |

**Backwards compatibility:** single-session users (no worktrees) see ZERO behavior change. `shared_state_dir()` and `run_state_dir()` degenerate to the same path in a non-worktree clone. The lock layer reads/writes the same location it did in v1.0.0. No env var, no flag, no opt-in. The lock layer's explicit `locks_dir=` parameter (used by `tests/test_locks.py` for test isolation) is preserved verbatim — all 17 v1.0.0 lock tests pass unchanged after the v1.1.0 default-resolution change.

**Failure modes:** every git subprocess invocation in `worktree_paths.py` is wrapped to fall back to `Path.cwd() / '.architect-team'` on any failure (not a git repo, git missing, broken metadata, empty stdout). `hooks/locks.py`'s `_default_locks_dir()` further wraps the helper import in a try/except — if `worktree_paths.py` is unreachable for any reason, the lock layer falls back to the legacy `DEFAULT_LOCKS_DIR` constant, so the public lock API never raises. The cross-worktree coordination guarantee degrades in these edge cases (each worktree's lock layer becomes isolated again), but no run is interrupted.

**Out of scope for v1.1.0:** cross-CLONE coordination (two separate clones of the same project on disk do NOT coordinate via this layer — the lock files + MemPalace are repo-local, not machine-wide); worktree lifecycle automation from inside the pipeline (the `superpowers:using-git-worktrees` skill already handles this).

### Auto-worktree lifecycle (v1.2.0)

v1.2.0 brings worktree-lifecycle automation IN-scope for the pipeline. v1.1.0 made state-resolution worktree-aware but left worktree CREATION as a manual user step (the user ran `git worktree add` before invoking `/architect-team`). v1.2.0 closes that gap: every invocation of the three pipeline-driving slash commands — `/architect-team`, `/architect-team:bug-fix`, `/architect-team:mini` — automatically creates a fresh worktree at `<parent-of-repo>/<repo-name>-<slug>/` on branch `architect-team/<slug>` BEFORE invoking the pipeline skill. The user's main checkout stays on whatever branch they were on; the run is filesystem-isolated; concurrent runs work without any manual setup. The 7 utility commands (`/architect-team:visual-qa`, `/architect-team:editability-audit`, `/architect-team:refine-prompt`, `/architect-team:memory`, `/architect-team:mempalace-install`, `/architect-team-setup`, `/architect-team:mini-review-sweep`) do NOT get the step — they operate against the current checkout because their work is inspection / configuration / replay, not feature delivery.

**Helper:** `scripts/setup/worktree_lifecycle.py` — sibling to v1.1.0's `worktree_paths.py`. Stdlib-only. Four public functions:

| Function | Purpose | Failure mode |
|---|---|---|
| `create_run_worktree(slug, base_branch="main", parent_dir=None) -> Path` | `git worktree add -b architect-team/<slug> <parent>/<repo>-<slug>/ <base>`. Collision handling appends `-2`, `-3`, ... when path or branch is taken (bounded at 999 attempts). | Raises `RuntimeError` with an actionable message on parent-dir-not-writable, base-branch-missing, `git worktree add` failure, or slug exhaustion. The slash command surfaces the error verbatim and stops — no silent fallback. |
| `cleanup_run_worktree(worktree_path, remove_branch=False) -> None` | `git worktree remove <path>` + optional `git branch -d architect-team/<slug>`. | Idempotent on already-gone worktree (catches `is not a working tree` / `is not a registered` / `no such file or directory`). Falls back to `--force` removal once before raising. `not fully merged` branch deletion raises with a manual-cleanup hint. |
| `current_worktree_is_run() -> bool` | True iff `git rev-parse --abbrev-ref HEAD` starts with `architect-team/`. | Returns False on any subprocess failure (not in a git repo, git missing, detached HEAD). Never raises. |
| `current_run_slug() -> str | None` | Extracts `<slug>` from `architect-team/<slug>`. | Returns None on non-run branches, detached HEAD, or subprocess failure. |

**Slash-command integration:** each pipeline-driving slash command body has a `## Auto-worktree creation (v1.2.0)` section between argument parsing (+ refinement) and skill invocation. The step is skipped when ANY of these holds: `--no-worktree` flag (or natural-language opt-out — *"no worktree"*, *"don't create a worktree"*, *"single tree"*, *"in place"*, *"in current tree"*); OR re-entry from inside an existing `architect-team/*` worktree (detected via `current_worktree_is_run()`). Otherwise the step derives a slug, invokes the helper via the polyglot `python3 -c '...' || python -c '...'` pattern, chdirs into the new worktree, and emits a one-line note to the user before invoking the pipeline skill.

**Cleanup semantics:** NOT automatic. The pipeline emits a recommendation at Phase 8 / B8 / M7 success — *"Your run worktree is at `<path>` on branch `architect-team/<slug>`. To clean up: `git worktree remove <path> && git branch -d architect-team/<slug>`."* — and the user decides when to clean up. The user may want to inspect the run, layer a follow-up via re-entry detection, or run manual tests against the run-branch tip.

**Backwards compatibility:** v1.1.0 single-tree users pass `--no-worktree` on every invocation for verbatim v1.1.0 behavior. The auto-worktree step adds ~1s of pipeline startup time (the `git worktree add` subprocess); negligible for a feature-shipping pipeline. Re-entry detection means existing in-flight runs already on `architect-team/<slug>` branches are picked up correctly — re-invoking `/architect-team` from inside one of those does NOT create a nested worktree.

**Out of scope for v1.2.0:** auto-cleanup after Phase 8 merge (pipeline reports + recommends; user decides — future v1.3+ could add `--auto-cleanup`); multi-worktree dispatch (each pipeline run gets ONE worktree; teammates dispatch INTO that worktree, not into per-teammate worktrees — v1.3+ idea); auto-worktree for the 7 utility commands (they're inspection / configuration / replay, not feature delivery — limit auto-worktree to the 3 pipeline-driving commands).

## Code & Data Lineage Graph (CDLG) integration (v3.8.0)

The v3.8.0 lineage foundation adds INTERNAL integration surfaces — no new external dependency, no new HTTP / DB / queue / 3rd-party API. The CDLG is an in-process, on-disk artifact (a graph JSON) produced by an agent and consumed by the pipeline's own diagnosis + parallel-dispatch machinery. The Playwright / runtime-execution dependency it grounds against (`code-path-witness.json`) is ALREADY a plugin artifact (v0.9.31 / v0.9.32) — v3.8.0 reuses it, never re-implements execution capture.

### CDLG ↔ runtime-witness reconciliation (the trust gate)

The load-bearing contract. `hooks/lineage_graph.py::reconcile_with_witness(graph, witness)` cross-checks every asserted CDLG edge against the existing **`code-path-witness.json`** runtime-execution witness:

- **Producer:** the `endpoint-tracer` agent (via the `endpoint-trace-mapping` skill) emits a candidate CDLG from live polyglot extraction — a graph of `func://` / `endpoint://` / `asset://` nodes and `calls` / `reads` / `writes` / `modifies` / `serves` / `originates` / `serves_route` edges.
- **Ground truth:** `code-path-witness.json` (the v0.9.31 / v0.9.32 wrong-code-path execution witness — already captured during Playwright / backend test runs).
- **Reconciliation output:** edge **recall** (witnessed edges present in the CDLG) + **hallucination rate** (CDLG edges asserting execution that the witness never observed) + the `witness_gate` verdict. An edge whose execution is unwitnessed is downgraded, not trusted — so the CDLG that downstream phases consume is witness-verified, not LLM-asserted-only.
- **Honest boundary:** the deterministic reconciliation + recall/hallucination math is real and unit-tested (`tests/test_lineage_graph.py`); the live polyglot extraction that *produces* the candidate graph against an arbitrary target repo is the agent's runtime job and is **kill-gated / NOT claimed proven** (per `docs/LINEAGE_UPGRADE_REQUIREMENTS.md` §7.1).

### CDLG ↔ pipeline diagnosis (P2 consumer rewire)

`diagnostic-research-team` (the Phase-2 pre-fix diagnostic stage) now **consumes the witness-verified CDLG** instead of re-tracing the code path from scratch. The CDLG is the call-graph the three `diagnostic-researcher` agents reason over; the `bug-fix-pipeline` P0 isolation reorder (replicate → scope-isolate → EXECUTED light FE/API discriminant → call-map → diagnose) front-loads the cheap evidence-backed `endpoint-trace-mapping` call-map BEFORE the expensive deep diagnosis. The `fe_api_verdict` discriminant + the `layer_fixed` reality are captured by `hooks/run_metrics.py` so wrong-layer fixes are measurable.

### CDLG ↔ parallel-execution locks (`cdlg_overlap`, P4)

`hooks/locks.py::cdlg_overlap(...)` extends the v1.0.0 cross-session lock layer from FILE-scope intersection (`globs_intersect`) to **call-graph-closure intersection**. Two slices whose edited file sets are disjoint can still collide if both call-closures reach a shared HOT CALLEE (a function reachable from both); `cdlg_overlap` flags that case so the Lead can serialize the colliding work rather than dispatch it truly-parallel. The lock layer's existing `acquire_lock` / `release_lock` / `detect_stale` semantics are unchanged; `cdlg_overlap` is an additional, graph-aware overlap predicate consulted at dispatch time.

### CDLG ↔ MemPalace function-level records (P5)

`mempalace-integration` adds **function-level lineage records keyed by `func://`** — the rename-stable content-fingerprint ID nomenclature from `hooks/lineage_graph.py` is the join key that lets MemPalace dedup + diff lineage facts across runs even when a function is renamed or moved. No new external integration: MemPalace remains the optional v0.9.4 local-first store; v3.8.0 just adds a new record kind keyed by the new ID scheme.

### Run-metric instrumentation (P0.5)

`hooks/run_metrics.py` writes a per-run metrics JSON at `<workspace>/.architect-team/run-metrics/<run_id>.json` (the §6 success metrics: `dev_loop_iterations`, first-pass-fix, oscillation / bug-still-present / fix-regression counts, the FE/API discriminant verdict vs the layer actually fixed, CDLG recall / hallucination) and mirrors it to MemPalace run-history. It is the measurement surface for the frozen-bug-benchmark protocol — purely internal, on-disk, no external call.

## Contracts & Schemas Catalog

The architect-team plugin defines the following contract-like schemas across its files:

| Schema | Defined in | Consumed by |
|---|---|---|
| `plugin.json` | `.claude-plugin/plugin.json` | Claude Code harness (plugin loader); `setup.py` reads version |
| `marketplace.json` | `.claude-plugin/marketplace.json` | Claude Code harness (marketplace registry) |
| `hooks.json` | `hooks/hooks.json` | Claude Code harness (event registration) |
| Review-gate evidence schema (v7 — 17 teammate self-review fields incl. `ui_interaction_review` + the 5 Verified Agent Output fields + the `independent_review` block; 3 OPTIONAL VAO fields) | `team-spawning-and-review-gates/SKILL.md` (definition); `hooks/review_evidence_schema.py` (the single shared source of truth — `SCHEMA_VERSION` = 7, `REQUIRED_EVIDENCE_FIELDS` (17) + `REQUIRED_INDEPENDENT_REVIEW_FIELDS` + `OPTIONAL_VAO_FIELDS` + the `VALID_*` value sets + `validate_evidence()`) | teammates write the self-review; the independent `task-reviewer` agent writes the `independent_review` block; `review-gate-task.py` + `teammate-idle-check.py` both import the shared module and reject evidence whose `independent_review.reviewer == teammate` (v0.9.13), block a `*_review` field set to `fail`, and require the `_note` on an `n/a` |
| Teammate manifest schema | `team-spawning-and-review-gates/SKILL.md` (definition); `hooks/teammate-idle-check.py` (read); `hooks/review-gate-task.py:_is_teammate_task` (read since v0.2.2) | orchestrator (write at spawn); both evidence hooks (read) |
| Solution Requirement (SR) schema | `team-spawning-and-review-gates/SKILL.md` `## Solution Requirements` | every reviewer/agent that surfaces an issue (write); orchestrator Phase 3b (read → auto-spawn fix teams); `pipeline-completion-audit.py` Stop hook (read → terminal-state audit) |
| test-completeness verdict / editability converged map / visual-verification verdict / master-review audit verdict | `test-completeness-verifier`, `editability-completeness` skill, `visual-verification-team` skill, `system-architect` Master Review Audit mode | the respective verifier roles (write); orchestrator + `pipeline-completion-audit.py` Stop hook (read → gate the run on a passing verdict) |
| `coverage-map.json` schema | `coverage-mapping/SKILL.md` | orchestrator (write throughout); coverage-mapping skill (validates) |
| `intake-state.json` schema | `intake-and-mapping/SKILL.md` | orchestrator (write at end of Phase −1; read at start of next run) |
| `.architect-team-notify.json` schema (v0.9.18) | `scripts/notify/notify.py` (`validate_config`); `.architect-team-notify.example.json` (template); README (documented schema) | a target project (writes it to opt in); `scripts/notify/notify.py` (reads + validates at notify time) |
| ROUTE_MAP.md schema | `frontend-route-mapping/SKILL.md` | route-mapper agent (write); codebase-map-reviewer / playwright-user-flows / integration agent (read) |
| Agent frontmatter (5 keys) | `tests/test_agents.py` `REQUIRED_KEYS` + `VALID_MODELS` + `VALID_TOOLS` | each agent file (must conform); test enforces structural validity |
| Skill frontmatter (2 keys) | `tests/test_skills.py` `REQUIRED_FRONTMATTER_KEYS` | each skill file |
| Command frontmatter (1 key) | `tests/test_commands.py` `REQUIRED_KEYS` | each command file |

## Deployment Topology

The architect-team plugin deploys as a Claude Code plugin via:

1. User runs `/plugin marketplace add https://github.com/paulingram/claude-skills.git`. The harness git-clones to `~/.claude/plugins/marketplaces/architect-team-marketplace/`.
2. User runs `/plugin install architect-team@architect-team-marketplace`. The harness copies the plugin into `~/.claude/plugins/cache/architect-team-marketplace/architect-team/<version>/` and registers it in `~/.claude/plugins/installed_plugins.json`.
3. User runs `/reload-plugins`. The harness reads `.claude-plugin/plugin.json`, `hooks/hooks.json`, every `skills/<name>/SKILL.md`, every `agents/<name>.md`, and every `commands/<name>.md` and registers them as namespaced (`architect-team:*`).
4. User runs `/architect-team:architect-team-setup` once to install external deps (openspec / pytest / playwright / chromium).
5. User invokes `/architect-team:architect-team <path>` to run the pipeline.

There is no server-side deployment. All execution is local in the Claude Code session.

## Failure Modes (cross-system)

- **Missing prerequisite plugin (superpowers / cartographer / ralph-loop)**: setup script reports `[missing]` with the install command. Orchestrator skill bodies reference these by name and would fail at first invocation if absent.
- **Stale marketplace clone**: pre-v0.2.2 manual `git pull` was needed because `/plugin marketplace add` did not re-fetch on remove+re-add. Post-v0.2.2 confirmed: `/plugin marketplace update <name>` does fetch and reports "1 plugin bumped" when a new commit is on origin.
- **Hook over-aggressive**: pre-v0.2.2 the review-gate hook blocked every TaskUpdate. v0.2.2 scoped it to teammate tasks only.
- **`uv pip install` without venv**: pre-v0.1.1, setup script failed to install Playwright on machines with `uv` on PATH. v0.1.1 detects venv and adds `--system` when none active.
- **Skill name collision with command**: pre-v0.2.0, the skill named `architect-team` collided with the slash command of the same name; Skill tool resolved to the command body. v0.2.0 renamed skill to `architect-team-pipeline`.
- **`disable-model-invocation: true` on a skill the command needs to invoke**: pre-v0.2.1, the orchestrator skill had this flag set, blocking the Skill tool. v0.2.1 removed it.

## Architectural notes & inherent limits

The v0.2.3-era open questions (REQ-001 `$ARGUMENTS` propagation, REQ-002 path-traversal sanitization, REQ-003 test-coverage gaps, REQ-004 hook-rejection escalation policy, REQ-005 spec drift) are all **resolved** — see `CHANGELOG.md`. The v0.9.9 logic-implementation review surfaced and closed three tiers of holes. What remains are the inherent limits of an LLM-agent architecture — recorded here honestly rather than papered over:

- **The orchestrator cannot be hooked mid-run.** It is the main session; no hook gates its mid-run behaviour. Every phase discipline is trust-based Markdown. The mitigations are real but bounded: the `Stop` hook (`pipeline-completion-audit.py`) gates the orchestrator's *terminal* state, and the Phase 8 `--check` gate makes "clean pass" a checked fact — but neither can enforce "did you run Phase 5's reviews."
- **The automated test suite is structural, not behavioural.** The 647 pytest tests prove the plugin's Markdown / JSON / hook scripts are well-formed and that components do not drift apart (`test_cross_consistency.py`) — and, for the notifier, that its config/provider/dispatch/CLI logic behaves correctly with the genuinely-external transports (`smtplib.SMTP`, `urllib.request.urlopen`) mocked. They do NOT prove the live multi-agent pipeline produces correct software — behavioural / integration testing of the orchestration is outside an automated suite by nature.
- **Runs are non-deterministic.** Re-running the pipeline on the same input yields different teammates, classifications, and convergence. The evidence-artifact trail + the Stop-hook audit make outcomes *auditable*, not *reproducible*.
- **Single-codebase 3-explorer convergence is degenerate** but the skill still mandates running it (it must produce the INTEGRATION_MAP.md that `reuse-first-design` consults). Accepted as-is.
