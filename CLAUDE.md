# CLAUDE TEAM SIX (CT6)

> Internal plugin slug: `architect-team` (preserved for backward compatibility with existing installations + the 23 slash commands). CLAUDE TEAM SIX is the user-facing display name introduced in v2.2.0.

## What this repo is

This repo IS the source of the **CLAUDE TEAM SIX** Claude Code plugin (internal slug `architect-team`) — a spec-to-production multi-agent coding pipeline. It orchestrates **MemPalace wake-up → Phase −2 (Triage) → Phase −1 → 8** against a requirements folder, with sibling pipelines for **bug-fix** (`/architect-team:bug-fix`), **UX testing** (`/architect-team:ux-test`), and **mini** (`/architect-team:mini`).

Current shape (v3.41.1): **48 skills** (incl. the phenotype subsystem with 4 seeded phenotypes — `user-management`, `config-management`, `ai-management`, `code-wiki`), **39 named agents**, **23 slash commands**, the `services/` server tier (the `librarian` + `triage` + `session-review` + `seeded-mempalace` services + the REPO-1…4 separation manifest; the Librarian is installable via `/architect-team:librarian-install`), the hook-enforced discipline stack (evidence-schema-v7 review gates, run continuity, the instruction-compliance lint wired as an enforced suite gate, the real-time skill-invocation hard-gate), the external-LLM gateway with a selectable secondary provider (OpenAI Codex or Z.ai GLM 5.2), and **5689 pytest self-tests + 4 skipped** across 202 test files (green under both Windows cp1252 and `PYTHONUTF8=1`).

The default dispatch mode is Claude Code's experimental **Agent Teams** primitive (long-lived named teammates with their own 1M context windows, a shared task list, direct `SendMessage`, and a Lead that owns coordination); it requires `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` and Claude Code ≥ 2.1.32, and silently falls back to subagents mode otherwise. `--no-teams` forces subagents mode.

## Stack

Markdown (skills/agents/commands), JSON (plugin/marketplace/hooks metadata), Python 3.10+ — **stdlib-only**. The Python surface:

- `scripts/setup/` — `setup.py` (the one-call setup entry: cartographer marketplace provenance `kingbootoshi/cartographer`, the npm EACCES `--prefix ~/.local` retry, the PEP-668 `uv`→`pip --user`→`--break-system-packages` install ladder + `tiktoken`, `--yes`/`CT6_SETUP_ASSUME_YES` non-interactive consent, `--codex|--no-codex`, `--external-llm`/`--no-external-llm`/`CT6_EXTERNAL_LLM`, `--secondary <openai|zai>`/`CT6_SECONDARY_PROVIDER` with provider names derived from the lever's `SECONDARY_PROVIDERS` registry; on an interactive real-TTY run — NOT `--yes`/`CT6_SETUP_ASSUME_YES`, NOT `--check-only`, NOT `--no-prompt` — the gateway installer may prompt for missing keys; installer failures degrade to warn rows, never gate setup) + `set_default_model.py` (the agent-model lever. Uniform: `--model fable|opus|sonnet|haiku` / `--check` — the IMPLEMENTED Opus fallback for a harness that predates the `fable` alias. Role split: `AGENT_ROLES` classifies the 39 agents — 18 architecture/control/design ⇒ `fable`, 21 development/code-checking/testing ⇒ the secondary alias — via `--split secondary` (canonical) / the tri-state `--auto` on `CT6_CODEX_56_AVAILABLE` (truthy=split, set-falsy=fable, absent=no-op) / policy-state `--check` / `--secondary-model` (superseding the deprecated `--codex-model`); unclassified stems fail safe to fable; the uniform lever refuses the split aliases. The `SECONDARY_PROVIDERS` registry (per-entry model/key_env/route_dialect/api_base/label — `openai` → `gpt-5.6-sol` on dialect `openai`, `zai` → `glm-5.2` on dialect `hosted_vllm` via `https://api.z.ai/api/paas/v4`, the gateway route DERIVED `<route_dialect>/<model>`; one dict entry per future provider, extensibility test-pinned) single-sources the provider set; the split writes the harness-spawnable spawn alias `SPAWN_ALIAS_MODEL_ID` `claude-haiku-4-5` (test-pinned; gateway-impersonated to the chosen secondary, disclosed) under the `secondary-split` policy (exported constants `POLICY_SECONDARY_SPLIT`/`POLICY_UNIFORM_FABLE`/`LEGACY_POLICY_CODEX_SPLIT`; readers recognize all three alias generations and `migrate_legacy_split` moves the superseded `codex-5.6-sol` + spawn-dead raw `ct6-secondary` frontmatter values to the spawn alias; the legacy `codex-5.6-sol`/`codex-split` still read, never written). `installed_plugin_agents_dir()`/`runtime_agents_dir()` resolve the INSTALLED plugin copy's `agents/` from Claude Code's `installed_plugins.json` (`$CT6_PLUGIN_REGISTRY` override, repo fallback) so install-time model policy lands where the runtime actually reads it) + `install_mempalace.py` + `install_librarian.py` (the stdlib-only Librarian installer + CLI behind `/architect-team:librarian-install`; hidden-getpass key prompting on a real interactive TTY, the per-key `key-declines.json` decline record with auto-reset, the `decline [--clear]` subcommand, `--interactive-prompts`/`--re-ask-keys`, status/purge honesty) + `install_gateway.py` (the stdlib-only external-LLM gateway installer behind `setup.py --external-llm`: LiteLLM via the PEP-668 ladder, routing the secondary alias + the disclosed `claude-haiku-4-5` spawn alias to the chosen provider via the registry-derived `<route_dialect>/<model>` route behind an always-on generated master key. TWO key-presence-resolved auth modes — api-key = full gateway + consent-gated `--activate` writing `ANTHROPIC_BASE_URL` into settings.json + the role split, with the spawn-alias impersonation route + explicit per-id Anthropic routes emitted ahead of the catch-all (no real-Anthropic route for the spawn id; the impersonation route rides BOTH auth modes); subscription = NO Anthropic key, fable keeps Claude sign-in auth, the split stays OFF, and the gateway serves the chosen provider's model to direct callers. Raw keys live ONLY in `~/.architect-team/gateway/gateway.env` (0600), never the repo. Auto-registration EXECUTES the user-level boot registration + start (`schtasks onlogon` / `systemctl --user` / LaunchAgents — never sudo; `--no-register` opt-out; uninstall stops + unregisters symmetrically, `--purge` for state). Ask-then-apply key prompting: the hidden-getpass `_prompt_for_key` seam (interactive TTY only, blank-to-skip, interrupt→skip), the per-key `key-declines.json` decline record (auto-reset on key resolution), the `decline <anthropic|openai|zai> [--clear]` subcommand, `--interactive-prompts`/`--re-ask-keys`, status `declined=` reporting. The secondary provider is selectable — `--secondary <openai|zai>` > `CT6_SECONDARY_PROVIDER` > the `gateway.json`-recorded choice > the openai grandfather for pre-registry installs (an `openai_model`-only state, no popup ever) > an interactive-TTY prompt — REMEMBERED in `gateway.json`, re-opened via `--re-ask-provider`; `--zai-key` with full decline parity; legacy migration on every config-regenerating install with prior-state carry-forward; a provider switch never deletes the other provider's stored key. A registered install keeps exactly ONE owned gateway instance (owned = the process TREE whose cmdline carries this state dir’s config path; the accumulation sweep never touches the port-holder’s tree), judges staleness against the SERVED `/model/info` state by two-armed route-contradiction (wildcard expansions are routed, not stale), deploys never-dark verify-then-swap (a STAGING instance passes the full ladder before any live stop; the generated launchers refuse a busy port + log masked key presence), and ends in a LIVE spawn-alias-MANDATORY `/v1/messages` completion confirmation via `confirm_gateway_serving` — the serving deployment verified against the registry-derived route, plus an auth-enforcement rung (an invalid key must be DENIED; the prisma no-DB crash-500 warns, only a 200 fails) — "CONFIRMED live — CT6 runs the split" — with one automatic bind-verified `restart_gateway` repair cycle (the port-holder’s tree stopped when the tracked stop leaves the port held); `status --live` probes the STATE-RECORDED aliases without repairing; `--check-only`/`--no-register` never probe; never gates setup) + `teams_mode.py` (dispatch detector + `format_dispatch_banner()`) + `worktree_paths.py` (worktree-aware state resolution) + `worktree_lifecycle.py` (worktree create/cleanup/merge) + `agent_resume.py` (resume + checkpoint) + `sync_agent_boilerplate.py` (canonical agent-boilerplate re-sync).
- `hooks/` — JSON wiring + 7 enforcement scripts: `review-gate-task.py` (PostToolUse(TaskUpdate) + TaskCompleted) / `teammate-idle-check.py` (SubagentStop + TeammateIdle) / `pipeline-completion-audit.py` (Stop; incl. the continuation guard — active-run lifecycle check + bounded no-progress persistence for engaged sessions + auto-escalation) / `pretool_unilateral_override_guard.py` (PreToolUse(Edit/Write/NotebookEdit)) / `pretool_skill_gate.py` (PreToolUse[*] — the real-time skill-invocation hard-gate + the sticky run arm: an ACTIVE `active-run.json` blocks build/dispatch tools until the pipeline Skill is re-engaged, with deterministic marker engagement recorded at the PostToolUse(Skill) wiring so a denied call never engages; both arms share one `_is_worker_session` standdown — a pipeline teammate is never gated — and the genuine-prompt anchor excludes `<teammate-message>` peer envelopes so an inbound peer message can't re-arm the gate mid-run) / `precompact-closeout.py` (PreCompact — the closeout doc-currency reminder) / `sessionstart-run-continuity.py` (SessionStart — the resume directive on startup/resume/clear/compact; + the model-split self-heal `maybe_heal_model_split()`, which re-applies the split to an INSTALLED plugin copy that drifted after a plugin update, restoring the alias the gateway state records as served (`spawn_alias` > `secondary_alias` > legacy `codex_alias`) — never a dev checkout, fail-open). Shared modules: `review_evidence_schema.py` (evidence schema **v7**: 17 required fields incl. 5 VAO fields + 3 optional fields `interactions_honored_review` / `live_verification_review` / `appearance_scope_review`) + `locks.py` (the cross-session lock layer; ATOMIC publish via a per-acquirer `.inflight.tmp` + `os.link` onto the final path, with a guarded EXCL-in-place fallback + an `_is_inflight` grace guard) + the **`hooks/vao/` package** (the 20 deterministic Layer-3 verification tools, split per discipline family; see CODEBASE_MAP) behind the **`hooks/vao_tools.py` facade** (re-exports every test-referenced name + keeps the CLI byte-identical) + `shared_util.py` (`load_json` / `_utc_now_iso` / the unified JSONL reader) + `shared_rule_constants.py` + `override_markers.py` + `skill_invocation_audit.py` (the Layer 6 Stop-hook auditor; its detection — `find_skill_requests` + `COMMAND_TO_SKILLS` — is reused by the `pretool_skill_gate.py` real-time gate) + `inflight_inbox.py` + `closeout_check.py` (the closeout staleness engine) + `run_continuity.py` (the run-continuity substrate: the `active-run.json` lifecycle marker + the progress fingerprint + engagement/teammate transcript analysis + the `--status` / `--engage` / `--set` / `--mark-complete` / `--stand-down` CLI; kill-switch `CT6_RUN_CONTINUITY_DISABLED`, budget `CT6_MAX_NO_PROGRESS_STOPS`) + `discipline_registry.py` + `run_metrics.py` + `lineage_graph.py`.
- `scripts/notify/notify.py` — the best-effort, stdlib-only per-project email notifier: 10 event types incl. the plan-carrying `run_start`, the `waiting_on_agents` / `agents_complete` dispatch-wait pair, the final `run_complete`, and the tick-driven `heartbeat`, plus the universal informative flags `--details` / `--progress` / `--next-step` and the repeatable `--plan-file` embed.
- `scripts/phenotypes/phenotypes.py` — the deterministic phenotype engine.
- `scripts/data_dictionary/data_dictionary.py` — the deterministic Data Dictionary engine: SQLite introspection + ~100-row sampling, grain/field inference, the fixed provenance vocabulary, value-level corroboration, the reference/relational maps, and the `DATA_DICTIONARY_MAP.md` serializer (the `data-dictionary` skill is its contract).
- `scripts/claude_md/claude_md_efficiency.py` — the deterministic Claude.md-efficiency engine: the pointer-shape + byte-budget assessor (CMD-1…3) + the minimal-pointer generator (CMD-4) (the `claude-md-efficiency` skill is its contract).
- `scripts/mcp_design/output_contract.py` — the deterministic MCP output-contract engine: `build_output_contract` (a closed JSON Schema + the structured-output tool) + `validate_against_contract` + `assess_contract` (MCP-1…3) (the `mcp-output-contract-design` skill + `mcp-design-agent` are its contract/worker).
- `scripts/helpdesk/logit.py` — the deterministic Logit / Helpdesk engine: the privacy-applied triage-submission builder (consent gate + full/summary/off allow-list redaction) + validator (HD-1…3) (the `helpdesk` skill + `/architect-team:logit` are its contract).
- `scripts/token_compression/caveman.py` — the deterministic "caveman" token-compression engine: the meaning-preserving `compress` (filler-drop + phrase-subs, preserving content/identifiers/numbers/line-structure/code) + `estimate_tokens` + `compression_stats` (TC-1…3, internal-comms only) (the `token-compression` skill is its contract).
- `scripts/compliance/instruction_compliance.py` — the deterministic instruction-compliance lint engine: `assess_instruction_files(root)` grades every AI-facing instruction surface (`skills/*/SKILL.md` + `agents/*.md` + `commands/*.md` + `CLAUDE.md` + the two `docs/*_MAP.md` maps) on the machine-checkable dimension of `docs/INSTRUCTION_COMPLIANCE_RUBRIC.md` — frontmatter shape (the house no-`': '`/no-`' #'` YAML hazards + a uniform 1024-char raw-description cap measured before any truncation), required-field presence per file class, section structure, and cross-reference validity (every narrow-grammar `skills/`/`agents/`/`commands/`/`hooks|scripts|services/`/`docs/*_MAP.md` citation resolves against the real repo inventory). The invoke-form citations (`/architect-team:<cmd>`, bare back-ticked skill names) are the rubric's documented LLM-judgment carve-out, deliberately NOT machine-checked. Stdlib-only (`yaml.safe_load` when importable), no import-time side effects (the `docs/INSTRUCTION_COMPLIANCE_RUBRIC.md` rubric is its contract; `tests/test_instruction_compliance.py` pins zero findings across the in-scope set).
- `scripts/claude_design/claude_design_import.py` — the deterministic Claude Design import engine: `detect_claude_design_offer` (both trigger forms — a `claude.ai/design/p/<id>` URL and/or a `claude_design` MCP mention, an inclusive OR — plus `?file=` / trailing `Implement:` parse) + `parse_design_url` + `materialize_project` (path-safe; `_safe_relpath` rejects absolute paths + `..` traversal) + `import_claude_design` + `plan_when_unavailable` (instruct-then-fallback) + the INJECTED `ClaudeDesignSource` MCP adapter / `FakeClaudeDesignSource` offline. Stdlib-only, no import-time side effects; NEVER calls the network (the real `claude_design` MCP fetch is a runtime ToolSearch adapter, no tokens persisted). Feeds the EXISTING `agents/oracle-deriver.md` interactive-mockup oracle path — no new downstream consumer (the `claude-design-import` skill is its contract).
- `services/common/` — the service-tier substrate (stdlib-only): `ed25519.py` (pure-Python RFC-8032 Ed25519, no 3rd-party dep) + `handshake.py` (signed submission envelopes + replay protection + a pluggable SEC-4 attestation hook) for the SEC triage handshake; `bg_runtime.py` (BG-1…4: scheduler + self-check + per-OS boot/restart install descriptors + log-ship interface); `service_config.py` (the same-Anthropic-key model + an `LLMClient` adapter; `DEFAULT_MODEL = claude-fable-5` + `FALLBACK_MODEL = claude-opus-4-8` + `resolve_model(preferred, fallback, availability_checker=None)` routed through `build_llm_client` — the live-availability probe stays an adapter boundary so `check_separation` stays green). DESIGN + stdlib core + tests; the separated service tier may carry its own deps (REPO-4).
- `services/librarian/` — the Librarian service (LIB-1…13, stdlib-only core), installable: `library_index.py` (a `sqlite3` keyword/summary/concept-cloud reference index + the LIB-10 conceptual search) + `extract.py` (the LLM read→confirm→title/summary/keywords/concept-cloud extraction with a string-aware JSON parse) + `librarian.py` (the fetch→extract→index→metadata orchestration on the shared `bg_runtime` + the LIB-8 file-folder body store) + `daemon.py` (the daemon entry point: builds the `Librarian` + `Scheduler` and `run_forever`; + `UrlSource(Source)`, a stdlib `urllib` topic→URL fetcher that skips a failing fetch gracefully). The data SOURCE (scrape / API / the `UrlSource` adapter), the MemPalace VECTOR store (LIB-9), and the LLM are adapters with stdlib fallbacks (`StaticSource` / `FakeLLMClient`). Installable via `/architect-team:librarian-install` + `scripts/setup/install_librarian.py` — provisions state under `~/.architect-team/librarian/`, wires the real Anthropic adapter when `ANTHROPIC_API_KEY` resolves (else provisioned-but-disabled with a `--enable` remediation), and generates + PRINTS the per-OS boot descriptor (never auto-loaded). Installable + self-managed daemon — NOT "deployed/production".
- `services/triage/` — the Triage / Evaluator service (EVAL-1…17 + SEC, stdlib-only core): `issue.py` (the normalized issue record + dedup fingerprint, reusing the helpdesk `logit` privacy engine — off by default, EVAL-17) + `evaluator.py` (the EVAL-1 senior-architect log review + the EVAL-3 hourly BG task) + `tally_queue.py` (EVAL-4/10 dedup tally + backlog) + `triage.py` (the EVAL-11/12 quarantine rule + resolution/recurrence + two-stage review) + `sink.py` (the EVAL-2 GitHub-issue sink adapter) + `server.py` (the SEC Ed25519-handshake submission server, reusing `services/common/handshake.py`). The live HTTP server / GitHub API / Postgres / LLM / SEC-4 attestation are adapters / operator-provided.
- `services/session_review/` — the Session Review service (SR-1…3, stdlib-only core): `session_review.py` — a session-level review agent (of similar design to the Librarian) that reviews a full session via the shared LLM (SR-1; string-aware parse), pushes a session summary (SR-2), and surfaces the issues the agents couldn't solve on the first attempt (SR-3) — filed as REUSED triage `issue` records through the triage `sink`. Runs on the shared `bg_runtime`. EVAL-17 default-off: under `off` it transmits nothing (no push, no filing). The LLM + the outbound-push target + persistence are adapters / operator-provided.
- `services/seeded_mempalace/` — the Seeded MemPalace service (SMP-1…5, stdlib-only core): `bundle.py` (the defined seeded-MemPalace bundle schema + a merge that refreshes the seeded sections while preserving every user namespace — SMP-3/5) + `catalog.py` (the SMP-4 phenotype catalog, REUSING the existing phenotype store, gated by entitlement) + `client.py` (the SMP-1/2 authenticated download — signs the request with the Ed25519 SEC handshake — + merge) + `server.py` (the SMP-2 server skeleton — verifies the handshake, resolves entitlements by the verified public key, gates the catalog). ChromaDB / the live server / the network / billing are adapters / operator-provided.
- `services/separation.py` + `services/SEPARATION_MANIFEST.md` — the service-tier separation manifest (REPO-1…4, stdlib-only): `SEPARATION_MANIFEST` (the open-core-vs-paid-repo plan + the adapter SEAMS the closed/paid pieces plug into — SEC-4 attestation, SMP-4 entitlement/billing) + `check_separation()` (the REPO-4 invariant — parses every `services/**/*.py` and asserts each is import-clean: stdlib + in-repo only at module load, so every external/closed dep is injected not hard-imported). The actual repo SPLIT is a future op; this designs + encodes + validates the boundary.

The 20 Layer-3 verification CLI subcommands (all dispatched through the `hooks/vao_tools.py` facade): `verify-oracle-match`, `verify-baseline-clean`, `verify-no-fake-data`, `verify-every-element`, `verify-rendered-parity`, `verify-interactions-honored`, `verify-live-verification-claim`, `verify-live-data-wiring`, `verify-no-standing-red`, `verify-no-end-of-run-deferral`, `verify-per-persona-path-coverage`, `verify-affordance-coverage`, `verify-no-implementation-scope-cut`, `verify-test-prod-safety-classification`, `verify-discipline-registry-current`, `verify-inflight-clarifications-processed`, `verify-deploy-mandate-satisfied`, `verify-target-element-measured`, `verify-no-pipeline-bypass`, `verify-no-unilateral-override`.

## Structure

- `.claude-plugin/` — identity (`plugin.json` + `marketplace.json`).
- `skills/` — 48 dirs (the pipeline bodies + the discipline skills + `verified-agent-output` + `interactive-mockup-discovery` + the phenotype skills + the exploration/lineage skills + `structure-optimization` + `data-dictionary` + `closeout` + `claude-md-efficiency` + `mcp-output-contract-design` + `helpdesk` + `token-compression` + `claude-design-import`).
- `agents/` — 39 files (the implementer/reviewer/researcher agents; `closeout-agent` is the session-end doc-currency worker; `mcp-design-agent` is the embedded-agent output-contract designer; `scaffold-agent` is an on-demand authoring utility).
- `commands/` — 23 files (the 23 slash commands).
- `hooks/` — JSON wiring + the 7 enforcement scripts + the `hooks/vao/` package + the shared modules (see Stack).
- `services/` — the CT6-6 service/server tier, written separable (REPO-3): `common/` is the shared substrate (the Ed25519 SEC handshake + the BG always-on runtime + the same-Anthropic-key config); `librarian/` is the topic-research curation service (a sqlite reference index + conceptual search on the BG runtime + the `daemon.py` entry point + `UrlSource` adapter; installable); `triage/` is the Evaluator / triage service (log→categorized issue, tally/quarantine, the SEC Ed25519-handshake submission server); `session_review/` is the session-level review agent (summary push + the issues agents couldn't solve on the first attempt); `seeded_mempalace/` is the authenticated seeded-MemPalace download (bundle schema + the phenotype catalog + research synthesis; client + server); `separation.py` + `SEPARATION_MANIFEST.md` are the REPO-1…4 separation manifest + the machine-checkable separability invariant. DESIGN + stdlib-only core + tests, NOT live-deployed (the live server / ChromaDB / Anthropic API are external infra); the Librarian install is opt-in + a self-managed local daemon, NOT a deployed service.
- `tests/` — pytest self-tests across the test-file set (structural + cross-consistency coverage; the VAO behavior tests + fixtures live under `tests/` and `tests/fixtures/vao/`).
- `docs/` — `CODEBASE_MAP.md`, `INTEGRATION_MAP.md`, `INSTRUCTION_COMPLIANCE_RUBRIC.md` (the written instruction-compliance standard; contract for `scripts/compliance/instruction_compliance.py`), `superpowers/` historical design + plan.

For full architecture, file purposes, conventions, gotchas, and a navigation guide, see [`docs/CODEBASE_MAP.md`](docs/CODEBASE_MAP.md). For the per-version history, see [`CHANGELOG.md`](CHANGELOG.md).

## Recent releases

The full per-version narrative lives in [`CHANGELOG.md`](CHANGELOG.md); only the three most recent releases are digested here.

- **v3.41.1 — gateway-activation-drift (a recorded-consent flag is never ground truth).** A non-merge-preserving rewrite of `~/.claude/settings.json` dropped the gateway env block while `gateway.json` still recorded `activated: true`, so sessions ran direct-to-Anthropic with the split off while every surface stayed green. Fixed as a CLASS: `status` names the drift loudly (summary + a dedicated `activation-drift` row + footer + a first-class `--json` field; a clean machine's output stays byte-identical), the install carry-forward VERIFIES against the served port before trusting the flag and HEALS from the persisted master key (corrupt settings.json aborts first; unhealable is a FAIL row, never a green carried-forward), and a new `maybe_heal_activation()` in `hooks/sessionstart-run-continuity.py` self-heals at SessionStart behind a TCP liveness guard, never clobbering a user-customized `ANTHROPIC_BASE_URL`. **REQ-004 — the root clobberer was this repo's own test suite:** `tests/test_install_gateway.py::test_uninstall_purge_removes_state_dir` ran a real `uninstall` with no `--settings-path`, so every full `pytest` run stripped the env block from the developer's real settings.json (a silent no-op on CI, which is why it survived) — the actual cause of BOTH observed incidents. Closed in three layers: the leaker now names `--settings-path`, a module-wide autouse fixture redirects `DEFAULT_USER_SETTINGS_PATH` to a per-test sentinel, and a session-scoped SHA-256 tripwire in `tests/conftest.py` turns any future leak of this class, in any test file, into a loud named suite failure (verified to actually fire). Suite 5646 → **5689 passing + 4 skipped** (202 test files); counts unchanged (48 / 39 / 23).
- **v3.41.0 — glm-secondary-route-fix (the GLM secondary made real).** Two stacked live-verified defects fixed: every `SECONDARY_PROVIDERS` entry now REQUIRES `route_dialect` with the gateway route derived `<route_dialect>/<model>` (`zai` → `hosted_vllm/glm-5.2` — api.z.ai speaks only chat-completions, so the hard-coded `openai/` prefix 404'd every call; `route_model` removed), and the role split writes the harness-spawnable `SPAWN_ALIAS_MODEL_ID` `claude-haiku-4-5` (Claude Code validates teammate model ids client-side; the gateway impersonates the id to the chosen secondary — disclosed in `gateway.json`, `status`, and the README). “CONFIRMED live” now requires a spawn-alias-mandatory `/v1/messages` completion with the serving deployment verified upstream plus an auth-enforcement rung; seven live-verified iterations also land restart-on-config-regeneration, served-state route-contradiction staleness, the port-holder-stopping bind-verified restart, exactly-one tree-aware owned instances, the never-dark verify-then-swap deploy, and the launcher port guard + masked key log. Suite 5542 → 5646 passing + 4 skipped (199 test files + the pytest-collected replication artifact at `tests/bug-fix-glm-secondary-route-fix/`); counts unchanged (48 / 39 / 23).
- **v3.40.1 — context-token-optimization (instruction-surface token-efficiency PATCH).** Docs/instruction files only, zero behavior change: this CLAUDE.md rewritten 95,221 → 25,331 B (`## Recent releases` bounded to the three most recent entries + the CHANGELOG pointer; `What this repo is` + `Stack` rewritten as-shipped; the Conventions `.mempalace` location corrected to disk truth), the three pipeline `## Dispatch mode` residual digests trimmed to their `common-pipeline-conventions` citations (all pinned anchors retained), two consistency fixes, and the new living capability spec `context-surface-efficiency` (59 → 60). Suite 5542 passing + 4 skipped IDENTICAL to v3.40.0 (199 test files); counts unchanged (48 / 39 / 23).

## Conventions at a glance

- Author commits with explicit override (repo's local git config has a "Paul Ingrram" typo):
  `git -c user.name="Paul Ingram" -c user.email="paulingram@users.noreply.github.com" commit ...`
- Run the test suite via `python -m pytest -v` from repo root (green under both Windows cp1252 and `PYTHONUTF8=1`; the expected pass/skip count for the current release is in the CHANGELOG entry).
- Bump version: update `.claude-plugin/plugin.json` + `.claude-plugin/marketplace.json` + `CHANGELOG.md`.
- Runtime state lives under `.architect-team/` (gitignored). v1.1.0 splits it into **shared** (in main worktree: `locks/`, `run-history/`) and **per-run** (in current worktree: `reviews/`, `teammates/`, `handoffs/`, this-run's `openspec/changes/<slug>/`); the MemPalace itself lives at the workspace root `.mempalace/palace`, NOT under `.architect-team/`. The `scripts/setup/worktree_paths.py` helper resolves both via `shared_state_dir()` / `run_state_dir()`. **v1.2.0** auto-creates the run worktree (default; `--no-worktree` opts out) via `scripts/setup/worktree_lifecycle.py::create_run_worktree(slug)` — branch `architect-team/<slug>`, path `<parent-of-repo>/<repo-name>-<slug>/`. **v1.3.0** auto-cleans merged worktrees on the next run via `cleanup_merged_worktrees()` (with `exclude_current=True` keeping the cwd safe); the `/architect-team:cleanup-worktrees` command is the on-demand variant. **v3.7.0** auto-merges a clean run's branch `--no-ff` into `main` and prunes it (`--no-auto-merge` opts out; a conflict falls back to a PR, never `--force`).
