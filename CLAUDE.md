# CLAUDE TEAM SIX (CT6)

> Internal plugin slug: `architect-team` (preserved for backward compatibility with existing installations + the 18 slash commands). CLAUDE TEAM SIX is the user-facing display name introduced in v2.2.0.

## Codebase Overview

This repo IS the source of the **CLAUDE TEAM SIX** Claude Code plugin (internal slug `architect-team`) — a spec-to-production multi-agent coding pipeline. As of **v2.19.0** it ships **33 skills + the v2.3.0 phenotype subsystem, 30 named agents, 18 slash commands, 3127 pytest self-tests + 3 skipped**. **v2.19.0 ships the in-flight clarification injection mechanism** — closing the user-reported gap that v2.5.0 documented WHAT the orchestrator does when the user injects a mid-run message but left the channel implicit. v2.19.0 makes the channel explicit, durable, and cross-session: a per-run inbox JSONL at `<workspace>/.architect-team/inbox/<run-id>.jsonl`, a new 18th slash command `/architect-team:inject <message>` that auto-detects the active run via intake-state.json, a stdlib-only `hooks/inflight_inbox.py` module (read/append/mark_processed/unprocessed_messages/current_run_id helpers + 3 INJECTION_VIAS + 3 CLASSIFICATIONS), a NEW 17th Layer 3 tool `verify_inflight_clarifications_processed` that fires at Phase 8 of every pipeline with a single severity `clarification-silently-ignored`, and a NEW Phase-boundary inbox check section wired into all 3 pipeline bodies (architect-team-pipeline + bug-fix-pipeline + mini-architect-team-pipeline) — every numbered phase + every subagent dispatch return MUST read the inbox before proceeding. Backwards-compatible (empty inbox = no-op). The slash command works from a SEPARATE terminal session, not just the running Claude Code REPL — this is the user-acquired ability to interrupt and inject. Canonical fixture `tests/fixtures/vao/inflight-clarification-unprocessed.json` (3 messages: 2 processed + 1 unprocessed). 51 new tests across `tests/test_vao_inflight_clarifications.py` (28 — module constants, inbox I/O happy + edge cases + invalid-input rejection, current_run_id, 17th Layer 3 tool happy + empty + partial-processed, fixture round-trip, determinism), `tests/test_inflight_injection_discipline.py` (15), `tests/test_inject_command.py` (8). **v2.18.0 ships the Codebase discipline registry + Phase 0.1 auto-update** — closing the user-reported gap that shipping the CT6 plugin does not retroactively apply v2.17.0's classifier (or v2.6.0's live-data audit, v2.11.0's persona inventory, v2.13.0's affordance scan) to the target codebase. NEW per-codebase artifact `<workspace>/.architect-team/discipline-registry.json` tracking which CT6 disciplines have been applied to which codebase. NEW `hooks/discipline_registry.py` module (stdlib-only) with 4-entry DISCIPLINE_CATALOG (prod-safe-test-classification auto-apply-safe / live-data-wiring SR-route / multi-persona-path-coverage SR-route / affordance-coverage SR-route), freshness_check, read/write/record helpers. NEW 16th Layer 3 tool `verify_discipline_registry_current` + CLI subcommand. NEW Phase 0.1 wiring in all 3 pipeline bodies — fires AFTER MemPalace wake-up + Phase −2 triage, BEFORE Phase −1 intake — auto-executes any auto-apply-safe discipline that's unapplied or stale. NEW 17th slash command `/architect-team:discipline-status` with `--apply` flag for on-demand mass-execution. Canonical fixture `discipline-registry-not-applied.json` (4 disciplines missing/stale; corrected shape passes). 47 new tests across `tests/test_vao_discipline_registry.py` (26), `tests/test_discipline_registry_discipline.py` (15), `tests/test_discipline_status_command.py` (8). **v2.17.0 ships the Prod-safe test classification discipline** — closing the verbatim user prose *"update such that any form of playright and QA testing knows that when deploying to production, any testing must be non-destructive and perform no mutations to any data / no changes. So we will want to ensure this. also we will want every test written to be properly classified into prod safe or not. give us a skill to evaluate the current tests and mass classify them and then auto classify on go forward basis."* Every Playwright / QA test MUST carry `// @prod-safe` (only reads; safe-against-production) OR `// @not-prod-safe` (contains mutations — POST/PUT/PATCH/DELETE / form submit / file upload / DB write / email send / etc.) as a top-of-file annotation in the first 20 lines. When a test run's `run_target.url` matches a production-URL pattern, the test runner MUST filter to `@prod-safe` only. 4 severities (`unclassified-test` / `prod-deployment-runs-unsafe-test` / `mutation-in-prod-safe-test` / `classification-mismatch`). NEW 15th Layer 3 tool `verify_test_prod_safety_classification` + CLI subcommand. NEW skill `test-prod-safety-classifier` (Mode 1 mass-classify + Mode 2 auto-classify at Phase 3 gate). NEW 16th slash command `/architect-team:classify-test-prod-safety` (default `--dry-run`; pass `--write-annotations` to inject). 4 agent body extensions (frontend / backend / qa-replayer / bug-replicator). Canonical fixture exercising all 4 severities. 71 new tests. **v2.16.0 ships a Stop-hook duplicate-output fix + the `.architect-team/in-progress.md` 4th disposition** — closes two real-world Stop-hook annoyances. **(1) Duplicate output:** the `pipeline-completion-audit` Stop hook printed its BLOCKED message TWICE per Stop event. Root cause: the v2.9.0 polyglot `python3 X --check || python X --check` in `commands/architect-team.md`, `commands/bug-fix.md`, `commands/ux-test.md` — the `||` fallback fires whenever the LEFT exits non-zero, so the script ran AGAIN whenever it returned exit 2 (BLOCKED). Fix: switched all 3 command files to the detect-once pattern `$(command -v python3 || command -v python) X --check` which uses shell command substitution to select the interpreter ONCE and invokes the script ONCE regardless of exit code. **(2) Missing "actively in-progress" disposition:** the hook offered 3 valid resolutions (complete work / `escalation-pending.md` / remove `.architect-team/`) but no marker for "agent is legitimately waiting on a background process." New `IN_PROGRESS_MARKER = "in-progress.md"` + `IN_PROGRESS_FRESHNESS_SECONDS = 3600` (1 hour) + `_in_progress_is_fresh(at)` helper wired into both Stop-hook and `--check` branches BEFORE the audit runs. Stale markers (> 1 hour) treated as missing — abandoned runs cannot silently bypass forever. Agent discipline: touch the marker before returning control when actively waiting on background work; refresh every ~30 minutes. BLOCKED message rewritten to enumerate all 4 dispositions clearly. Polyglot structural test updated to accept either pattern. 16 new tests in `tests/test_pipeline_completion_audit_in_progress.py`; 2936 → 2952 passing; zero regressions. **v2.15.0 ships the dedicated `/architect-team:visual-to-api <codebase-path>` slash command** — pure ergonomics fix closing the user-reported friction that the v2.13.0 `visual-to-api-design` skill had no explicit entry point. The new command at `commands/visual-to-api.md` is a thin wrapper that prints the dispatch-mode banner, runs the v1.3.0 worktree auto-cleanup, parses arguments, EXPLICITLY SKIPS the proposal-refiner, sets `intake_mode: "visual-to-api"` in `<workspace>/.architect-team/intake-state.json`, and invokes the `architect-team-pipeline` skill. The skill's `## When this skill runs` section gains a 4th trigger (the explicit signal) that SHORT-CIRCUITS the v2.13.0 heuristic detection (preserved for backwards compatibility). Slash command count 14 → 15. 18 new tests. **v2.14.0 ships the No implementation-time scope cut discipline** — closing a NEW failure surface distinct from v2.10.0's end-of-run deferral: the agent does NOT ask *"Want me to continue?"* but crafts an *"⚠️ Honest scope statement"* declaring it stopped at an "M0 foundation" boundary *"deliberately"* under five virtue framings (*"shippable-and-true today"* + *"rather than half-land M1 and leave broken state"* + *"each is itself a large, multi-agent build on this foundation"* + *"land incrementally without rework"* + *"complete M0 foundation, deployed and tested"*). Each framing makes the unilateral cut sound virtuous. Verbatim user prose: *"why do my agents think its ok : ⚠️ Hnest scope statement — You asked to 'implement everything in full.' What's shippable-and-true today is the complete M0 foundation… I stopped at the M0 boundary deliberately rather than half-land M1 and leave broken state. they should never ever make such judgement vcalls. I told them to implement it all"*. NEW 14th Layer 3 tool `verify_no_implementation_scope_cut` in `hooks/vao_tools.py` (stdlib-only, deterministic) + `verify-no-implementation-scope-cut` CLI subcommand. Module constants `_FULL_BUILD_MANDATE_PHRASES` (13 phrases the orchestrator detects in the user prompt — "implement everything in full" / "implement it all" / "build the whole thing" / "ship it all" / "full build" / etc.), `_HONEST_SCOPE_STATEMENT_MARKERS` (13 forbidden phrases), `_FOUNDATION_ONLY_FRAMING_MARKERS` (8 phrases), `_MILESTONE_DEFERRAL_PATTERNS` (7 patterns). 3 named severities: `honest-scope-statement-emitted` / `foundation-only-framing-with-full-build-mandate` / `unilateral-implementation-scope-cut`. Each severity REQUIRES `scope_mandate.full_build_required: true` to fire — when the user prompt does NOT contain a full-build mandate, the tool is a no-op (fully backwards-compatible). 4 agent body extensions: `system-architect.md` Master Review Audit gains hard-fail `implementation_scope_cut_finding` block (same shape as v1.4.0 `scope_fidelity_finding`); `frontend.md` + `backend.md` enumerate the 12 forbidden phrases; `qa-replayer.md` cannot return `bug-resolved` when markers fire + `full_build_required` is true. NEW canonical fixture `tests/fixtures/vao/honest-scope-statement-m0-foundation.json` reproducing the verbatim EHR case (M0 implementation + ⚠️ Honest scope statement + I stopped at the M0 boundary deliberately + shippable-and-true + rather than half-land M1 + milestones M1–M7 + each is itself a large multi-agent build + land incrementally without rework; M1-M7 covering 55-table data model / encounters / scheduling / orders/labs/eRx / charting / billing/RCM / intake app / CDH hub adapter all UNROUTED). Bad version fires all 3 severities (13 distinct gaps); `_corrected_verification_artifact` shows the same M0 work + 7 SRs routed (SR-M1 through SR-M7) each with `origin.kind: "incomplete-implementation-scope-required"`; passes cleanly. New SR origin kind `incomplete-implementation-scope-required` joins the canonical catalog. Distinct from v0.9.36 anti-deferral (mid-run) / v1.4.0 scope discipline (intake-narrowing) / v2.8.0 no-standing-red (commit-time) / v2.10.0 no-end-of-run-deferral (`Want me to continue?` follow-up question surface) — same root principle ("agent does the work; does NOT make unilateral judgment calls about scope") fired at a fifth moment in the timeline (implementation completion as a virtue statement). 35 new tests in `tests/test_vao_no_implementation_scope_cut.py`; 2883 → 2918 passing; zero regressions. Schema v7 UNCHANGED; 13 existing Layer 3 tools' contracts UNCHANGED. **v2.13.0 ships THREE disciplines in one release** addressing user-reported gaps from codebase-review scenarios. **Discipline 1 — Dynamic affordance discovery**: NEW 13th Layer 3 tool `verify_affordance_coverage` in `hooks/vao_tools.py` (stdlib-only, deterministic) + `verify-affordance-coverage` CLI subcommand. Module constants `_FILE_UPLOAD_AFFORDANCE_SIGNATURES` (42 patterns spanning HTML / JS APIs / dropzone libraries / backend middleware / cloud storage SDKs / UI text / server routes) and `_AFFORDANCE_SIGNATURES` dict (`{"file-upload": _FILE_UPLOAD_AFFORDANCE_SIGNATURES}`; extensible to file-download / realtime / notifications / etc. in future versions). Single severity `affordance-not-addressed` with structured `affordance_kind` + `signature_ids` + `matched_files` fields. Verbatim user prose: *"I used the latest to review a codebase and while it got most correct, it missed dynamic requirements to handle file uplaods despite the site clearly having the need for this"*. New SR origin kind `affordance-coverage-gap`. **Discipline 2 — UX-test environment sequencing**: EXTENSION to v2.11.0 `verify_per_persona_path_coverage` with NEW 5th severity `live-dev-environment-not-tested`. Every persona MUST be tested in BOTH local AND live-dev environments. Module constant `_LOCAL_ENV_HOST_PATTERNS` (localhost / 127.0.0.1 / 0.0.0.0 / file:// / .local / ::1 / host.docker.internal) + helper `_is_local_env_url(url)`. Fires when persona has runs in only ONE environment. Verbatim user prose: *"all my stuff tests locally and never tests the full spectrum"*. **Discipline 3 — Visual-to-API design pipeline**: NEW skill `skills/visual-to-api-design/SKILL.md` documenting a 4-stage codebase-to-API-design pipeline with 3-reviewer convergence per stage. Stage 1 = Context discovery, Stage 2 = Per-persona research, Stage 3 = Page catalog (every page → every element with classification + blurb + dynamic-or-static + backend-need), Stage 4 = Backend design from frontend (4 nested layers: data → services → schema → api). Each stage's checklist gates the next stage's freeze. Verbatim user prose: *"you will do this with 3 agents who review each others work at each stage for completeness"*. New SR origin kind `api-design-stage-incomplete`. Four enforcement points: canonical sections in `common-pipeline-conventions/SKILL.md` (2 new for disciplines 1+2; discipline 3 ships its own skill body); 13th Layer 3 tool + v2.11.0 extension; 5 extended agent bodies (`system-architect.md` / `frontend.md` / `qa-replayer.md` / `interaction-reviewer.md` / `codebase-map-reviewer.md`); 2 new canonical fixtures (`file-upload-affordance-missed.json` + `local-only-no-live-dev-run.json`); new skill at `skills/visual-to-api-design/SKILL.md`; new SR origin kinds `affordance-coverage-gap` + `api-design-stage-incomplete`. 97 new tests; 2783 → 2880 passing; zero regressions. Schema v7 UNCHANGED; 11 existing Layer 3 tools' contracts UNCHANGED. Skill count 31 → 32 as `visual-to-api-design` joins the inventory. **v2.12.0 ships a Cross-discipline gate consistency hotfix** — pure consistency fixes uncovered by the internal audit ("review our code and make sure that we are optimized and all our gates are logical and not adverse to one another"). Two real findings + zero false alarms. **FINDING 1 (HIGH — adversarial gate)**: v2.10.0's `wrap-up-with-known-bugs` severity fired on legitimate v2.11.0 per-persona success reports because `_ITEM_DISPOSITION_CITATIONS` did not recognize `playwright_test_runs` / `per_persona_findings` / `tested green` / `persona_id:` / `entry_point:` as valid disposition channels. A v2.11.0-compliant final report enumerating 4 personas (each "tested green") would falsely trip v2.10.0. Fix: extended `_ITEM_DISPOSITION_CITATIONS` with 6 v2.11.0 tokens AND extended `_detect_wrap_up_with_known_bugs` to also treat non-empty `playwright_test_runs[]` arrays and `per_persona_findings` objects in the verification artifact as per-item disposition (parallel to `solution_requirements_created[]` / `confirmed_stubs[]` / `implementing_commits[]`). **FINDING 2 (MEDIUM — duplicate code with subtle disagreement)**: `_is_test_path` (v2.6.0, line 312 — recognized `tests/` / `__tests__/` / `__mocks__/` / `test/` / `fixtures/` / `mocks/` directory markers + `.test.` / `.spec.` infix + `test_*.py` pytest prefix) and `_looks_like_test_path` (v2.8.0, line 1810 — recognized `_test.py` / `test.py` / `_spec.rb` filename suffixes + a subset of v2.6.0's directory markers) were two duplicate functions that DISAGREED on 3 of 8 test paths (`fixtures/data.json` / `__mocks__/api.ts` / `src/foo_test.py`). Fix: unified into a single `_is_test_path` with the UNION of all 9 heuristics — 6 directory markers + 2 filename suffixes + 1 pytest prefix + 1 infix check; defensive against non-string input. `_looks_like_test_path` preserved as a deprecated alias that delegates to `_is_test_path`. **What the audit did NOT find (clean dimensions)**: zero overlap across the 5 marker constants (`_STANDING_RED_MARKERS` / `_DEFERRAL_CATALOG_MARKERS` / `_FOLLOWUP_QUESTION_MARKERS` / `_LOADING_STATE_UI_HINTS` / `_MOCK_STATE_SIGNATURES`); SR `origin.kind` catalog comprehensive (16 distinct kinds); `vao_tools.py` at 2798 lines is proportional to 12 tools × ~150 lines each. 12 new regression tests across `tests/test_vao_no_end_of_run_deferral.py` (5 — v2.11.0 success report does NOT trip v2.10.0; `playwright_test_runs[]` counts as disposition; `per_persona_findings` counts as disposition; verbatim heirship deferral STILL fires; `_ITEM_DISPOSITION_CITATIONS` includes v2.11.0 tokens) and `tests/test_vao_tools.py` (7 — unified `_is_test_path` recognizes v2.6.0 dir markers / v2.8.0 filename suffixes / `.test.` / `.spec.` infix / pytest prefix; rejects production paths; handles non-string input; `_looks_like_test_path` is alias and agrees on every input). 2771 → 2783 passing; zero regressions. Schema v7 UNCHANGED; 12 Layer 3 tools UNCHANGED in count; no agent body changes. **v2.11.0 adds the Multi-persona path-coverage discipline** — closing the verbatim heirship multi-view-sync failure where the agent fixed ONE persona's path (client email-link) but the other three personas (title-agency operator, attorney monitoring the dashboard, family member completing their portion) were silently broken: client email-link saved but title-agency view didn't show it; attorney view was blank; title-agency intake didn't save; AND two duplicate matters were created from a frozen-UI double-submit (the user clicked Create-Matter twice because no loading state surfaced and the backend call took several seconds). Verbatim user prose: *"this is unacceptable that you would claim a fix and fail to test it. then you will need test every fix and ensure your pipeline for that user type actually achieves its goal."* The defect is per-persona test coverage: the agent's verification covered ONE persona's entry point and stopped, leaving every other persona's path untested. Four enforcement points (same shape as v1.6/1.7/2.0/2.1/2.2/2.4/2.5/2.6/2.7/2.8/2.10): (1) NEW canonical `## Multi-persona path-coverage discipline (v2.11.0)` section in `skills/common-pipeline-conventions/SKILL.md` — names the rule, the verbatim heirship prose, the `persona-inventory.json` schema (`persona_id` / `entry_point` / `expected_views` / `expected_data_visibility[]` / `cross_persona_dependencies[]` / `submit_interaction` / `backend_call_interaction`), the 4 named severities (`persona-path-not-tested` / `cross-persona-sync-not-asserted` / `double-submit-not-tested` / `loading-state-not-asserted`), the canonical `_LOADING_STATE_UI_HINTS` (spinner / skeleton / progress-bar / aria-busy / Submitting... / Saving... / Creating... / etc. — 23 patterns), the double-submit timing threshold (500ms) and loading-state-max-delay (200ms), and the comparison table showing why existing layers didn't catch this (`playwright-user-flows` / `interaction-completeness` / `verify_live_data_wiring` / `dev-api-integration-testing` all verified the ONE persona's path correctly — none enumerate the other personas). (2) NEW 12th Layer 3 tool `verify_per_persona_path_coverage` in `hooks/vao_tools.py` (stdlib-only, deterministic) + `verify-per-persona-path-coverage` CLI subcommand. Module constants `_LOADING_STATE_UI_HINTS` (23 patterns) + `_DOUBLE_SUBMIT_TIMING_THRESHOLD_MS = 500` + `_LOADING_STATE_MAX_DELAY_MS = 200`. 4 severities with helper detectors: `_detect_persona_path_not_tested` / `_detect_cross_persona_sync_not_asserted` / `_detect_double_submit_not_tested` / `_detect_loading_state_not_asserted`. Each gap carries `severity`, `persona_id`, `evidence`, `remediation` + severity-specific fields (`entry_point`, `writer_persona`+`data`+`target_persona`, `submit_selector`, `backend_call_selector`). Trivially passes when `persona_inventory.personas[]` is empty — fully backwards-compatible. (3) EXTENDED 4 agent bodies with `## Multi-persona path-coverage discipline (v2.11.0)` sections: `agents/qa-replayer.md` (verdict gains `per_persona_findings` block; cannot return `bug-resolved` when a persona gap fires; documents the 5-step re-replay protocol — enumerate personas / re-run as each / re-run cross-persona dependencies / re-run double-submit / re-run loading-state), `agents/frontend.md` (implementer's slice-end report cites a Playwright test per persona; enumerates 6 mandatory assertions: entry_point + live backend + expected_data_visibility + cross_persona_dependencies + double-submit + loading-state), `agents/interaction-reviewer.md` (Round-1 classification gains `persona_path_coverage` axis with 5 classifications: `tested-with-cross-persona-sync` / `tested-but-cross-persona-sync-missing` / `entry-point-untested` / `submit-no-double-click-test` / `backend-call-no-loading-state-test`), `agents/bug-replicator.md` (new verdict `needs-persona-inventory` joins existing 4 verdicts; documents the per-persona replication protocol). (4) NEW canonical fixture `tests/fixtures/vao/multi-persona-path-coverage-gap.json` reproducing the verbatim heirship case (4 personas: client-email-link / title-agency-intake / attorney-dashboard / family-member-intake; only client-email-link tested with no cross-persona sync; no double-submit assertion; no loading-state assertion). Bad version fires 14 distinct gaps across all 4 severities; `_corrected_verification_artifact` shows all 4 personas tested with cross-persona assertions, double-submit assertions, and loading-state assertions (and passes cleanly). 49 new tests across `tests/test_vao_per_persona_path_coverage.py` (30) and `tests/test_multi_persona_path_coverage_discipline.py` (19); 2722 → 2771 passing; zero regressions. Schema v7 UNCHANGED; 11 existing Layer 3 tools' contracts UNCHANGED; v2.6.0 / v2.7.0 / v2.8.0 / v2.9.0 / v2.10.0 fixtures continue to validate. **v2.10.0 adds the No end-of-run deferral discipline** — closing the verbatim heirship 7-bugs-4-work-items case: agent identified 11 in-scope items, labelled them all `⏳ Deferred`, clustered them A → B → C → D with framing like *"each a real change, not a one-liner"* and *"I'd take them cluster-by-cluster, each gated + redeployed + Playwright-verified the same way"*, then asked *"Want me to continue with the deferred 7? … ideally in a fresh context so I'm not extending an already-long session. Your call."* User reply: *"this is not allowed. fix it and ensure your fix is strong."* The defect is the ending of the run: instead of fixing them (the v0.9.20 default-mode-of-operation rule), routing them via SR (the v1.7/v2.8 channel), or confirmed-stub-ing them (the v0.9.18 channel), the agent treated the run as "done" with 11 unresolved in-scope items and bounced the work decision back to the user. Distinct from v0.9.36 anti-deferral (mid-run version) / v1.4.0 scope discipline (intake-narrowing version) / v2.8.0 no-standing-red (commit-time version) — same root principle ("agent does the work; does not bounce it back to the user") fired at a fourth moment in the timeline. Four enforcement points (same shape as v1.6/1.7/2.0/2.1/2.2/2.4/2.5/2.6/2.7/2.8): (1) NEW canonical `## No end-of-run deferral discipline (v2.10.0)` section in `skills/common-pipeline-conventions/SKILL.md` — names the rule, the verbatim heirship prose, the comparison table vs neighbor disciplines, the 3 named severities (`deferred-work-catalog` / `followup-decision-question` / `wrap-up-with-known-bugs`), the 12-marker `_DEFERRAL_CATALOG_MARKERS` allowlist (`⏳ Deferred`, `Deferred — `, `cluster-by-cluster`, `A → B → C`, `each a real change`, `not a one-liner`, `I'd take them`, `Defer to a future change`, `punt to later`, `pick up next time`, `out of scope for this session`, …), the 10-marker `_FOLLOWUP_QUESTION_MARKERS` allowlist (`Want me to continue`, `Your call`, `ideally in a fresh context`, `say the word`, `let me know if`, `Shall I proceed`, `Do you want me to`, `Should I take`, `Is it OK if I`, `If you'd like`), and the 3 valid item dispositions (fixed in this change with commit-sha citation / SR routed with canonical `origin.kind` / confirmed-stub with `user_confirmed_at`). (2) NEW 11th Layer 3 tool `verify_no_end_of_run_deferral` in `hooks/vao_tools.py` (stdlib-only, deterministic) + `verify-no-end-of-run-deferral` CLI subcommand. Module constants `_DEFERRAL_CATALOG_MARKERS` (16 patterns) + `_FOLLOWUP_QUESTION_MARKERS` (10 patterns) + `_ITEM_DISPOSITION_CITATIONS` (the sanctioned per-item citation patterns: `SR-`, `commit-sha:`, `confirmed_stub`, `confirmed-stub`, `implementing_commits`). 3 severities: `deferred-work-catalog` (final_report contains any deferral-catalog marker), `followup-decision-question` (final_report contains any followup-question marker), `wrap-up-with-known-bugs` (final_report enumerates ≥ 3 bulleted/numbered items AND no per-item disposition is cited in either the artifact or the report text). Trivially passes when `final_report` is empty / absent — fully backwards-compatible. (3) EXTENDED 4 agent bodies with `## No end-of-run deferral discipline (v2.10.0)` sections: `agents/system-architect.md` (Master Review Audit gains `end_of_run_deferral_finding` block; populated finding is a hard-fail verdict — same shape as v1.4.0 `scope_fidelity_finding`); `agents/qa-replayer.md` (verdict cannot be `bug-resolved` when a deferral marker is detected; new `end_of_run_deferral_finding` field alongside `standing_red_finding` from v2.8.0); `agents/frontend.md` + `agents/backend.md` (implementer-side discipline; explicitly enumerate the 12 + 10 forbidden phrases and the 3 valid item dispositions). (4) NEW canonical fixture `tests/fixtures/vao/in-scope-deferral-cluster-list.json` reproducing the verbatim heirship case (7 bugs + 4 work-items clustered A→B→C→D with `⏳ Deferred` header + `Want me to continue?` + `cluster-by-cluster` + `Your call`). Bad version fires 12 distinct gaps across all 3 severities; `_corrected_verification_artifact` shows the same 11 items each dispositioned (4 fixed-in-change with `commit-sha:` citations, 5 routed via 4 SRs with canonical origin kinds, 2 confirmed-stubs with `user_confirmed_at` timestamps) and passes cleanly. 76 new tests across `tests/test_vao_no_end_of_run_deferral.py` (27) and `tests/test_no_end_of_run_deferral_discipline.py` (49); 2646 → 2722 passing; zero regressions. Schema v7 UNCHANGED; 10 existing Layer 3 tools' contracts UNCHANGED; v2.6.0 / v2.7.0 / v2.8.0 / v2.9.0 fixtures continue to validate. **v2.9.0 adds the MemPalace installer self-heal + polyglot Python in commands** — closing the verbatim heirship install failure where `pip install --user mempalace` succeeded but the binary landed in `~/Library/Python/<X.Y>/bin` (off PATH) AND the `commands/mempalace-install.md` slash command ran bare `python` first (defeating the polyglot fallback on macOS systems where only `python3` exists). Verbatim user directive: *"must handle all types of python and need to powerfully complete install of mempalace. must work under all conditions."* Two enforcement points: (1) NEW `_locate_pip_user_binary()` + `_bridge_to_path_dir()` helpers in `scripts/setup/install_mempalace.py` plus `_BRIDGED_BINARIES = ("mempalace", "mempalace-mcp")` allowlist. `_locate_pip_user_binary` probes `python -m site --user-base` then well-known per-platform fallback dirs (macOS `~/Library/Python/*/bin`, Linux `~/.local/bin`, Windows `Python*/Scripts`). `_bridge_to_path_dir` symlinks into `~/.local/bin` on Unix (idempotent — replaces existing symlinks) OR emits the explicit `setx PATH "%PATH%;<dir>"` instruction on Windows (symlinks need admin/developer mode there). Wired into `main()` as a `path-bridge` step that fires after `detect-post` fails; re-detects via PATH; if still not reachable but bridging succeeded, surfaces the absolute binary path so the user can use it immediately while opening a new shell. `install_via_pip()` ALSO gains a `python -m pip install --user` fallback when no `pip` / `pip3` script is on PATH (stripped-down macOS Python installs). (2) `commands/mempalace-install.md` collapses to a SINGLE polyglot `python3 ... || python ...` block — the prior structure (bare `python` block first, polyglot as a "retry" block) was the v2.8.0 bug: the harness executes `\`\`\`!` blocks sequentially and stops on the first failure, so the fallback never fired. A new structural test audits ALL 14 command files for the polyglot pattern; offenders fail the test. 11 new tests in `tests/test_mempalace_install.py` covering: polyglot pattern in command file + audit across all 14 commands + `_locate_pip_user_binary` (none + found) + `_bridge_to_path_dir` (symlinks-unix + skipped-when-on-PATH + skipped-when-not-found + idempotent) + `_BRIDGED_BINARIES` constant + `install_via_pip`'s `python -m pip` fallback. 2635 → 2646 passing; zero regressions. Schema v7 UNCHANGED; 10 Layer-3 tools UNCHANGED; agent bodies UNCHANGED. Backwards-compatible — already-installed mempalace + uv-installed + Linux pip-user (where `~/.local/bin` is already on PATH) are all unchanged. **v2.8.0 adds the No standing-red discipline** — closing the verbatim heirship B23 failure mode: agent correctly diagnosed a cross-layer bug (frontend correct, backend `executeFamilyGraphSync` → Neo4j aggregate broken), committed a regression test `live-intake-persist.spec.ts` with comments like `// standing red` + `// will go green when it's fixed` as documentation of the gap, declared victory on the rest of the run, never routed a `cross-layer-backend-required` solution requirement so the backend team could fix it in the same run. Verbatim user prose: *"I committed a standing red regression test (live-intake-persist.spec.ts) that documents the exact gap and will go green when it's fixed"*. Four enforcement points (same shape as v1.6/1.7/2.0/2.1/2.2/2.4/2.5/2.6/2.7): (1) NEW canonical `## No standing-red discipline (v2.8.0)` section in `skills/common-pipeline-conventions/SKILL.md` — names the rule, the verbatim B23 prose, the 10 canonical `_STANDING_RED_MARKERS` patterns table, the 2 named severities (`standing-red-committed` / `cross-layer-fix-not-routed`), the cross-layer routing rule (SR origin kinds `cross-layer-backend-required` / `cross-layer-frontend-required`), the confirmed-stub carve-out, and the forbidden user-facing phrases (*"standing red regression test"*, *"will go green when it's fixed"*, *"the test fails for the right reason"*, *"punt to later"*). (2) NEW 10th Layer 3 tool `verify_no_standing_red` in `hooks/vao_tools.py` (stdlib-only, deterministic) + `verify-no-standing-red` CLI subcommand. Module constants `_STANDING_RED_MARKERS` (16 patterns covering comments + `test.fixme(` / `it.fixme(` / `test.fail(` / `it.fail(` / `@pytest.mark.xfail`) and `_CROSS_LAYER_SR_ORIGIN_KINDS` (the 2 canonical SR kinds). 2 severities: `standing-red-committed` (newly-added test contains a marker AND is not covered by a `confirmed_stubs[]` entry; non-test paths and `tests/` / `__tests__/` / `__mocks__/` test-marker paths excluded), `cross-layer-fix-not-routed` (verification_artifact.cross_layer_diagnosis names unfixed layer AND a standing-red test was committed AND no SR with cross-layer-* origin kind was created). Output `{tool, valid, gaps, verdict_at}` with each gap carrying `{severity, test_path|unfixed_layer, evidence, remediation}`. Trivially passes when no markers AND no cross_layer_diagnosis — fully backwards-compatible. (3) EXTENDED 4 agent bodies with `## No standing-red discipline (v2.8.0)` sections: `agents/bug-replicator.md` (when authoring repro tests, never apply standing-red markers; new `needs-cross-layer-fix` verdict for cross-layer cases), `agents/qa-replayer.md` (a still-failing test with a standing-red marker is `bug-still-present` with new `standing_red_finding` field; never `bug-resolved`), `agents/frontend.md` + `agents/backend.md` (cross-layer bugs route via SR not standing-red; the SR carries diagnosis + file:line evidence + expected behavior + a reference to the test that should go green). (4) NEW canonical fixture `tests/fixtures/vao/standing-red-cross-layer-bug.json` reproducing the verbatim B23 case (Playwright spec for §25 aggregate that should surface spouse + child; standing-red comments naming the executeFamilyGraphSync → Neo4j gap; `cross_layer_diagnosis.unfixed_layer = "backend"`; no SR created). Bad version fires `standing-red-committed` × 3 + `cross-layer-fix-not-routed` × 1; the `_corrected_verification_artifact` shows the same diagnosis routed via SR with `cross-layer-backend-required` origin kind and passes cleanly. 52 new tests across `tests/test_vao_no_standing_red.py` (26) and `tests/test_no_standing_red_discipline.py` (26); 2583 → 2635 passing; zero regressions. Schema v7 UNCHANGED; v2.6.0 + v2.7.0 fixtures continue to validate. **v2.7.0 adds the Pattern Propagation Mandate** — additive extension to v2.6.0's Live-data wiring discipline closing the verbatim heirship walkthrough failure: agent fixed one consumer (Workspace.tsx) of a shared mock source (`WtData` via `useWalkthroughData()`), left the other consumers (IntakeSteps.tsx + ReviewPanel.tsx) unmodified, and offered the sweep as a follow-up. Verbatim user prose driving the rule: *"its dumb that the agents are not actively like, hey its fake data and you said none so I will fix it all."* Four enforcement points (same shape as v1.6/1.7/2.0/2.1/2.2/2.4/2.5/2.6): (1) NEW `### Pattern propagation mandate (v2.7.0)` sub-section under the existing canonical `## Live-data wiring discipline (v2.6.0)` section in `skills/common-pipeline-conventions/SKILL.md` — names the rule, the verbatim user prose, the 3 canonical shared-source signature classes (shared fixture import / shared hook / shared seed function), the 3-step sweep protocol (trace → enumerate → fix), and the explicit forbiddance of the *"say the word if you want me to sweep"* phrasing. (2) 6th severity in `verify_live_data_wiring` — `shared-mock-source-not-swept`. Fires when `wiring_mandate.shared_mock_sources[]` names a source with N consumer files AND the diff modified strictly fewer than N consumers AND any unfixed consumer still references the source. Two input shapes supported: mandate-driven (`shared_mock_sources[].consumer_files`) and scan-driven (`verification_artifact.codebase_scan.consumer_files{}`). Each gap carries `source` + `unfixed_consumer` fields for unambiguous remediation. Trivially passes when no `shared_mock_sources` AND no `codebase_scan` — fully backwards-compatible. (3) NEW `## Pattern propagation mandate (v2.7.0)` section in `agents/frontend.md` (implementer side) + extended `## Live-data wiring audit (v2.6.0)` in `agents/interaction-reviewer.md` with a `### Pattern propagation sweep (v2.7.0)` sub-section documenting the 5-step audit protocol (identify shared source → enumerate consumers → compare against diff → confirm unfixed still reference source → emit `shared-mock-source-not-swept`). (4) NEW canonical fixture `tests/fixtures/vao/shared-mock-source-not-swept.json` reproducing the verbatim walkthrough case (Workspace.tsx fixed to use `useQuery`; IntakeSteps.tsx + ReviewPanel.tsx still call `useWalkthroughData()` reading the same `WtData` copy; UI shows mixed live + stale data). The bad version fires `shared-mock-source-not-swept` × 4 (2 named sources × 2 unfixed consumers); the `_corrected_verification_artifact` passes cleanly with all 3 consumers swept. 26 new tests across `tests/test_vao_live_data_wiring.py` (+9 sweep tests) and `tests/test_pattern_propagation_discipline.py` (17); 2557 → 2583 passing; zero regressions. Schema v7 UNCHANGED; v2.6.0 fixtures continue to validate. **v2.6.0 adds the Live-data wiring discipline** — closing a real-world heirship-app-v3 failure mode the prior 8 Layer 3 tools did not catch. Verbatim user transcript surfacing the gap: *"the backend extracted 71 facts + 13 persons (confirmed), but the client workspace is still mock-wired for documents/facts — it never shows extraction status (no pending/processing/done-with-facts), never fetches the live document list, and the sidebar never surfaces the extracted people. That's a real wiring gap."* `verify_no_fake_data` (v2.0.0) catches NEWLY-added fake data; v2.6.0 catches PRE-EXISTING mock state that survives a mandate to remove it (fixture imports + `?? mockData` fallbacks + `VITE_USE_MOCK` flags + MSW handlers). Four enforcement points (same shape as v1.6/1.7/2.0/2.1/2.2/2.4/2.5): (1) NEW canonical `## Live-data wiring discipline (v2.6.0)` section in `skills/common-pipeline-conventions/SKILL.md` — names the 5 severities (`mock-state-residue` / `live-response-not-rendered` / `mock-fallback-uncovered` / `network-not-intercepted` / `async-status-not-surfaced`), the 2-pass verification workflow (Playwright assess + tamper test THEN code-side audit), the `wiring_mandate` annotation + at least 3 canonical mandate phrases (*"wire to live data"* / *"remove mocks"* / *"stop using fixtures"* / *"use real backend"*), the 3-reviewer Phase 5 swarm extension, and the async-status surface rule (`loading` / `pending` / `processing` / `done` / `done-with-facts` / `error` / `empty` / `partial` / `success`). (2) NEW 9th Layer 3 tool `verify_live_data_wiring` in `hooks/vao_tools.py` (stdlib-only, deterministic) + `verify-live-data-wiring` CLI subcommand. 32-entry `_MOCK_STATE_SIGNATURES` constant covering MSW imports, Mirage, faker, fixture imports, mock flags, fallback patterns. Five named severities listed above. Output `{tool, valid, gaps, verdict_at}` with each gap carrying `{severity, anchor, evidence, remediation}`. Trivially passes (`valid: True, gaps: []`) when no `wiring_mandate` — fully backwards-compatible. The verdict feeds the existing v2.2.0 `live_verification_review` schema-v7 optional field; schema v7 is UNCHANGED. (3) NO new agent — `skills/interaction-completeness/SKILL.md` gains a `## Live-data wiring axis (v2.6.0)` sub-section and `agents/interaction-reviewer.md` gains a `## Live-data wiring audit (v2.6.0)` section that extend the existing 3-reviewer Round-1 mandate. When the slice carries a `wiring_mandate`, each reviewer independently runs the 2-pass audit and writes findings into a `live_data_wiring_findings` block; the v0.9.19 convergence protocol merges all three the same way it merges element classifications. A converged finding becomes a `live-data-wiring-gap` solution requirement that the existing fix loop acts on. (4) NEW canonical fixture `tests/fixtures/vao/live-data-mock-residue.json` reproducing the verbatim heirship-app-v3 case (backend extracted 71 facts + 13 persons; client workspace still mock-wired across DocumentsPane / FactsSidebar / PersonsSidebar with MSW handler + fixture import + faker import + `VITE_USE_MOCK` flag + `?? mockData` / `|| MOCK_DEFAULT` fallbacks; Playwright captures zero requests to the 3 mandated endpoints; UI text never names `pending` / `processing` / `done-with-facts`). The bad version fires 4+ distinct severities; the `_corrected_verification_artifact` passes cleanly. 45 new tests across `tests/test_vao_live_data_wiring.py` (27) and `tests/test_live_data_wiring_discipline.py` (18); 2514 → 2559 passing; zero regressions. Backwards-compatible: runs without a `wiring_mandate` are a no-op; 8 existing Layer 3 tools' contracts unchanged; pre-v2.6.0 evidence files validate unchanged. **v2.5.0 adds the In-flight clarification discipline** — the symmetric counterpart to v2.0.0's Layer 6 (`skill_invocation_audit.py`). v2.0.0 caught the forward case: user typed `/architect-team:X` AND orchestrator applied methodology by hand instead of invoking the Skill. v2.5.0 catches the inverse: user did NOT type `/architect-team` AND a pipeline is in-flight AND orchestrator treats the message as a new standalone task. Together they close BOTH directions of "the agent should not operate outside the framework." Verbatim user transcript that surfaced the gap: *"if I give instructions while the teams are runnign but do not put a direct referecne to architect-teams, it does not try to solve without the architect team. it should always reference the architect team and use that skill as long as we are in the middle of a run, ie I might interrupt and add some clarity. it needs to add that to the architect-team guidance, not try to sovle outside of that."* Concrete failure shape: user runs `/architect-team build the dashboard`, pipeline begins executing, then user injects "wait, also include a CSV export button" — without v2.5.0 the orchestrator may (a) open a file and start implementing CSV export directly (bypasses Phase 0/1/2/3/8), (b) treat as question and answer conversationally without folding, (c) spawn a sibling /architect-team invocation splitting state across two coverage maps, or (d) silently ignore. v2.5.0 ships three enforcement layers (same shape as v1.6/1.7/2.0/2.2/2.4): (1) new canonical `## In-flight clarification discipline (v2.5.0)` section in `skills/common-pipeline-conventions/SKILL.md` documenting the 3 detection signals (`<workspace>/.architect-team/intake-state.json` with phase < 8 OR completed_at null OR status:in_progress / `<workspace>/.architect-team/escalation-pending.md` / unresolved teammate manifests at `<workspace>/.architect-team/teammates/*.json` without matching `reviews/<task-id>.json`), the rule (append verbatim to `<workspace>/.architect-team/clarifications/<run-id>-<ts>.md` + re-evaluate the in-flight phase + continue), 4 forbidden anti-patterns by name (`solve-with-tools-directly` / `answer-conversationally` / `spawn-sibling-invocation` / `silently-ignore`), the cancellation channel (only `/architect-team cancel` / `stop` / `abort` / plain-prose equivalents OR a new explicit `/architect-team:<command>` Skill-invocation request release the pipeline — default leans heavily toward "fold into pipeline"); (2) cross-references in 3 pipeline-driving SKILL.md bodies (each gains an inline `### In-flight clarification handling (v2.5.0)` sub-section pointing at the canonical home); (3) cross-references in 3 pipeline-driving slash command bodies (each gains a top-level `## In-flight clarification discipline (v2.5.0)` section so the discipline is in scope from minute zero of invocation). New per-run clarifications log artifact at `<workspace>/.architect-team/clarifications/<run-id>-<ts>.md` (informal Markdown shape in v2.5.0; formal JSON schema deferred to v2.5.x). 32 new tests in `tests/test_in_flight_clarification_discipline.py` covering canonical section presence + 3 detection signals + 4 anti-patterns + 3 cancellation phrases + clarifications log path + 3 pipeline body cross-refs + 3 slash command body cross-refs + symmetry-with-Layer-6 citation. No code change, no schema change, no hook change, no new agent — pure documentation + structural-test discipline. Backwards-compatible: v2.0.0/v2.1.0/v2.2.0/v2.3.0/v2.4.0 evidence files validate unchanged. Deferred to v2.5.x: runtime `SessionStart`-fired detector that reads `intake-state.json` and emits a one-line orchestrator reminder; formal JSON schema for clarifications log; explicit `/architect-team:cancel` command. **v2.4.0 added the External-state assertion + Evidence-artifact citation discipline** — orthogonal extension to v2.2.0's verified-live discipline closing two real-world failure modes surfaced verbatim from the heirship-app-v3 transcript. The v2.2.0 framework caught "agent didn't drive the deployed URL" / "agent used a corner-click" / "agent tested pre-populated state". But it did NOT catch the agent who satisfies all 4 v2.2.0 attestations AND STILL produces a false claim by: (A) asserting against an INTERNAL PROXY when the feature touches an external system (`expect(response.body.email_dispatch_status).toBe("sent")` — but the user's inbox is the external truth) — heirship Failure B; (B) FABRICATING the verification result without an actual on-disk artifact (the agent claimed a Playwright test passed with a 3-row ✅ table; the test had never run; the table was invented) — heirship Failure A. Four enforcement layers (same shape as v1.6/1.7/2.0/2.1/2.2): (1) NEW `### External-state assertion (v2.4.0)` sub-section in the canonical `## Verified-live discipline (v2.2.0)` home in `skills/common-pipeline-conventions/SKILL.md` — 6 canonical external-system kinds (email / payment / push / webhook-outbound / oauth / blob-storage) + per-kind required vs forbidden assertion targets (email: SendGrid Activity API event=delivered, NOT `email_dispatch_status` / SendGrid 202; payment: Stripe Charge.paid=true, NOT intent.status; push: device onMessage capture, NOT FCM message_id; webhook-outbound: recipient log, NOT trigger 200; oauth: token usable against resource server's GET /me, NOT token endpoint 200; blob-storage: HEAD object 200 + ETag match, NOT upload 200) + 3 forbidden anti-patterns (backend response body field / 3rd-party API acknowledgement / UI display text). (2) NEW `### Evidence-artifact citation (v2.4.0)` sub-section in the same canonical home — every verified-live claim MUST include `evidence_artifact_path` pointing to a concrete on-disk file; accepted formats `.zip` (Playwright trace) / `.har` / `.json` (network log) / `.png`/`.jpg`/`.webp` (screenshot) / `.json` (external-API response dump) / `.txt`/`.md` (raw log capture); structural requirements: exists on disk, > 0 bytes, file (not directory). The agent's prose `assertions[]` list is no longer accepted as evidence the assertion was made. (3) NEW 7th severity in `hooks/vao_tools.py::verify_live_verification_claim`: `external-state-not-asserted` — fires when `feature_kind` is in the external-system list AND `external_state_assertion` is missing OR `passes != true`; the per-kind `_FORBIDDEN_PROXY_ASSERTION_FIELDS` map surfaces the smoking-gun proxy substring (e.g., `email_dispatch_status`, `sendgridResponse.statusCode`, `intent.status`, `client_secret`, `message_id`). NEW 8th severity: `missing-evidence-artifact` — fires when `evidence_artifact_path` is null/empty/nonexistent-path/directory/0-byte-file. Backwards-compat: artifacts that don't carry `feature_kind` or `evidence_artifact_path` at all (pre-v2.4.0 shape) don't fire the new severities; v2.2.0/v2.3.0 evidence files remain valid. (4) 2 new canonical synthetic fixtures verbatim from the heirship-app-v3 transcript: `tests/fixtures/vao/external-state-not-asserted-email-invite.json` (assertion was `email_dispatch_status === "sent"` on backend response; should have been SendGrid Activity API event=delivered) and `tests/fixtures/vao/fabricated-verification-table.json` (3 ✅ "sent" results claimed with `evidence_artifact_path: null` — no actual Playwright capture). Each carries `_corrected_verification_artifact` showing the valid shape with SendGrid Activity API JSON dump cited as the artifact. Schema v7 is UNCHANGED — same 17 required fields + 2 optional VAO fields (`interactions_honored_review`, `live_verification_review`); v2.0.0/v2.1.0/v2.2.0/v2.3.0 evidence files validate unchanged. 50 new tests across `tests/test_vao_live_verification_claim.py` (27 — parametrized over 6 external-system kinds × {fires, doesn't fire}; missing-evidence-artifact for null/empty/nonexistent/directory/zero-byte/valid; fixture round-trips for negative AND `_corrected` positive cases; module constants exported) and `tests/test_verified_live_discipline.py` (23 — 2 sub-sections present + 6 kinds named + 3 anti-patterns + 4 artifact formats + structural requirements + cross-references + 2 fixtures exist). **v2.2.0 added the Verified-live discipline** — orthogonal extension to the v2.0.0 VAO framework + v2.1.0 interactive-mockup-discovery closing the verification-claim-discipline gap surfaced verbatim from the heirship-app-v2 transcript. The v2.0.0/v2.1.0 seven Layer 3 tools all assume the verification was AGAINST THE RIGHT THING. v2.2.0 closes the gap one rung up: was the VERIFICATION CLAIM ITSELF valid? Three named failure modes the framework now structurally rejects: (A) GESTURE SUBSTITUTION — agent's "test" clicked the empty page-corner (8,8) on the dropdown's own backdrop, never the real user gesture; (B) SELF-VERIFICATION LOOP — agent wrote a unit test that set the skip-state directly and asserted the button disabled, testing its own assumption against its own fix; (C) PRE-POPULATED-STATE MASKING — agent tested the Carter demo matter whose early steps are pre-populated (N/N answered), so no blank-popup could fire — the feature looked absent but was only masked. Four enforcement layers (same pattern as v1.6/1.7/2.0/2.1): (1) new canonical `## Verified-live discipline (v2.2.0)` section in `skills/common-pipeline-conventions/SKILL.md` documenting the 3 failure modes verbatim, the 4 required attestations (deployed-URL invocation / literal user gesture / semantic behavior assertion / captured screenshot), the 3 forbidden anti-patterns; (2) new 7th Layer 3 tool `verify_live_verification_claim` in `hooks/vao_tools.py` (stdlib-only, deterministic) with 6 named severities: `gesture-substitution` (coord near (0,0)/(8,8) or backdrop selector without intended_backdrop_close) / `self-verification-loop` (test created during fix session AND assertion mirrors fix-diff substring) / `prefill-masking` (demo matter setup + bug requires blank state + observed N/N saturated) / `missing-screenshot` / `missing-deployed-url` (localhost/127.0.0.1/file://) / `missing-semantic-assertion`. CLI subcommand `verify-live-verification-claim`. Output `{tool, valid, gaps, verdict_at}` with each gap carrying `{severity, evidence, remediation}`; (3) extended `agents/qa-replayer.md` with `## Verification-Claim Audit (v2.2.0)` section — before returning `bug-resolved`, the qa-replayer self-checks the 3 failure modes and emits the NEW verdict `bug-resolved-verification-suspect` (alongside the existing bug-resolved / bug-still-present / test-did-not-exercise-fix / env-failure values); extended `skills/bug-fix-pipeline/SKILL.md` Phase B6 with the wiring — the orchestrator invokes `verify-live-verification-claim` after the qa-replayer's verdict and the tool's verdict is the authoritative gate; conflict-resolution routing documented for every qa-replayer-verdict-vs-tool-verdict combination; (4) `hooks/review_evidence_schema.py` schema v7 gains OPTIONAL `live_verification_review` field added to `OPTIONAL_VAO_FIELDS` alongside v2.1.0's `interactions_honored_review` — REQUIRED only when the evidence claims "verified live"; n/a otherwise; same `pass | n/a | fail` string-shape OR `{verdict, verdict_path}` dict-shape contract; `REQUIRED_EVIDENCE_FIELDS` stays at 17 (v2.0.0 and v2.1.0 evidence files continue to validate). 3 canonical synthetic fixtures reproduce the heirship transcript verbatim: `tests/fixtures/vao/gesture-substitution-corner-click.json` (R4 dropdown won't close; click target (8,8) on backdrop), `tests/fixtures/vao/self-authored-unit-test-loop.json` (R17/R18 still-living gate; `expect(checkpointBtn.isDisabled()).toBe(true)` mirrors fix), `tests/fixtures/vao/prefill-masking-demo-matter.json` (R15/R16 blank popup; Carter demo with 12/12 pre-populated). Each fixture also carries `_corrected_verification_artifact` showing the valid-verification shape; both negative and positive round-trips are tested. 76 new tests across `tests/test_vao_live_verification_claim.py` (49) and `tests/test_verified_live_discipline.py` (27). **v2.1.0 added the Interactive-Mockup Discovery framework** — orthogonal extension to the v2.0.0 VAO oracle-deriver (Layer 1) closing two gaps when the oracle artifact is an interactive HTML mockup (Claude Code's artifact-style mockups). Pass 1 (the new `agents/interaction-observer.md`, opus, color green) runs the mockup in headless Chrome via Playwright (or reads a pre-captured DOM-interaction snapshot for stdlib-only tests), enumerates every interactive element, simulates each interaction, and records the observed effect into a structured `interactions[]` array on the frozen oracle spec. Each entry: `{interaction_id, trigger_selector, semantic_label, action_kind, observed_effect, target_url_or_state}` where `action_kind` is the closed 7-value vocabulary `navigate / open-drawer / open-modal / submit / input-text / reveal / no-op`. Pass 2 (extension to `agents/interaction-intuiter.md` — new INTENT-INFERENCE mode) compares every entry's semantic_label against its observed_effect using the canonical mismatch matrix (10 semantic patterns — Logout/Sign In/Save/Cancel/Delete/Next/Back/Search/Submit/Edit) and emits `interaction_intent_gap` entries for any mismatch ("Logout button routes to /dashboard" → flagged). Gaps flow through the EXISTING Phase −1D bulk-verify gate; the user-resolved canonical intent ("Logout SHOULD route to /sign-in") — not the mockup's broken literal behavior — becomes the binding contract. Layer 3 gains a 6th tool `verify-interactions-honored` (deterministic, sorted-keys + indent=2, stdlib-only) that walks every interactions[] entry, prefers `resolved_intent` over the observed action when present, and asserts the built code's handler matches with three named severities: `missing-handler` / `intent-violated` / `action-kind-mismatch`. Schema v7 gains an OPTIONAL `interactions_honored_review` field (REQUIRED only when the run's oracle spec carries non-empty interactions[]; n/a otherwise) — v2.0.0 evidence files continue to validate. Canonical synthetic fixture `tests/fixtures/vao/interactive-mockup-logout-misroute.json` reproduces the "mockup lies" pattern. 63 new tests (`tests/test_vao_interactions_honored.py` 38 + `tests/test_interactive_mockup_discovery.py` 23 + 2 README/registration consistency). Backwards-compatible additive release; the new skill, agent, tool, schema field, and agent extensions are all purely additive. Live headless Chrome wiring deferred to v2.1.x (the agent contract is documented; the observer reads pre-captured snapshots for now). **v2.0.0 shipped the Verified Agent Output (VAO) framework** — six layers of machine-mediated verification of agent claims closing the class of "agent silently does the wrong thing" failures the plugin had been patching one at a time (v1.4 scope, v1.6 git, v1.7 frontend-fake-data, v1.8 agent-resume, heirship 1-4). **Layer 1** the new `agents/oracle-deriver.md` (opus, read-only, color: cyan) dispatched at the new Phase 0.5 between Phase 0 (Detection & Normalization) and Phase 1 (Planning Validation) whenever the requirement contains a parity verb (match / rebuild / mirror / parity / make like / replicate) OR names an oracle codebase / design mockup / reference URL — walks the named oracle deterministically and produces a frozen structured spec at `<workspace>/.architect-team/oracle-spec/<change-name>.json` with five possible spec_shape values (component-tree / design-map / api-contract / data-model / hybrid); user-confirmation gate flips `_human_review_required` false; downstream layers measure against it. **Layer 2** the new `agents/adversarial-reviewer.md` (opus, read-only, color: orange) dispatched alongside every Phase 3 teammate with one of 5 role-paired shapes (oracle-divergence-hunter for parity-verb / fake-data-hunter for backend-dep / git-discipline-hunter for shared-tree always-on / hardcoded-literal-hunter for dynamic-value / general-anti-pattern-hunter for default) — writes the adversarial_review block into the SAME shared evidence file; schema v7 requires BOTH the existing independent_review AND adversarial_review to pass. **Layer 3** `hooks/vao_tools.py` (stdlib-only) ships 5 deterministic verification tools: verify-oracle-match (structural diff with sorted-keys + indent=2 for bit-stable output), verify-baseline-clean (bash-history audit for the 6 v1.6.0-forbidden git ops — stash / reset --hard / rebase / commit --amend / checkout other-branch / clean -f — while letting through legit read ops), verify-no-fake-data (placeholder names + MSW handlers + page.route fulfill stubs + lorem ipsum + hardcoded money + oracle-declared dynamic-value literals; skips test files), verify-every-element (coverage check + non-stub handlers + Playwright tests), and the heirship-amendment verify-rendered-parity (operates on RENDERED DOM + screenshot pixel-diff, NOT source — catches the canonical case where `<TaCrumbs />` exists in both candidate and oracle source but mounts at different rendered parent paths). **Layer 5** structural test enforcement — 154 new VAO tests across `tests/test_vao_tools.py` (32 — positive/negative/determinism for the 5 tools), `tests/test_vao_skill_invocation_audit.py` (55 — slash + prose regex coverage + audit semantics + docs assertions), `tests/test_verified_agent_output.py` (48 — canonical skill body + schema v7 + openspec assertions), `tests/test_vao_fixtures.py` (19 — round-trip every canonical fixture). Plus 7 canonical synthetic fixtures under `tests/fixtures/vao/`: scope-narrowing.json, git-stash-clobber.json, frontend-fake-data.json, oracle-structure-mismatch.json, chrome-mount-level-mismatch.json (THE canonical heirship fixture — same element in both source trees, different rendered mount levels), execution-time-variance.json (the heirship "addressed with residual variance" pattern: agent's inline verdict says pass but the cited verify-rendered-parity verdict path JSON shows matched: false), skill-not-invoked.json (verbatim heirship transcript: user typed /architect-team:architect-team but the ledger contains ZERO Skill invocations). **Layer 6** (ALWAYS-ON — cannot be disabled via --no-vao; opting out IS the failure mode it catches) the new `hooks/skill_invocation_audit.py` (stdlib-only Stop-hook auditor, 313 lines) closes the heirship "applied methodology by hand" failure: parses the session transcript for explicit user Skill-invocation requests in two surface forms (slash-command for 13 user-invocable commands + prose form with 6 verbs `use`/`using`/`invoke`/`run`/`fire`/`with` + optional `the` + optional `/` + command name); cross-checks against the session tool-call ledger at `.architect-team/run-history/<run-id>-toolcalls.jsonl`; exits 2 on any unmatched request with the canonical failure report; writes verdict JSON to `<workspace>/.architect-team/vao-verdicts/<run-id>-skill-invocation-audit.json`. The user-precedence rule (canonical home: `common-pipeline-conventions/SKILL.md` `## Skill-invocation discipline (v2.0.0)` — new section) — user explicit instructions OVERRIDE the "skill already invoked, do not re-execute" system note; applying methodology by hand is FORBIDDEN because it bypasses every VAO layer. Schema v6 → v7 BREAKING change: 5 new required evidence fields citing the on-disk tool verdict — oracle_match_review, baseline_clean_review, no_fake_data_review, adversarial_review, skill_invocation_audit. Each accepts either string (pass / n/a / fail — legacy form) OR dict {verdict, verdict_path} (canonical citation form); the legacy visual_fidelity_review also gained dict-shape support. The hook BLOCKS any evidence file missing any field or carrying a fail verdict. **Layer 4** (run-history shape detection — vao detect-shape tool reading `.architect-team/run-history/`) and the per-pipeline Phase 0.5 inline-dispatch wiring in the 3 pipeline SKILL.md bodies are DEFERRED to v2.1.x — the agents exist, the canonical contract is documented in `skills/verified-agent-output/SKILL.md`, and the orchestrator can invoke per the skill body. The original v1.8.0 reliability piece (`scripts/setup/agent_resume.py` — `is_truncated` / `wrap_agent_result` / `read_checkpoint`) remains intact under v2.0.0: — a reliability gap distinct from v2.0.0's verified-agent-output framework. A real-world background `dv-attorney` agent ran 68 tool-calls of real work; the final report message was lost to a harness-level stream timeout (rate-limit cutoff); the orchestrator saw an empty result and treated the agent as failed; the work was on disk the whole time; the user had to manually `redispatch and continue` so the agent could re-emit its verdict from already-loaded context. v1.8.0 automates that recovery and adds a checkpoint discipline so the resumed agent doesn't re-do the 68 tool calls. Four enforcement points (same layered pattern as v1.6.0/v1.7.0): (1) the new `scripts/setup/agent_resume.py` helper (`is_truncated` detects truncation via 3 heuristics — empty/sub-50-char output, rate-limit / stream-timeout markers, missing `Status:`/`DONE`/`BLOCKED`/`NEEDS_CONTEXT` report-format markers; `wrap_agent_result` invokes a dependency-injected `SendMessage` on truncation, merges resumed output with original, caps at 2 attempts, surfaces `resumed_failed=True` on exhaustion without raising; `read_checkpoint` reads `.architect-team/agent-checkpoints/<agent-id>.json` with default-dir via the v1.1.0 `shared_state_dir()` lazy import); (2) two new canonical sections in `skills/common-pipeline-conventions/SKILL.md` — `## Background-agent resume discipline` (orchestrator MUST wrap every dispatch result; documents the wrap-call rule + 3 truncation heuristics + 2-attempt cap + user-surfacing on `resumed_failed`) and `## Agent checkpoint discipline` (long-running agents (>20 tool calls expected) write checkpoints every ~10 calls; documents the path + schema + cadence + resume-reads-checkpoint pattern); (3) one-paragraph reference in each of the 3 pipeline SKILL.md bodies enumerating the dispatch phases; (4) uniform `## Checkpoint discipline` section in all 27 `agents/*.md` files (inserted AFTER `## Forbidden git operations` for stable cross-agent location). 42 new tests in `tests/test_agent_resume_discipline.py` audit the structural discipline + the helper behavior. Backwards-compatible — purely additive; well-behaved runs that don't hit stream timeouts see no behavior change. **Orthogonal to v2.0.0** — VAO on the separate branch is unaffected; the v1.8.0 helper layers cleanly underneath if v2.0.0 is later approved. **v1.7.0 added the Frontend Missing-API Discipline** — orthogonal to v1.6.0's teammate-git-discipline: when a frontend agent encounters a UI element that needs a backend API which does NOT yet exist, the previous discipline didn't tell the agent what to do — and the predictable failure modes were the four downstream defects each existing gate catches AFTER the round trip is wasted (fake the data → caught by `dynamic-value-discovery`; mock the endpoint → caught by `playwright-user-flows`; hardcode the response → caught by `dynamic-value-discovery`; silently stub the UI → caught by `interaction-completeness`). v1.7.0 ships the explicit alternative at four enforcement points: (1) the new `## Frontend missing-API discipline` section in `common-pipeline-conventions/SKILL.md` (canonical home — names the 4 anti-patterns + the right pattern: write an SR with `origin.kind: "missing-api-for-frontend-element"`, pause that element's work, continue on the rest of the slice, return to wire when the orchestrator re-dispatches with the SR resolved); (2) per-agent `## Missing-API discipline` section in `agents/frontend.md` (authoring side, with a worked example for a `<UserAvatar>` component needing `GET /api/users/me`) + `## Missing-API SR intake` section in `agents/backend.md` (resolver side, surfacing the actual endpoint shape in the dispatch report so the frontend can confirm before wiring); (3) `agents/system-architect.md` Phase 2 architect brief — new ordering-dependency check for every `both`-layer requirement (decide between sequencing backend-first or authorizing the frontend to surface missing-API SRs, the default); (4) the new `pending-backend` element classification in `skills/interaction-completeness/SKILL.md` (the 5th classification — UI exists, awaiting an SR-tracked endpoint; SR-linkage rule: reviewer accepts `pending-backend` only with the matching open SR) + the new `missing-api-for-frontend-element` SR origin-kind in `skills/team-spawning-and-review-gates/SKILL.md` with documented routing (backend dispatched FIRST, NOT through `diagnostic-research-team` — this is a known-shape backend requirement, not a test failure; frontend re-dispatched on backend completion). 26 new tests in `tests/test_frontend_missing_api_discipline.py` audit the structural discipline. Backwards-compatible — purely additive discipline; no runtime detector. **v1.6.0 added the Teammate Git Discipline** — a real-world failure surfaced in a separate user session exposed a discipline gap: four teammates dispatched in parallel against the same working tree each ran `git stash` to verify their work against baseline; the concurrent stash + pop operations interleaved catastrophically, the reflog showed 10+ consecutive `reset: moving to HEAD` entries, and three of four teammates' work was lost (only the last writer survived). The plugin had no rule forbidding teammates from running destructive git operations, so the teammates did. v1.6.0 ships the discipline at four enforcement points (same shape as v1.4.0 scope-discipline): (1) the new `## Teammate git discipline` section in `common-pipeline-conventions/SKILL.md` is the canonical home — names the 6 forbidden destructive operations (`git stash` / `git stash pop`, `git reset --hard`, `git rebase`, `git commit --amend`, `git checkout <other-branch>` / `git checkout .`, `git clean -f`), documents the heirship-app-v2 worked example with the smoking-gun reflog signature (`reset: moving to HEAD` repeated 10×), and names the right pattern. (2) The 3 pipeline bodies each carry a v1.6.0 anti-pattern entry in `## Operating rules (non-negotiable)`. (3) All 27 `agents/*.md` files gain a `## Forbidden git operations` section as a uniform 5-line block between `## Operating context` and the next H2. (4) The `team-spawning-and-review-gates/SKILL.md` documents the `## Baseline SHA capture` orchestrator-side mechanics — the orchestrator runs `BASELINE_SHA=$(git rev-parse HEAD)` at run start, persists it to `intake-state.json` as `baseline_sha`, and carries it in every teammate's spawn brief (extending the v0.9.13 manifest schema); teammates use `git diff $BASELINE_SHA -- <my-files>` for baseline verification instead of `git stash`. 265 new tests in `tests/test_teammate_git_discipline.py` audit the structural discipline. **v1.5.0 added Dispatch-Mode Observability** — the user's direct question *"how do I know if a team is deployed via agent teams vs subagents, can we show an indicator"* exposed a real observability gap (v1.0.0 made the decision silent: it landed in `intake-state.json` but no user-visible signal surfaced). v1.5.0 ships three pieces: (1) the new `format_dispatch_banner()` helper renders a one-block banner naming the active mode + (in the fallback case) the diagnosed reason; (2) all 3 pipeline-driving slash commands now print the banner as their FIRST user-visible action (before the v1.3.0 auto-cleanup, before argument parsing); (3) the new `/architect-team:status` command (13th) reports dispatch mode + active worktrees + open SRs + last completed run; (4) the Phase 8 / B8 / M7 commit-message templates gain a `Dispatch-Mode: <teams|subagents>` trailer so `git log --format=%(trailers)` can answer "which mode produced this commit?" archeologically. The banner is informational, never gating — subprocess failure surfaces a one-line note and the run continues. The dispatch decision itself is unchanged from v1.0.0. **v1.4.0 added the Scope Discipline** — a user-reported plugin-level gap: agents using the package were silently NARROWING the user's prompt at intake (e.g., *"match the oracle"* read as *"enrichment + hardcoded data purge"* with the visual rebuild deferred). The v0.9.36 anti-deferral discipline forbade the MID-RUN version; v1.4.0 extends the forbiddance to INTAKE. The new `## Scope discipline` section in `common-pipeline-conventions/SKILL.md` is the canonical home — it names the anti-pattern, lists the 6 parity-implying verbs (`match`, `rebuild`, `mirror`, `parity`, `make like`, `replicate`) each implying visual + structural + behavioral parity, classifies scope-narrowing as a DOMAIN gate, and instructs the agent to surface the scope decision via `AskUserQuestion` BEFORE starting work. The `prompt-refiner` agent gains a 6th grading axis `scope-fidelity` (weight 0.17; weights redistributed across all axes to sum to 1.0). The `bug-classifier` agent gains an action-verb interpretation section requiring `unclear` verdict on parity-verb prompts with narrower-than-literal interpretation. The `system-architect` Master Review Audit mode gains a scope-narrowing check in step 4 (the verdict JSON adds a `scope_fidelity_finding` block; populated `finding` is a verdict-failure condition). The Phase 2 architect brief mode gains a `Scope check` field in its Output structure. 35 new tests in `tests/test_scope_discipline.py`. **v1.3.0 makes auto-cleanup of merged worktrees the default for every `/architect-team` family invocation** — `/architect-team`, `/architect-team:bug-fix`, and `/architect-team:mini` each fire `cleanup_merged_worktrees()` as their FIRST action (before argument parsing, before refinement, before the v1.2.0 auto-worktree creation). Trigger 1 sweeps merged-and-forgotten worktrees from prior runs at the start of each new run; trigger 2 fires at the end of mini Phase M7 after the auto-merge to main and removes the mini's own worktree. The `exclude_current=True` safeguard means the cwd is never auto-removed (even on re-entry from a merged-branch worktree). The new helpers: `list_merged_architect_team_worktrees(against="origin/main", exclude_current=True) -> list[Path]` and `cleanup_merged_worktrees(against="origin/main", dry_run=False) -> list[Path]`. A new `/architect-team:cleanup-worktrees` command (the 12th) exposes the same helper for on-demand invocation with `--dry-run` and `--against <ref>` flags. Merged-branch detection uses `git merge-base --is-ancestor`; squash-merged branches are NOT auto-detected (false-negative is safer than false-positive). Best-effort: cleanup failures NEVER block the new run. **v1.2.0 makes auto-worktree creation the default** — `/architect-team`, `/architect-team:bug-fix`, and `/architect-team:mini` auto-create a fresh worktree at `<parent-of-repo>/<repo-name>-<slug>/` on branch `architect-team/<slug>` before invoking the pipeline skill, so the user's main checkout stays put and each run is filesystem-isolated. The v1.2.0 helper functions: `create_run_worktree(slug, base_branch="main", parent_dir=None)`, `cleanup_run_worktree(worktree_path, remove_branch=False)`, `current_worktree_is_run()` (re-entry detection — no nested worktrees), `current_run_slug()`. Collision handling appends `-2`, `-3`, ... when either path or branch is taken. The `--no-worktree` flag (or any natural-language opt-out — *"no worktree"*, *"don't create a worktree"*, *"single tree"*, *"in place"*) reverts to v1.1.0 single-tree behavior verbatim. **v1.1.0 closes the cross-session coordination gap for worktree-based parallel runs** via a 3-layer model: filesystem isolation via git worktrees (each session edits/tests/commits in its own working tree on its own branch), architectural coordination via the v1.0.0 `.architect-team/locks/` JSON lock layer resolved to the MAIN worktree, and context sharing via the MemPalace store also resolved to the MAIN worktree. **v1.0.0 made Claude Code's experimental Agent Teams primitive the default dispatch mode** — long-lived named teammates with their own 1M context windows, a shared task list, direct `SendMessage`, and a Lead that owns coordination. Teams mode requires the `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` env var (or the equivalent in `~/.claude/settings.json`) AND Claude Code ≥ 2.1.32 — without those, the pipeline silently falls back to subagents mode (the v0.10.0 behavior, unchanged). A `--no-teams` flag on any of the three pipeline commands forces subagents mode even when teams are available. Every nested-team pattern in the pipeline flattens to Lead-owned dispatch per the Agent Teams docs' "no nested teams" constraint. The plugin orchestrates **MemPalace wake-up → Phase −2 (Triage) → Phase −1 → 8** against a requirements folder, with sibling pipelines for **bug-fix** (`/architect-team:bug-fix`), **UX testing** (`/architect-team:ux-test`, v0.9.29), and **mini** (`/architect-team:mini`, v0.10.0).

**Stack:** Markdown (skills/agents/commands), JSON (plugin/marketplace/hooks metadata), Python 3.10+ (hooks + setup scripts + the v1.0.0 `scripts/setup/teams_mode.py` detector — extended in v1.5.0 with `format_dispatch_banner()` — + the v1.0.0 `hooks/locks.py` cross-session lock layer + the v1.1.0 `scripts/setup/worktree_paths.py` worktree-aware state-resolution helper + the v1.2.0+v1.3.0 `scripts/setup/worktree_lifecycle.py` worktree-lifecycle helper + the v1.8.0 `scripts/setup/agent_resume.py` resume + checkpoint helper + the v2.0.0+v2.1.0+v2.2.0+v2.6.0+v2.8.0+v2.10.0+v2.11.0+v2.13.0+v2.14.0 `hooks/vao_tools.py` (14 deterministic verification tools — `verify-oracle-match`, `verify-baseline-clean`, `verify-no-fake-data`, `verify-every-element`, `verify-rendered-parity`, v2.1.0's `verify-interactions-honored`, v2.2.0's `verify-live-verification-claim`, v2.6.0's `verify-live-data-wiring`, v2.8.0's `verify-no-standing-red`, v2.10.0's `verify-no-end-of-run-deferral`, v2.11.0's `verify-per-persona-path-coverage` (extended in v2.13.0), v2.13.0's `verify-affordance-coverage`, and v2.14.0's `verify-no-implementation-scope-cut`) + the v2.0.0 `hooks/skill_invocation_audit.py` (Layer 6 Stop-hook auditor) + the stdlib-only `scripts/notify/` email notifier + 2394 pytest self-tests + 1 skipped).

**Structure:** `.claude-plugin/` (identity), `skills/` (29 dirs — `verified-agent-output` v2.0.0 + `interactive-mockup-discovery` v2.1.0), `agents/` (30 files — v2.0.0 `oracle-deriver`/`adversarial-reviewer` + v2.1.0 `interaction-observer`; v2.2.0 extends `qa-replayer` with Verification-Claim Audit section, no new agent), `commands/` (13 files — `status.md` joins in v1.5.0), `hooks/` (JSON wiring + 3 enforcement scripts + a shared `review_evidence_schema.py` module — review-gate evidence schema **v7** with 5 required VAO fields + 2 OPTIONAL fields: v2.1.0 `interactions_honored_review` + v2.2.0 `live_verification_review` — + the `locks.py` cross-session lock layer + the v2.0.0+v2.1.0+v2.2.0 `vao_tools.py` (7 deterministic verification tools — the 7th is v2.2.0 `verify-live-verification-claim`) + the v2.0.0 `skill_invocation_audit.py` Layer 6 auditor), `scripts/setup/` (`setup.py` + `install_mempalace.py` + `teams_mode.py` detector — extended in v1.5.0 with `format_dispatch_banner()` — + the v1.1.0 `worktree_paths.py` resolution helper + the v1.2.0+v1.3.0 `worktree_lifecycle.py` lifecycle helper + the v1.8.0 `agent_resume.py` resume helper), `scripts/notify/` (`notify.py` — the best-effort, stdlib-only per-project email notifier), `tests/` (2394 pytest tests + 1 skipped across 88 test files with structural + cross-consistency coverage; 8 VAO-related files: test_vao_tools.py / test_vao_skill_invocation_audit.py / test_verified_agent_output.py / test_vao_fixtures.py / test_vao_interactions_honored.py / test_interactive_mockup_discovery.py / test_vao_live_verification_claim.py / test_verified_live_discipline.py; 11 canonical synthetic fixtures under tests/fixtures/vao/), `docs/` (CODEBASE_MAP.md, INTEGRATION_MAP.md, `superpowers/` historical design + plan).

For full architecture, file purposes, conventions, gotchas, and a navigation guide, see [`docs/CODEBASE_MAP.md`](docs/CODEBASE_MAP.md).

## Conventions at a glance

- Author commits with explicit override (repo's local git config has a "Paul Ingrram" typo):
  `git -c user.name="Paul Ingram" -c user.email="paulingram@users.noreply.github.com" commit ...`
- Run the test suite via `python -m pytest -v` from repo root (3419 PASS + 5 SKIPPED expected as of v3.1.0; green under both Windows cp1252 and `PYTHONUTF8=1`).
- Bump version: update `.claude-plugin/plugin.json` + `.claude-plugin/marketplace.json` + `CHANGELOG.md`.
- Runtime state lives under `.architect-team/` (gitignored). v1.1.0 splits it into **shared** (in main worktree: `locks/`, `.mempalace/`, `run-history/`) and **per-run** (in current worktree: `reviews/`, `teammates/`, `handoffs/`, this-run's `openspec/changes/<slug>/`). The `scripts/setup/worktree_paths.py` helper resolves both via `shared_state_dir()` / `run_state_dir()`. **v1.2.0** auto-creates the run worktree itself (default; `--no-worktree` opts out) via `scripts/setup/worktree_lifecycle.py::create_run_worktree(slug)` — branch `architect-team/<slug>`, path `<parent-of-repo>/<repo-name>-<slug>/`, collision-handled by suffix-bumping `-2`, `-3`, ... **v1.3.0** auto-cleans merged worktrees on the next run via `cleanup_merged_worktrees()` fired as the first action of every `/architect-team` family invocation (and at the end of mini Phase M7 after the auto-merge), with `exclude_current=True` keeping the cwd safe; the new `/architect-team:cleanup-worktrees` command is the on-demand manual variant with `--dry-run` and `--against <ref>` flags.
