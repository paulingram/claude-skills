# CLAUDE TEAM SIX (CT6)

> Internal plugin slug: `architect-team` (preserved for backward compatibility with existing installations + the 21 slash commands). CLAUDE TEAM SIX is the user-facing display name introduced in v2.2.0.

## What this repo is

This repo IS the source of the **CLAUDE TEAM SIX** Claude Code plugin (internal slug `architect-team`) — a spec-to-production multi-agent coding pipeline. It orchestrates **MemPalace wake-up → Phase −2 (Triage) → Phase −1 → 8** against a requirements folder, with sibling pipelines for **bug-fix** (`/architect-team:bug-fix`), **UX testing** (`/architect-team:ux-test`), and **mini** (`/architect-team:mini`).

As of **v3.19.0** it ships **44 skills + the v2.3.0 phenotype subsystem (now 4 seeded phenotypes — `user-management`, `config-management`, `ai-management`, `code-wiki`), 38 named agents, 21 slash commands, and 4588 pytest self-tests + 5 skipped** (across 182 test files; green under both Windows cp1252 and `PYTHONUTF8=1`). The default dispatch mode is Claude Code's experimental **Agent Teams** primitive (long-lived named teammates with their own 1M context windows, a shared task list, direct `SendMessage`, and a Lead that owns coordination); it requires `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` and Claude Code ≥ 2.1.32, and silently falls back to subagents mode otherwise. `--no-teams` forces subagents mode.

## Stack

Markdown (skills/agents/commands), JSON (plugin/marketplace/hooks metadata), Python 3.10+ — **stdlib-only**. The Python surface:

- `scripts/setup/` — `setup.py` + `install_mempalace.py` + `teams_mode.py` (dispatch detector + `format_dispatch_banner()`) + `worktree_paths.py` (worktree-aware state resolution) + `worktree_lifecycle.py` (worktree create/cleanup/merge) + `agent_resume.py` (resume + checkpoint) + `sync_agent_boilerplate.py` (canonical agent-boilerplate re-sync).
- `hooks/` — JSON wiring + 6 enforcement scripts (`review-gate-task.py` PostToolUse(TaskUpdate)+TaskCompleted / `teammate-idle-check.py` SubagentStop+TeammateIdle / `pipeline-completion-audit.py` Stop / `pretool_unilateral_override_guard.py` PreToolUse(Edit/Write/NotebookEdit) / `pretool_skill_gate.py` PreToolUse[*] — v3.15.0 real-time skill-invocation hard-gate / `precompact-closeout.py` PreCompact — v3.18.0 closeout doc-currency reminder) + the shared `review_evidence_schema.py` (evidence schema **v7**: 17 required fields incl. 5 VAO fields + 3 optional fields `interactions_honored_review` / `live_verification_review` / `appearance_scope_review`) + `locks.py` (cross-session lock layer) + the **`hooks/vao/` package** (the 20 deterministic Layer-3 verification tools, split per discipline family; see CODEBASE_MAP) behind the **`hooks/vao_tools.py` facade** (re-exports every test-referenced name + keeps the CLI byte-identical) + `shared_util.py` (`load_json` / `_utc_now_iso` / the unified JSONL reader) + `shared_rule_constants.py` + `override_markers.py` + `skill_invocation_audit.py` (Layer 6 Stop-hook auditor; its detection — `find_skill_requests` + `COMMAND_TO_SKILLS` — is reused by the v3.15.0 `pretool_skill_gate.py` real-time gate) + `inflight_inbox.py` + `closeout_check.py` (the v3.18.0 closeout staleness engine) + `discipline_registry.py` + `run_metrics.py` + `lineage_graph.py`.
- `scripts/notify/notify.py` — the best-effort, stdlib-only per-project email notifier (6 event types incl. `heartbeat`).
- `scripts/phenotypes/phenotypes.py` — the deterministic phenotype engine.
- `scripts/data_dictionary/data_dictionary.py` — the deterministic Data Dictionary engine (v3.17.0): SQLite introspection + ~100-row sampling, grain/field inference, the fixed provenance vocabulary, value-level corroboration, the reference/relational maps, and the `DATA_DICTIONARY_MAP.md` serializer (the `data-dictionary` skill is its contract).
- `scripts/claude_md/claude_md_efficiency.py` — the deterministic Claude.md-efficiency engine (v3.19.0): the pointer-shape + byte-budget assessor (CMD-1…3) + the minimal-pointer generator (CMD-4) (the `claude-md-efficiency` skill is its contract).

The 20 Layer-3 verification CLI subcommands (all dispatched through the `hooks/vao_tools.py` facade): `verify-oracle-match`, `verify-baseline-clean`, `verify-no-fake-data`, `verify-every-element`, `verify-rendered-parity`, `verify-interactions-honored`, `verify-live-verification-claim`, `verify-live-data-wiring`, `verify-no-standing-red`, `verify-no-end-of-run-deferral`, `verify-per-persona-path-coverage`, `verify-affordance-coverage`, `verify-no-implementation-scope-cut`, `verify-test-prod-safety-classification`, `verify-discipline-registry-current`, `verify-inflight-clarifications-processed`, `verify-deploy-mandate-satisfied`, `verify-target-element-measured`, `verify-no-pipeline-bypass`, `verify-no-unilateral-override`.

## Structure

- `.claude-plugin/` — identity (`plugin.json` + `marketplace.json`).
- `skills/` — 44 dirs (the pipeline bodies + the discipline skills + `verified-agent-output` + `interactive-mockup-discovery` + the phenotype skills + the exploration/lineage skills + `structure-optimization` + `data-dictionary` + `closeout` + `claude-md-efficiency`).
- `agents/` — 38 files (the implementer/reviewer/researcher agents; `domain-researcher` / `endpoint-tracer` / `structure-analyst` / `reference-tracer` / `structure-adversary` are the more recent joiners; `closeout-agent` (v3.18.0) is the session-end doc-currency worker; `scaffold-agent` is an on-demand authoring utility).
- `commands/` — 21 files (the 21 slash commands).
- `hooks/` — JSON wiring + the 6 enforcement scripts + the `hooks/vao/` package + the shared modules (see Stack).
- `tests/` — pytest self-tests across the test-file set (structural + cross-consistency coverage; the VAO behavior tests + fixtures live under `tests/` and `tests/fixtures/vao/`).
- `docs/` — `CODEBASE_MAP.md`, `INTEGRATION_MAP.md`, `superpowers/` historical design + plan.

For full architecture, file purposes, conventions, gotchas, and a navigation guide, see [`docs/CODEBASE_MAP.md`](docs/CODEBASE_MAP.md). For the per-version history, see [`CHANGELOG.md`](CHANGELOG.md).

## Recent releases

The full per-version narrative lives in [`CHANGELOG.md`](CHANGELOG.md). The most recent:

- **v3.19.0 — Claude.md efficiency (CT6-6 component 3: CMD-1…CMD-4).** When (and only when) MemPalace is installed, `CLAUDE.md` becomes a thin POINTER document — it tells the agent WHERE to find things so context is loaded on demand (CMD-2), stays very small (CMD-3), and carries two parts: standards that point to a reference MemPalace + customizations the user toggles on/off (CMD-4). The discipline is CONDITIONAL on MemPalace (CMD-1) — with none installed a self-contained `CLAUDE.md` is correct and the discipline does not apply. New skill `skills/claude-md-efficiency/SKILL.md` over a new stdlib-only engine `scripts/claude_md/claude_md_efficiency.py`: `assess_claude_md` (pointer-shape + a byte budget → advisory signals: `over-budget` / `no-pointers` / `missing-standards-pointer` / `missing-customizations`) + `generate_pointer_claude_md` (emits a minimal pointer doc — wake-up first step + standards + customizations). Reuse-first over `mempalace-integration` (the availability check + the mine/wake-up flow it points INTO). Honest boundary: the signals are heuristics (a marker count + a budget, not proof the pointers resolve), and context is NEVER deleted from `CLAUDE.md` unless first stored in MemPalace. Adversarial review (producer≠checker): SHIP (no blockers); added the recommended empty/at-budget/byte-counting boundary pins. Suite 4574 → 4588 passing + 5 skipped (182 test files), both encodings. NO new command/agent/Layer-3-tool.
- **v3.18.0 — the `closeout` capability (CT6-6 component 2: CO-1…CO-3).** A double-check at the END of a work session: before context is compacted or work is declared done, confirm the documentation reflects what changed — and if a doc is lax, UPDATE it, not just flag it. The novel piece is the **fire-before-compact trigger**: a new `PreCompact` hook `hooks/precompact-closeout.py` (the first `PreCompact` wiring) runs the deterministic staleness engine `hooks/closeout_check.py` against the working tree and, when docs look stale, injects a non-blocking, fail-open closeout reminder (CO-1). The review-and-update (CO-2/CO-3) is the new `skills/closeout/SKILL.md` contract + the new `agents/closeout-agent.md` worker (Write-only, bounded to the documentation-currency inventory; operates from the working-tree diff so it runs OUTSIDE a full pipeline run where `doc-updater`'s coverage-map/run-ledger inputs don't exist) — reusing the `documentation-currency` discipline + the `doc-updater` whole-file-rewrite pattern. Manual trigger: the 21st command `/architect-team:closeout` (`--check` for read-only). The engine's inventory mirrors `documentation-currency` (pinned by a test against drift); the `new-surface-undocumented` signal keys off the SPECIFIC inventory-count docs (README/CLAUDE/CODEBASE_MAP) so a lone CHANGELOG touch can't mask a stale grid. Adversarial review (producer≠checker) returned FIX-FIRST; remediated 4: the CHANGELOG-touch false-negative, rename/copy detection in the porcelain parser, the `_resolve_repo_root` cwd fallback, and the missing edge tests. Suite 4539 → 4574 passing + 5 skipped (181 test files), both encodings. NO new Layer 3 tool.
- **v3.17.0 — the `data-dictionary` skill (CT6-6 component 1: DD-1…DD-18).** The first in-repo component of the Claude Team 6 program: a self-contextualizing data-dictionary builder. Given code and/or docs that reference data tables/databases, it derives the data model, defines every field, and records provenance + corroboration. New `skills/data-dictionary/SKILL.md` (the LLM-judgment contract) over a new stdlib-only deterministic engine `scripts/data_dictionary/data_dictionary.py` (the machine): SQLite introspection + ~100-row sampling (DD-9/10), grain inference (DD-11), field inference (DD-12), the FIXED provenance vocabulary `direct-user-input` / `direct-code-comment` / `inference` / `live-data` (DD-13), value-level corroboration of EVERY provided definition — key claims AND type claims — against the real data (DD-14), the by-field/by-table reference map + the relational/blend map incl. non-DB code joins (DD-7), and the `DATA_DICTIONARY_MAP.md` (+ `data-dictionary.json`) serializer (DD-7/16). Reuse-first against the `*-mapping` family; sequenced after codebase+integration mapping (DD-6/16); MemPalace persistence via `mempalace-integration` (DD-15); the maintenance discipline mirrors `documentation-currency` (DD-17/18). Honest live-DB boundary: live inspection needs a reachable DB — the no-DB `build_from_inputs(...)` path never marks a field `live-data` and the artifact's `live_inspection` block records `ran: false`; small-sample inferred keys are hedged below `high`. Adversarial review drove 5 fixes pre-ship (DD-14 was key-only; a SQL-identifier crash on quoted names; a documented-but-missing no-DB path; un-hedged small-sample keys; gap-blessing tests). Verified by a real local-SQLite dogfood + `tests/test_data_dictionary.py` (21 cases). Suite 4516 → 4539 passing + 5 skipped (180 test files); NO new Layer 3 tool.
- **v3.16.0 — responsive + parallel `/architect-team:inject` (concurrent in-run lanes).** Closes *"inject just sits there passively … I need it to spawn more teams so we can have multiple problems worked on in parallel."* Two root causes fixed: the inbox was polled only at phase boundaries (the Lead blocked synchronously on teammates), and a parallel team for an injected problem was a FORBIDDEN anti-pattern (`spawn-sibling-invocation`). Now: a new `parallel-problem` classification (`hooks/inflight_inbox.py`) + a `lane_id` field opens a sanctioned concurrent in-run **lane** (a background team holding a disjoint file-scope lock via `hooks/locks.py`, converging via Phase 4) instead of folding; the inbox-check protocol polls on every phase boundary AND every background-dispatch return/wake (background dispatch frees the Lead); the `spawn-sibling-invocation` rule is amended to distinguish a fragmenting second RUN (still forbidden) from a concurrent LANE inside the one run (newly allowed). Adversarial review drove the honesty: it is polling-not-push, lane isolation is file-glob/advisory (`cdlg_overlap` NOT wired into `acquire_lock` — keep lane scopes coarse; Phase 4 backstops), background lanes degrade to sequential in subagents-mode, and a failed lane spawn downgrades rather than wedging Phase 8. New `tests/test_parallel_lane_inject.py` (13 cases incl. the end-to-end dogfood). Suite 4503 → 4516 passing + 5 skipped (179 test files); no new Layer 3 tool.
- **v3.15.1 — skill-gate narrowing fix.** The v3.15.0 gate's `PreToolUse[*]` matcher blocked EVERY non-`Skill` tool, which over-fired on the `/architect-team` command WRAPPER's own documented pre-Skill setup (dispatch banner / merged-worktree cleanup / worktree creation — all `Bash`) — observed on a 3.15.0 server blocking the banner `Bash` + `ToolSearch`. Fix: the gate now blocks ONLY build/dispatch tools (`_BLOCKED_TOOLS = {Edit, Write, NotebookEdit, Agent, Task, TaskCreate, TaskUpdate, TaskStop}`) and allows everything else immediately; read-only investigation + the wrapper's `Bash` setup are never blocked, so a well-behaved run never trips the gate — it fires only on a genuine BUILD/DISPATCH-by-hand before the Skill. Re-verified across 9 real transcripts / 3939 calls: blocks only on build/dispatch tools (204 genuine catches), ZERO non-build/dispatch tools blocked. Adversarial review: SHIP; corrected a docstring overclaim and documented the inherent residual (raw-`Bash` builds aren't blocked — backstopped after-the-fact by the Layer-6 `skill_invocation_audit` + `verify-no-pipeline-bypass`). Suite 4501 → 4503 passing + 5 skipped (178 test files).
- **v3.15.0 — skill-invocation hard-gate (real-time pipeline-bypass prevention).** A new `PreToolUse[*]` hook `hooks/pretool_skill_gate.py` (stdlib-only) closing the *"sometimes when I call for the architect team, the AI doesnt load the skill"* gap — it converts skill-invocation enforcement from after-the-fact DETECTION (the unwired Layer-6 `skill_invocation_audit.py`) into real-time PREVENTION: when the session transcript's most-recent GENUINE user prompt is an unsatisfied pipeline-command request, it BLOCKS (exit 2) the first non-`Skill` tool call until a pipeline skill is engaged. Universal/global (keyed off the plugin's own `COMMAND_TO_SKILLS` + the Skill ledger, reusing the Layer-6 detection; no codebase-specific strings); scoped to the 5 pipeline-driving commands only; false-block-safe — the anchor excludes the harness's injected/meta records (`isMeta` body-echoes, `promptSource:"system"` notifications, `isSidechain` subagent transcripts), satisfaction = engaging ANY pipeline skill (so `/architect-team`'s `proposal-refiner`-first step never false-blocks) with ts-ordered user-precedence, the `Skill` tool is always allowed, and it fails open on any error. Adversarial review against two real transcripts caught a session-bricking `isMeta` body-echo bug + a refiner-first false-block before ship; re-verified across 9 real transcripts / 3822 simulated tool calls: 0 spurious blocks, 402 genuine bypasses caught. New `tests/test_pretool_skill_gate.py` (38 cases); the PreToolUse wiring test updated for the two-hook reality. Suite 4463 → 4501 passing + 5 skipped (178 test files); NO new Layer 3 tool.
- **v3.14.0 — appearance-change policy (strict / propose / innovate).** A new cross-pipeline discipline closing the unsolicited frontend-appearance-change gap (*"the agent will arbitrarily change our front end, adding things we didnt explicitly ask for"*). Three modes govern what a user SEES: `strict` (the DEFAULT — no appearance-affecting change beyond the explicit mandate; the three sanctioned mandate sources are requirement text / spec restoration / the mandated-capability minimum; backend changes unrestricted; improvement ideas RECORDED to `.architect-team/appearance-proposals/<run-id>.json`, never implemented), `propose` (proposals batched at a multi-select `AskUserQuestion` approval gate; only approved ones implemented), `innovate` (authorized + every delta logged `implemented-innovate` + DESIGN_MAP-reconciled). Canonical home in `common-pipeline-conventions`; `--appearance` flag on the 3 pipeline commands; `appearance_mode` bound at Phase −2 / B−1 / M0 into `intake-state.json` + every spawn brief; completeness-SRs that would ADD new visible UI surface are `appearance_gated` under strict/propose. Schema v7 gains the THIRD optional field `appearance_scope_review` (hook-blocked on `fail`; 17 required fields unchanged); task-reviewer per-delta mandate trace + system-architect Master-Review run-diff walk enforce it; NO new Layer 3 tool (named v3.14.x follow-up). Suite 4415 → 4463 passing + 5 skipped (177 test files).
- **v3.13.2 — phenotype-store docs join the documentation-currency inventory (discipline PATCH).** `phenotypes/README.md` + `phenotypes/SCHEMA.md` are now inventory rows in `documentation-currency` (the v3.13.0 gap class: a fourth phenotype shipped while the store README still said three seeds, and every audit passed because the doc wasn't in the gate). doc-updater read-list + Write-allowlist and the system-architect audit walk-list updated in lockstep (per-phenotype records `phenotypes/<label>/**` explicitly fenced out — feature artifacts, not currency docs); 4 new test pins guard the skill-vs-agent drift. Suite 4411 → 4415 passing + 5 skipped.
- **v3.13.1 — code-wiki usage documentation (docs-only PATCH).** `phenotypes/README.md` brought current (three→four seeds; a `code-wiki` row; a NEW launching-a-code-wiki quick-start matching the scaffold's `post_emit_notes`; the Adding-a-phenotype section now reflects that absorb is shipped and `code-wiki` is its first absorb-tool-produced record) + the main README gains a `### ▸ Launch a code wiki from your maps` USAGE section. Zero code change; counts and the 4411+5 suite total unchanged.
- **v3.13.0 — code-wiki phenotype (absorbed from deepwiki-open).** A fourth phenotype seeded into the v2.3.0 subsystem via `phenotype-absorption`: `code-wiki`, a self-hosted documentation-wiki application pattern absorbed (READ-ONLY) from [deepwiki-open](https://github.com/AsyncFuncAI/deepwiki-open.git) (MIT). The presentation layer — sidebar nav tree + markdown content pane + client-rendered Mermaid + dark/light theming — is re-expressed as a lean, generalized Next.js scaffold (App Router, standalone, plain CSS, deps `next/react/react-dom/react-markdown/remark-gfm/mermaid`); the entire LLM machinery (9 providers, RAG, WebSocket chat/Ask, slides/workshop, the `api/` Python tree) is the strip boundary, and the content source is swapped to a `lib/maps-loader` that ingests a `codebases.json` registry → `docs/*_MAP.md` into the kept deepwiki wiki-cache JSON shape. `kind=singleton` (the P2 verdict); `components.deploy.via = "config-management phenotype"` (the ai-management precedent — `iac/aws` ECS Fargate + `iac/gcp` Cloud Run service-layer plug-ins). Demo EXECUTED: emit → `npm install/build/start` → HTTP 200 on the index + a CODEBASE_MAP page with derived content → a Playwright screenshot of 2 rendered Mermaid SVGs + the nav tree; `tofu validate` passes both IaC sets. `tests/test_code_wiki_phenotype.py` (22 cases) green; engine/schema/the three prior seeds untouched. The run also folded in an upstream fix: `hooks/discipline_registry.py` now skips `.architect-team/` in its frontend-marker scan (a 3-site `_SKIP_DIR_PARTS` class fix + 4 regression tests, SR-routed through `diagnostic-research-team`) so a vendored read-only absorption reference clone no longer false-flags a no-UI repo. Suite **4411 passing + 5 skipped** (176 test files) both encodings.
- **v3.12.0 — structure-optimization performance + review remediation.** A 3-lens review panel (consistency architect + adversarial refuter + performance analyst) audited the v3.11.0 `structure-optimization` set and verified 16 findings — 2 blocking partition-check bugs (`.split()` → `.splitlines()`; `os.path.normcase` on both sides), 1 dead notifier wiring (`pipeline_complete` → `phase_complete`), 8 material ambiguities (shard policy + assembly, S6-fail routing, multi-codebase partition loop, arg precedence, dir creation, duplicate recovery, convergence criterion), and 3 under-pinned invariants — plus a ranked optimization set fixing the S5/S3 cost dominators. Shipped IN-PLACE (zero new files): the S5 adversary-round warm-start (delta-scoped re-runs + carried `modalities_run` union + re-confirm-not-re-derive), per-round partition-recompute dedup (published `adversarial/round-<R>/partition-check.json`), per-draft/per-revision front-loading, the structured agree/dispute convergence with frozen rows, the executable balance-by-reference-surface shard policy + assembly validation, S1 freshness pipelining + an orchestrator-precomputed file universe, payload-trimmed briefs, S7 mechanical transcription, S6 thinnest-coverage spot-check sampling, the 3-adversary floor note, and a permanent `## Optimization guardrails` section fencing four anti-candidates — every accuracy invariant (deterministic partition check, full reference closure with mandatory `search_log`, two-consecutive-clean exit, architect all-five-blocks gate, openspec strict validate) preserved verbatim.
## Conventions at a glance

- Author commits with explicit override (repo's local git config has a "Paul Ingrram" typo):
  `git -c user.name="Paul Ingram" -c user.email="paulingram@users.noreply.github.com" commit ...`
- Run the test suite via `python -m pytest -v` from repo root (green under both Windows cp1252 and `PYTHONUTF8=1`; the expected pass/skip count for the current release is in the CHANGELOG entry).
- Bump version: update `.claude-plugin/plugin.json` + `.claude-plugin/marketplace.json` + `CHANGELOG.md`.
- Runtime state lives under `.architect-team/` (gitignored). v1.1.0 splits it into **shared** (in main worktree: `locks/`, `.mempalace/`, `run-history/`) and **per-run** (in current worktree: `reviews/`, `teammates/`, `handoffs/`, this-run's `openspec/changes/<slug>/`). The `scripts/setup/worktree_paths.py` helper resolves both via `shared_state_dir()` / `run_state_dir()`. **v1.2.0** auto-creates the run worktree (default; `--no-worktree` opts out) via `scripts/setup/worktree_lifecycle.py::create_run_worktree(slug)` — branch `architect-team/<slug>`, path `<parent-of-repo>/<repo-name>-<slug>/`. **v1.3.0** auto-cleans merged worktrees on the next run via `cleanup_merged_worktrees()` (with `exclude_current=True` keeping the cwd safe); the `/architect-team:cleanup-worktrees` command is the on-demand variant. **v3.7.0** auto-merges a clean run's branch `--no-ff` into `main` and prunes it (`--no-auto-merge` opts out; a conflict falls back to a PR, never `--force`).
