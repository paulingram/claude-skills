# Copy to .env.local (dev) or .env (docker) and adjust.
# WIKI_CONTENT_DIR: the directory holding codebases.json + the maps it points at.
WIKI_CONTENT_DIR={{wiki_content_dir}}
# PORT: the HTTP port the wiki serves on (also set in package.json scripts + compose).
PORT={{port}}
